# AGENTS.md

## 1. 目的

本文件用于约束所有参与 `trade-strategy-ai` 重构的 AI Agent。

Agent 必须严格按照正式 TaskList 和重构方案执行，不得自行改变产品方向，不得省略任务，不得在未满足验收标准时声称完成。

最终目标是将项目重构为面向中国普通用户的完整产品：

```text
文章导入
→ LLM 提取规则
→ 人工审核
→ 历史回测
→ 分市场状态验证
→ 作者方法画像
→ 策略发布
→ 每日盘前
→ 每日盘后
→ 规则、画像和策略持续优化
```

---

# 2. 文档目录约束

所有重构相关文档只能在以下目录读取和生成：

```text
trade-strategy-ai/docs
```

要求：

1. TaskList、设计说明、迁移方案、Review、验收报告、实施记录和用户文档必须放在该目录。
2. 不得在仓库根目录或源码目录生成新的重构文档。
3. 不得在文档中写入本机绝对路径。
4. Prompt 文件可放在项目约定目录，但 Prompt 设计、版本和迁移说明必须放入 `trade-strategy-ai/docs`。

---

# 3. 必读顺序

开始任何重构任务前，必须依次读取：

1. `trade-strategy-ai/docs/Trade-Refactor-TaskList.md`
2. `trade-strategy-ai/docs/trade-strategy-ai-web-refactor-plan-market-state-v2.md`
3. `trade-strategy-ai/docs/PROMPT_REVIEW_AND_MIGRATION.md`
4. `trade-strategy-ai/docs/AUTHOR_PROFILE_PROMPT_FLOW.md`
5. 当前任务直接相关的设计、迁移和验收文档
6. 相关代码、测试、数据库模型和 API

优先级：

```text
AGENTS.md
> 主 TaskList
> 最新正式重构方案
> 当前任务设计和迁移文档
> 历史文档
```

发现冲突时，遵循优先级更高的文档，并在实施记录中说明。

---

# 4. 任务执行规则

1. 每次只执行一个明确 Task ID，或用户明确指定的一组紧密关联任务。
2. 不得跳过前置依赖。
3. 不得跨多个 Stage 自行扩大实现范围。
4. 实现前必须检查现有代码，避免重复建设。
5. 可以修改 UI、前端、后端、数据库、Prompt 和运行时实现。
6. 允许删除、合并或替换旧实现，但必须完成数据迁移、兼容验证和退役说明。
7. 不得只完成前端或后端后宣称业务闭环完成。
8. 不得以 Mock、硬编码、静态占位页或空接口冒充正式实现。
9. 不得为了赶进度省略测试、迁移、错误状态或用户说明。
10. 发现现有实现与目标冲突时，优先保证正式产品目标正确。

---

# 5. 面向用户约束

最终 Web 必须面向普通用户，而不是开发人员。

普通用户主页面和主导航不得直接出现：

- Job
- Workflow
- Pipeline
- Artifact
- Provider
- Schema
- CLI
- 数据库表名
- 内部函数名
- 文件路径
- `kaipan-fetch`
- `kaipan-normalize`
- `ohlcv-crawl`
- `market-state-build`

必须使用业务中文，例如：

```text
更新历史行情
整理盘前市场数据
开始回测
生成盘前计划
查看本次结果
重新执行失败步骤
```

每个主要页面必须明确：

1. 页面用途
2. 当前需要的输入
3. 系统将执行的处理
4. 输出结果
5. 下一步操作

必要时可加入简短使用说明，但不得解释内部开发架构。

用户界面统一使用“市场状态”，不得展示 `Regime`。

---

# 6. 单一事实源与版本约束

不得同时维护两套正式：

- 页面入口
- 规则 Schema
- 作者画像 Schema
- 策略 Schema
- Prompt 调用链
- 回测结果
- 数据查询方式

兼容层只能作为过渡，必须有退役条件。

以下对象必须版本化并可追溯：

- Prompt
- 规则
- 作者画像
- 策略
- 市场状态模型
- 回测数据集
- 规则适用性结果

不得使用临时文件路径代替正式业务引用。

数据库结构可以修改，但必须：

- 提供迁移
- 支持安全重跑
- 避免静默丢失数据
- 标记无法完整迁移的数据
- 提供恢复或回滚方案
- 测试升级路径

---

# 7. Prompt 与 LLM 约束

1. Prompt 必须版本化。
2. Prompt 输出必须通过 Pydantic 或 JSON Schema 校验。
3. Prompt 文件、运行时代码和测试不得维护不同 Schema。
4. Prompt 修改后必须使用固定样本集做回归测试。
5. 旧 Prompt 只能短期兼容，不能继续承载新功能。

LLM 不得：

- 编造文章未声明的规则
- 编造止盈、止损、持有周期或市场状态
- 计算或伪造回测指标
- 声称作者真实收益率、胜率、仓位或执行纪律
- 直接发布正式规则、画像或策略
- 根据单日结果修改稳定画像
- 把推断写成事实

必须区分：

```text
文章明确声明
LLM 推断
程序统计
回测观察
人工批准
```

100+ 篇文章必须采用：

```text
逐篇结构化
→ 去重聚类
→ 批次汇总
→ 规则族
→ 回测验证
→ 增量更新
```

不得一次性把全部文章全文发送给 LLM。

---

# 8. 数据、回测和策略约束

## 8.1 数据

OHLCV 是基础回测数据，必须支持历史回灌、每日增量、缺口检查、补抓和固定数据版本。

Kaipan 用于盘前、盘后和增强回测。

缺失数据不得：

- 默认为 false
- 默认为 0
- 默认为条件满足
- 静默降级

必须明确显示缺失范围、影响和处理方式。

## 8.2 回测

回测必须：

1. 固定 DatasetSnapshot
2. 禁止调用实时 Provider
3. 防止未来数据泄漏
4. 记录规则、数据、基准和市场状态模型版本
5. 输出全周期和分市场状态结果
6. 样本不足时标记 `insufficient_sample`
7. 保证结果可复现

## 8.3 策略

正式策略不得每天生成。

每天只生成：

```text
DailyRuleSelection
DailyStrategyInstance
TradingDayPlan
```

盘后必须分别生成：

```text
RuleOptimizationProposal
AuthorProfileRevisionProposal
StrategyRevisionProposal
```

单日结果不得直接覆盖正式规则、画像或策略。

---

# 9. UI、错误和状态约束

新增或修改页面必须处理：

- loading
- empty
- error
- partial
- permission denied
- unavailable

所有错误必须告诉用户：

```text
发生了什么
影响了什么
应该怎么处理
```

不得只显示异常堆栈、HTTP 状态码、`Job failed` 或 `Unknown error`。

用户可见按钮必须使用明确动作，例如：

```text
开始回测
补齐缺失数据
批准今日计划
发布新策略
查看失败原因
```

不得长期使用含义模糊的“执行”“运行”“提交”。

---

# 10. 测试与回归约束

每个任务必须根据影响范围执行：

- 单元测试
- API 测试
- 数据库迁移测试
- 前端组件和路由测试
- Prompt 回归测试
- 数据迁移测试
- E2E 测试
- 人工验收

不得：

- 删除测试以消除失败
- 放宽断言适应错误实现
- 跳过测试后声称通过
- 只运行局部测试而忽略受影响模块

无法运行测试时，必须明确记录：

- 未运行的测试
- 原因
- 已完成的替代检查
- 剩余风险

---

# 11. 完成判定

以下情况不得标记完成：

- 只写了文档
- 只创建空文件或占位页
- 只创建数据库表但未接入业务
- 只实现 API 但页面不可用
- 页面仍使用 Mock
- Prompt 未接入调用链
- 数据只迁移一部分
- 测试失败或未运行
- 验收标准未满足
- 功能无法从主流程访问
- 用户仍需理解内部开发步骤
- 结果无法追溯
- 存在未说明的阻塞

状态统一使用：

```text
[ ] 未开始
[-] 进行中
[x] 已完成
[!] 阻塞
[~] 延后但不阻塞当前阶段
```

只有全部验收通过后才能使用 `[x]`。

部分完成必须使用 `[-]`，并明确：

- 已完成内容
- 未完成内容
- 阻塞原因
- 下一步
- 当前风险

不得使用“基本完成”“主要完成”等模糊表述替代真实状态。

---

# 12. 实施记录

每完成一个任务，必须在以下文档更新记录：

```text
trade-strategy-ai/docs/Refactor-Implementation-Log.md
```

至少记录：

```text
Task ID
状态
修改范围
关键设计决定
数据库迁移
兼容处理
已运行测试
测试结果
未完成项
已知风险
验收结论
```

不得只在聊天、终端或提交信息中记录状态。

---

# 13. 最终交付标准

整个重构只有在以下条件全部满足后才能宣布完成：

1. 用户进入首页后知道下一步
2. 用户能独立完成文章到规则
3. 用户能理解规则证据和可回测状态
4. 用户能理解分市场状态回测结果
5. 用户能理解作者画像不是实盘画像
6. 用户能创建、验证、发布和回滚策略
7. 用户每天主要只需查看盘前和盘后
8. 数据缺失时能直接修复或明确降级
9. 页面无不必要开发术语
10. 所有规则、画像、策略和每日计划可追溯
11. 旧入口完成迁移和退役
12. 测试、迁移、用户文档和 E2E 验收全部完成

任何一项未满足，都不得宣布整个重构完成。

---

# 14. Agent 完成回复格式

完成一个任务后，必须使用以下格式：

```text
任务：
状态：
已完成：
未完成：
修改文件：
数据库迁移：
已运行测试：
测试结果：
已知风险：
验收结论：
```

未完成时，状态必须是：

```text
进行中
或
阻塞
```

不得使用模糊语言暗示完成。
