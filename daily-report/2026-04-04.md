# 2026-04-04 日报

## 今日产出
- Persona Router MVP 代码落地：多风格 StyleCluster + MarketState 驱动路由（收益最大化权重）
- 标准化 JSON schema：明确 `article_metadata.strategy_rules[]` / `preconditions[]` 字段结构与 claim_key 字典（v0）
- 闭环接入：盘前 TradeIdea 标注风格簇，日报 HTML 增加“风格簇”列，输出路由决策 JSON
- MarketState 规则化分类：支持用指数/ETF 日线 CSV（如 510300）生成 regime/vol
- AkShare 数据接入：新增可复用工具类拉取 510300 日线并缓存为 CSV，`market-state-build --from-akshare` 可一键生成 MarketState
- 宿主薄壳预留：提供稳定 JSON 命令契约与 handler，为 Claude Code/Openclaw 集成铺路
- 文档全量同步：需求/计划/架构/TaskList/规格/README 更新
- 工程与环境：固化 Python 3.13.10 作为默认构建/运行环境，并清理不再需要的 .venv 备份目录。

## 关键变更（代码/文档）
- 代码：trade-strategy-ai/src/persona/*、trade-strategy-ai/src/host/*、trade-strategy-ai/src/agents/manager_agent/agent.py、trade-strategy-ai/cli/main.py
- 文档：doc/PersonaRouterSpec.md、doc/需求.md、doc/Plan.md、doc/Project.md、doc/TaskList.md、README.md、trade-strategy-ai/README.md

## 当前状态
- Phase 0：原盘前/盘后闭环已具备
- Phase 0.5：已在本机完成一次端到端运行（AkShare 拉取 → MarketState → 路由 → 日报/考核输出）

## 风险与阻塞
- dev extras（ruff/mypy）下载可能受网络波动影响；运行与测试可先只装 pytest 相关最小依赖。

## 下一步任务计划（Phase 1 最小切片）
1) 增量抓取最小链路（先落盘，后入库）：
	- 目标：按 trader/source 配置抓取文章列表与正文；用 last_seen + content_hash/URL 去重；可重复运行。
	- 交付物：data/processed/crawl/{source}/{author_id}/articles.jsonl

2) 文章抽取落地到结构化字段（先规则后 DSL）：
	- 目标：对每篇文章产出 article_metadata（strategy_rules/preconditions），字段遵循 doc/PersonaRouterSpec.md。
	- 交付物：data/processed/extract/{source}/{author_id}/article_metadata.jsonl

3) 真实数据生成 StyleCluster 并替换 sample：
	- 目标：基于抽取结果做分桶/聚合，生成 clusters 文件；盘前路由读取真实 clusters。
	- 交付物：data/processed/persona/clusters.{source}.{author_id}.json + persona_route_YYYY-MM-DD.json

4) 回放验证与最小指标：
	- 目标：固定一天 as_of_date，重复跑“抓取→抽取→聚类→路由→日报”，确保结果可复现；记录样本量与失败原因。

## 今日追加产出（Phase 1 抓取启动）
- 完成淘股吧真实需求收敛：确认手工 Cookie 首版、评论清洗规则、作者/读者分类、楼中楼拍平但保留可恢复字段。
- 将抓取架构直接提升为“同站点多作者、兼容未来多站点”，避免后续为多作者/多站点返工。
- 新增抓取设计文档：`doc/2026-04-04-tgb-incremental-crawl-design.md`
- 新增实现计划：`doc/2026-04-04-crawl-implementation-plan.md`
- 新增使用说明：`doc/使用说明.md`，记录淘股吧 Cookie 获取和 `crawl.auth.tgb.cn.cookie` 配置方式
- 更新 TaskList：补充 `crawl.auth` / `crawl.sources` 配置拆分、`TgbCrawler`、轻量反爬、自动登录预留等任务

## 今日追加代码变更
- 配置层：
  - `trade-strategy-ai/src/common/config.py` 新增 `crawl` 配置模型、域名认证配置、source 配置、环境变量展开
  - `trade-strategy-ai/config/app.yaml` 增加 `crawl` 默认结构
- 抓取层：
  - `trade-strategy-ai/src/agents/data_agent/skills/crawl_blog.py` 实现多作者遍历、状态文件、JSONL 落盘、去重、评论清洗
  - `trade-strategy-ai/src/agents/data_agent/sites/base.py` 定义认证与站点抓取器边界
  - `trade-strategy-ai/src/agents/data_agent/sites/tgb.py` 实现淘股吧首版解析，支持详情页、DOM 评论解析和文本块回退解析
- CLI：
  - `trade-strategy-ai/cli/crawl.py`、`trade-strategy-ai/cli/main.py` 增加 `crawl` 命令入口

## 今日追加验证
- 新增测试：
  - `trade-strategy-ai/tests/unit/common/test_config.py`
  - `trade-strategy-ai/tests/unit/agents/test_crawl_blog.py`
  - `trade-strategy-ai/tests/unit/cli/test_crawl_cli.py`
- 验证命令：
  - `../.venv/bin/pytest tests/unit/common/test_config.py tests/unit/agents/test_crawl_blog.py tests/unit/cli/test_crawl_cli.py -q`
- 验证结果：`7 passed`

## 当前阶段状态（更新）
- Phase 1 已进入真实抓取实现阶段，且已有可扩展的首版骨架。
- 当前可确认：支持同站点多作者配置，兼容未来多站点扩展；淘股吧已具备首版解析能力。
- 当前未完成：真实 Cookie 联调、分页抓全、自动登录和更完整反爬。

## 会话收尾记录
- 已按用户确认执行结束流程，并更新今日 `daily-session` / `daily-report`。
- 已核查配置中的 Cookie / API Key 落盘情况；当前仓库配置文件中未发现需要额外清除的真实敏感值。
- 会话结束时的执行重点保持不变：等待真实 `crawl.auth.tgb.cn.cookie` 与作者源配置后，进行第一次 `crawl` 联网联调。

## 今日追加产出（真实联网联调完成）
- 用户已配置真实 `tgb.cn` Cookie，本轮完成了第一次真实联网联调，而不是仅停留在本地骨架验证。
- 修正 `trade-strategy-ai/config/app.yaml` 中的配置层级错误：
  - 原本 `crawl.sources` / `crawl.throttling` 误缩进到 `crawl.auth.tgb.cn` 下，程序加载后 `sources_count=0`
  - 修正后配置可正常识别，真实作者源已生效
- 修复淘股吧真实列表页解析：
  - 真实文章链接为 `a/...`，不带前导 `/`
  - 调整 `TgbCrawler.parse_article_list` 后，真实列表页可解析出 `100` 篇文章
- 修复淘股吧真实评论解析：
  - 真实文章页评论结构为 `.comment-data` / `.comment-data-text` / `.user-name` / `.pcyclspan`
  - 新增对应解析分支后，已不再把整篇正文误混入首条评论

## 今日追加验证（真实站点）
- 单测补充并通过：
  - 列表页相对链接解析测试
  - `.comment-data` 评论结构解析测试
- 当前 `trade-strategy-ai/tests/unit/agents/test_crawl_blog.py` 验证结果：
  - `6 passed`
- 真实联调命令：
  - `../.venv/bin/python -m cli.main crawl --config config/app.yaml --max-articles 1`
- 真实联调结果：
  - 列表页 `HTTP 200`
  - 详情页 `HTTP 200`
  - 最终输出 `tgb:10461311:1`

## 今日追加真实产物
- `trade-strategy-ai/data/processed/crawl/tgb/10461311/articles.jsonl`
- `trade-strategy-ai/data/processed/crawl/tgb/10461311/state.json`
- 已验证样本文章：
  - 标题：`教你看懂市场内力与外力的本质`
  - 正文长度：`13531`
  - 评论数：`49`
- 当前评论抽取质量已明显改善，首批预览已是逐条评论记录，而非正文污染。

## 当前阶段状态（再次更新）
- Phase 1 已完成“真实 Cookie + 最小 crawl 联调”这一步关键验证。
- 当前真正的下一步已从“第一次联网联调”切换为：
  1. 列表分页抓全
  2. 评论分页/对话页抓全
  3. 恢复更完整的楼中楼结构字段
  4. 自动登录 / Cookie 续期
