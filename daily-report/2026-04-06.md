# 2026-04-06 日报

## 今日成果

**Phase 1.1 + 1.2 完成**：DuckDB 增量导出 + pending_tasks 处理器，全部集成到 pipeline DAG。

### 实现内容

1. **export_task.py**：异步增量导出 articles + metadata 到 DuckDB
   - 支持 force_full 全量导出
   - DuckDB 表结构：articles(20列) + metadata(12列)
   - INSERT OR REPLACE 幂等写入

2. **pending_tasks 处理器**：`src/pipeline/tasks/process_tasks.py`
   - `run_process_tasks()`: 读取 pending_tasks.jsonl，按 article_id 去重
   - 固定次数重试（3次，指数退避）
   - 失败写入 failed_tasks.jsonl
   - Pipeline DAG：`crawl → clean → validate → store → process → export`

3. **Pipeline DAG 集成**：
   - `crawl → clean → validate → store → export` 端到端链路完成
   - `PipelineRunResult` 新增 `export: ExportResult` 字段

### 验证

- 全量导出：1 article + 1 metadata，28s
- 增量跳过：0 新增，107ms（正确）
- Pipeline 端到端：store=0（无新文章），pending_tasks 处理后清空，failed_tasks 无记录

## 发现的问题 / 风险

1. ~~**export_task 增量水位风险**~~ ✅ 已修复 P1-026H
2. ~~**process_tasks 配置注入仍为模块级 global**~~ ✅ 已修复 P1-026D
3. ~~**failed_tasks 无限积累无清理**~~ ✅ 已修复 P1-026E
4. **P1-026B** DuckDB JSON 读写约定待补齐（json_extract 用法）
5. **P1-026C** export_task PostgreSQL 回归待验证

## 下一步（按优先级）
1. ~~**P1-026H**~~ ✅ export_task 增量水位：crawled_at watermark + export_state 表
2. ~~**P1-026D**~~ ✅ process_tasks 去 global config：闭包捕获
3. ~~**P1-026E**~~ ✅ failed_tasks 重试 + TTL 清理：MAX_RETRY_COUNT=3, FAILED_TTL_DAYS=7, dead_tasks.jsonl
4. LLM 抽取质量提升：失败样本集、schema 统计、prompt 迭代（先聚焦 metadata 抽取链路）
5. clusters 聚类升级：claim_key/规则相似度聚类 + 产物可回放
6. Persona 证据引用（evidence_refs）：补充原文片段/URL/时间戳
7. 增量聚类：新增/更新文章 → 只增量计算
8. API 薄壳（可选）：/host/command 与产物下载
9. CI 回归：migration + pipeline dry-run

## 产物
- `src/pipeline/tasks/export_task.py`
- `src/pipeline/tasks/process_tasks.py`
- `data/processed/duckdb/trade_strategy_ai.duckdb`
- `doc/superpowers/specs/2026-04-06-duckdb-export-design.md`
- `doc/superpowers/specs/2026-04-06-pending-tasks-processor-design.md`
- `doc/superpowers/plans/2026-04-06-pending-tasks-processor.md`

## 未提交
- fix: pass config from DAG to process_tasks handlers（需从 Trade 目录执行 git commit）

## 明日计划（2026-04-07）

### 优先级 1：P1-026B — DuckDB JSON 读写约定补齐
- 确认 DuckDB JSON 字段（tags, comments_payload, raw_payload 等）写入为 TEXT，读回需 json_extract
- 补充 json_extract 查询示例到文档
- 状态：待办，难度低，与 P1-026H 直接相关

### 优先级 2：P1-026C — export_task PostgreSQL 回归验证
- 确认 DATABASE_URL 配置
- 在 PostgreSQL 环境运行全量/增量导出，验证性能与一致性
- 状态：待办，需要 PostgreSQL 环境

### 优先级 3：P2 LLM 抽取质量提升
- 收集 metadata 抽取失败样本集
- 统计 schema 覆盖率（extracted_concepts, trading_symbols, strategy_rules 等）
- prompt 迭代（先聚焦 metadata 抽取链路）
- 状态：P2 中等优先级，与 Phase 2 对齐
