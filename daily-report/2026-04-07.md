# 2026-04-07 日报

## 今日成果
- **P2-016 技术指标特征库完成**：PatternFeatureEngine + evaluate_condition() 接口，41 个测试全通过
- **engine.py bug 修复**：3 个 sma 数组索引 bug（bollinger/stochastic/macd）
- P1-023、P1-024、P1-025 数据质量体系完成（见昨日详情）

## 实现内容
1. **P2-016 PatternFeatureEngine**：特征计算引擎，支持：
   - 11 个基础特征纯函数（volume_ratio, price_vs_ma, ma_slope, gap_ratio 等）
   - 8 个惰性计算指标（RSI, Stochastic, MACD, Bollinger, CCI, MA50, MA200）
   - `evaluate_condition(field, op, value)` 核心判断接口，支持 80+ op
   - 形态识别（头肩底/顶，双底/顶、旗形、杯柄，三角收敛等）
2. **engine.py bug 修复**：
   - `bollinger`: `middle[last_idx]` → `middle[-1]`
   - `stochastic`: `d_vals[d_idx]` → `d_vals[-1]`
   - `ensure_bb_width/ma50/ma200`: bar 数检查修正
3. **扩展文档**：`docs/superpowers/guides/adding-indicators.md` 说明如何新增指标/op

## 验证
- 全部 41 个测试通过（Task 2: 18 个，Task 3: 23 个）
- 3 个 commit，均在 main 分支

## 发现的问题 / 风险
- canonical pattern 的形态识别算法使用简化实现，需真实数据验证
- engine.py sma 函数行为不直观（返回比输入短的数组），已通过修复和文档说明

## 优先处理问题（Code Review 发现）

### 🔴 HIGH - 安全问题
| # | 文件 | 问题 | 修复建议 |
|---|------|------|----------|
| S1 | `config/app.yaml:159` | 硬编码 API key `internal-key-001` | 改为环境变量注入 |
| S2 | `src/api/main.py:21-27` | CORS `allow_origins=["*"]` + `allow_credentials=True` 不安全 | 指定明确域名或环境变量配置 |
| S3 | `src/api/dependencies.py:44-49` | auth.enabled=true 但 api_keys=[] 时任何key都通过 | 无有效keys时应拒绝请求 |

### 🔴 HIGH - 逻辑Bug
| # | 文件 | 问题 | 修复建议 |
|---|------|------|----------|
| B1 | `src/indicators/engine.py:200-201` | ATR计算 window 验证 `len(tr) < window` 逻辑错误 | 改为 `len(tr) <= window` |
| B2 | `src/indicators/engine.py:230-237` | stochastic 计算无边界检查，高/低价数组短于收盘价数组会 IndexError | 添加 `len(highs) >= len(closes)` 验证 |
| B3 | `src/indicators/pattern_features.py:196-200` | `day_range == 0` 时返回硬编码 `0.5` 掩盖无效数据 | 返回 `np.nan` 或抛出异常 |

### 🟡 MEDIUM - 代码质量
| # | 文件 | 问题 | 修复建议 |
|---|------|------|----------|
| Q1 | `src/indicators/pattern_features.py:878-988` | 多个 `_eval_*` 方法返回 `False` 是 stub | 实现或删除 + 更新分发表 |
| Q2 | `src/persona/pattern_loader.py:151-153` | 静默跳过畸形pattern文件，难以调试 | 添加日志或统计 |
| Q3 | `src/persona/dsl.py` vs `schemas.py` | `ConditionExpr` 类型定义重复且不一致 | 统一为 Pydantic model |
| Q4 | `src/persona/pattern_matcher.py:154-359` | 条件判断的索引检查不一致，部分会 IndexError | 统一添加 `idx < 1` 检查 |

### 🟡 MEDIUM - 设计问题
| # | 文件 | 问题 | 修复建议 |
|---|------|------|----------|
| D1 | `src/persona/dsl_validator.py:72-79` | 字段验证逻辑 `if not expr.field` 后未独立验证 `cmp` | 改为独立 if 分支 |
| D2 | `src/indicators/pattern_features.py:120-130` | 每次 `evaluate_condition` 重复创建 numpy 数组 | 缓存数组转换 |
| D3 | `config/settings.py` vs `database.py` | `@lru_cache` 循环依赖风险 | 统一缓存策略 |

### 🟢 LOW
- magic numbers 未提取为常量（`pattern_features.py` 多处）
- `engine.py` 缺少函数 docstring
- `validation.py` 474行过大，考虑拆分

## 下一步（按优先级，可离线开发）
1. P2-010 — 交易特征集计算（夏普比、最大回撤、胜率，纯算法）
2. P2-012 — 行为聚类（K-means/DBSCAN，可用模拟数据）
3. P2-015 — 特征计算脚本（Pandas/Polars 数据转换 pipeline）
4. P2-011 — 特征归一化算法
5. P2-017 — 基本面特征计算（PE、PB、增长率）

**阻塞中（需LLM或真实数据）**: P2-003/004/005（LLM提取），P2-020~023（数据验证）

## 产物
- `src/indicators/pattern_features.py`
- `tests/unit/indicators/test_pattern_features.py`（41 tests）
- `docs/superpowers/specs/2026-04-07-p2-016-technical-indicators-design.md`
- `docs/superpowers/plans/2026-04-07-p2-016-technical-indicators-plan.md`
- `docs/superpowers/guides/adding-indicators.md`
