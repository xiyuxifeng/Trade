# 2026-04-04 daily session

## 一句话目标
把 Phase 0.5（Persona Router）跑通，并把 Phase 1 抓取层推进到“真实站点可用 + 增量落盘 + 分页/反爬可跑”。

## 关键决策
- 运行形态：独立 Python 应用（CLI 为核心，可选长期调度/服务）；不自动下单。
- Persona：同一作者允许多个 StyleCluster 并存；盘前基于 MarketState 选 Top-1 + Top-2，并输出可解释理由。
- 抓取：首版手工 Cookie（不做自动登录），但站点/认证边界必须为 Playwright/续期预留；第一版先做低频反爬与强去重。

## 今日完成（代码/能力）
### Phase 0.5：Persona Router MVP
- 已实现 StyleCluster/MarketState/路由与 clusters 文件读写；ManagerAgent 盘前标注 style_cluster 字段并输出 persona_route JSON；日报 HTML 增加“风格簇”列。
- MarketState v0：基于指数/ETF 日线（例如 510300）规则化分类（MA20/MA60 + vol20 分位 + ret5 shock）；AkShare 工具支持拉取并缓存 CSV。

### Phase 1：淘股吧增量抓取（真实可用）
- 配置层：新增 crawl 配置模型（crawl.auth / crawl.sources / crawl.throttling），支持环境变量展开。
- 抓取主流程：同站点多作者遍历、state.json 增量状态、articles.jsonl 落盘、URL/content_hash 去重、评论清洗与作者/读者标记。
- 站点适配器：TgbCrawler 完成列表/详情/评论解析；支持列表分页、评论分页、楼中楼结构拍平字段（parent/root/reply_to_user）。
- 反爬增强：随机 throttle、403/429 指数退避重试、请求头增强、登录态判断更精确。

## 真实联调与测试
- 单元测试：完整跑过 unit（17 passed）。
- 真实站点联调：tgb 列表/详情/评论均可抓取；示例作者（10461311）已完成增量落盘与跳过验证；评论分页去重后得到 90 条唯一评论。

## 产物（落盘）
- crawl：trade-strategy-ai/data/processed/crawl/tgb/10461311/articles.jsonl、state.json
- persona：trade-strategy-ai/data/processed/phase0/persona_route_2026-04-04.json

## 环境约定
- 默认 Python 3.13.10；已清理多余 venv 备份，仅保留 .venv。

## 下一步（10 个任务 / TODO，按依赖顺序）
- [ ] 1）对齐 TaskList 与代码现状：梳理 Phase 1 数据层哪些已实现（models/迁移/入库/pipeline），补齐“完成定义”，避免状态漂移。
- [ ] 2）补齐数据库运行环境：完善 trade-strategy-ai/docker-compose.yml（PostgreSQL），补充 .env.example，并做一次 async 连接可用性验证。
- [ ] 3）Alembic 首个迁移：创建 migrations/versions 并生成初始 migration（blog_articles/article_metadata/market_data/trade_logs）。
- [ ] 4）实现 JSONL → DB 入库：填充 trade-strategy-ai/src/agents/data_agent/skills/store_db.py，支持 source_url/content_hash 幂等去重与 upsert。
- [ ] 5）补齐 clean + validate：把评论清洗与 DataValidator 串起来，输出“可抽取/不可抽取”原因统计。
- [ ] 6）实现最小 pipeline：补齐 trade-strategy-ai/src/pipeline/{dag.py,scheduler.py} 与 pipeline/tasks（crawl/clean/validate/store），形成一键全链路。
- [ ] 7）LLM 抽取 v0：实现最小 LLM client（配置驱动），用 prompts/ 把文章抽取为 ArticleMetadata.strategy_rules/preconditions 并落库。
- [ ] 8）增量触发（P1-006B）：当检测到新文章/评论更新时，生成“抽取/聚类/记忆刷新”任务（先落盘为待办，后续再异步化）。
- [ ] 9）真实 StyleClusters 生成：实现“文章抽取结果 → StyleCluster 聚合/打分”的 v0，产出 clusters.json，替换 sample clusters。
- [ ] 10）端到端回归：跑通 crawl → store_db → extract → build_clusters → run-pre-market，并验证日报 HTML 的风格簇列来自真实数据。

## 风险与注意事项（本阶段）
- DB/迁移一旦落地，后续 schema 变更要严格走 Alembic，避免本地/CI 环境漂移。
- LLM 抽取先追求“可用+可落库+可回放”，不要在 v0 追求高准确率；优先把失败样本可追踪。
- Cookie 抓取仍有失效风险；优先把“抓取失败原因/退避统计”结构化记录，便于后续自动登录接入。
