# 2026-04-07 daily session

## 一句话目标
完成 P1-023（异常检测）、P1-024（去重去噪）、P1-025（数据质量测试）三个任务

## 关键决策
- P1-023 选 A+轻量 batch task：在 validation.py 扩展检测方法 + 新增 anomaly_detection_task.py
- P1-024 选 C：覆盖 Article + TradeLog + MarketData 去重检测，并扩展 clean_task.py 支持实际去重
- P1-025 选 A：仅单元测试，覆盖所有检测逻辑
- IQR 计算改用 numpy percentile 而非简单四分位索引，保证统计正确性
- 高费率阈值默认 1%，通过 config 配置

## 今日完成
1. **P1-023 detect_price_outliers()**：`DataValidator` 新增 IQR 离群检测方法，16 个单元测试全部通过
2. **P1-023 detect_missing_fields()**：覆盖 BlogArticle/TradeLog/MarketData 必填字段
3. **P1-023 detect_sequence_gaps()**：检测日线时间序列缺口，容忍 50%
4. **P1-023 run_anomaly_detection_task()**：新增 anomaly_detection_task.py，批量扫描 JSONL 输出 anomaly report + summary
5. **P1-024 detect_article_duplicates()**：基于 content_hash 和 source_url 检测
6. **P1-024 detect_market_duplicates()**：基于 (symbol, market, timeframe, traded_at) 唯一键检测
7. **P1-024 detect_semantic_noise()**：基于广告正则模式检测文章噪音
8. **P1-024 detect_trade_high_fee()**：检测高费率，默认 1% 阈值可配置
9. **P1-024 detect_trade_duplicates() 扩展**：新增 external_id 检测分支
10. **P1-024 dedup_task.py**：新增 pipeline task，支持基于复合键去重
11. **P1-024 clean_task.py 扩展**：新增 `--remove-duplicates` 参数，启用后自动去除 content_hash/source_url 重复记录
12. **P1-025 test_quality.py**：新增 16 个单元测试覆盖所有检测逻辑
13. **config/app.yaml**：新增 data_quality 配置节（fee_high_threshold, iqr_multiplier, volume_spike_multiplier）
14. **验证结果**：make smoke 2/2 通过，pytest -q 99/99 通过

## 验证结果
- make smoke：2/2 通过
- pytest -q：99/99 通过
- 所有 16 个 quality 测试通过

## 发现的问题 / 风险
- detect_article_duplicates() 对 hash+url 同时重复的情况会报两条 issue（hash 优先），下游使用时需注意去重计数逻辑

## 下一步（本会话）
- 确认 P1-026 数据监控 Dashboard 的设计方向
- 可考虑把 data_quality 配置接入 Settings

## 产物
- `src/pipeline/validation.py`（扩展）
- `src/pipeline/tasks/anomaly_detection_task.py`（新增）
- `src/pipeline/tasks/dedup_task.py`（新增）
- `src/pipeline/tasks/clean_task.py`（扩展）
- `tests/unit/pipeline/test_quality.py`（新增）
- `config/app.yaml`（扩展）
- `docs/superpowers/specs/2026-04-07-data-quality-design.md`（新增）
- `docs/superpowers/plans/2026-04-07-data-quality-plan.md`（新增）

## 风险与注意事项
- anomaly_detection_task.py 和 dedup_task.py 是独立的 pipeline task，尚未集成到 run_pipeline() DAG，后续需确认接入点
