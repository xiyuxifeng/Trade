# 2026-04-11 日报

## 今日成果
完成 P1-032/033 API接口 + 增量爬取修复 + 备份恢复功能

## 实现内容
### P1-032/033 API 手动触发和报告查询
- **手动触发端点** (`api/routers/run.py`):
  - `POST /run/backtest` - 触发动量分析
  - `POST /run/extract` - 触发内容抽取
  - `POST /run/analyze` - 触发综合分析
  - 线程安全：双检锁 + `threading.Lock`
  - 可配置超时：`timeout_seconds` 参数

- **报告查询端点** (`api/routers/reports.py`):
  - `GET /reports` - 查询报告列表
  - 支持分页 (`skip`/`limit`)
  - 支持日期过滤 (`start_date`/`end_date`)
  - 文件 I/O 改为 `asyncio.to_thread()`

### Code Review 问题修复
- **Critical 1**: 修复 FastAPI 启动时 singleton 重复初始化（双检锁）
- **Critical 2**: CORS 改为具体域名配置
- **Important 5,6,7,8**: 超时参数改为可配置

### 增量爬取逻辑修复
- **问题**：已存在于数据库的文章被重复抓取
- **原因**：使用 `last_seen_article_url` 作为停止条件
- **修复**：改为"URL 在 seen_urls 中就直接跳过"，不作为停止条件
- **文档**：已更新 `docs/crawl.md`

### 备份恢复功能
- **备份** (`src/backup/service.py`):
  - `backup_project_state()` - 数据库全量表 + `data/processed/` 目录
  - `restore_project_state()` - 从备份恢复
  - Manifest 记录表名、行数、备份时间

- **CLI 命令** (`cli/main.py`):
  - `backup-data` - 备份到 `data/backups/{timestamp}/`
  - `restore-data` - 从备份恢复
  - `migrate-crawl-state` - 同步 crawl 状态

### 数据库表结构修复
- `market_data` 表：添加 source, market, timeframe, adj_factor, is_adjusted, indicators, raw_payload
- `trade_logs` 表：添加 source, market, position_side, order_type, currency, strategy_tag, rationale
- 新增 migration 文件 `2026-04-11_0002`

## 验证
- `backup-data` 命令执行成功
- Git 已推送：commit 7bcb2e7

## 发现的问题 / 风险
- Alembic 迁移存在历史问题，部分列手动添加
- restore 功能未实际测试

## 下一步
1. 测试 restore-data 命令
2. 跨设备迁移验证
3. 继续 TaskList 其他任务

## 产物
- `api/routers/run.py` - 手动触发端点
- `api/routers/reports.py` - 报告查询端点
- `src/backup/service.py` - 备份恢复服务
- `cli/main.py` - backup/restore/migrate-crawl-state 命令
- `docs/crawl.md` - 增量爬取文档更新
