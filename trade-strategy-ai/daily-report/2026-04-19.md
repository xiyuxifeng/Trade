# 2026-04-19 日报

## 今日成果
- 收敛项目文档入口和唯一任务主线。
- 将主任务清单升级为可脱离上下文执行的交付型 `TaskList`。
- 明确“文章 LLM 提取完成后”的下一阶段工作重点。
- 完成会话收尾：停止 PostgreSQL，并清理配置中的敏感值。

## 实现内容

### 1. 主清单重构
- 将原 `New-TaskList.md` 重构为面向执行的主清单。
- 增加以下关键内容：
  - 无上下文接手规则
  - 任务状态与完成规则
  - 最终可交付标准
  - 每个 Stage 的阶段目标、交付物、子任务、依赖、并行边界
- 将文件正式更名为 `docs/TaskList.md`。

### 2. 文档入口收敛
- 明确当前有效文档入口为：
  - `docs/Project.md`
  - `docs/Plan.md`
  - `docs/需求.md`
  - `docs/TaskList.md`
  - `docs/Kaipan-Interface-Mapping.md`
- 同步修正文档中原 `New-TaskList.md` 的引用。

### 3. 项目主线澄清
- 当前主线不再是旧多 Agent 草图并行建设。
- 唯一主线明确为：
  `数据快照 -> provider -> 市场候选池 -> per-trader 策略版本 -> 盘前决策 -> 盘后评估/归因 -> 回测验真 -> 自主优化`
- 明确 `Project.md` 只负责项目结构说明，不再混入计划职责。

### 4. 下一阶段工作聚焦
- 当前已完成内容主要是文章 LLM 提取。
- 下一阶段不继续深挖文章抽取，而是先补齐：
  - `kaipan` 数据资产层
  - 配置、契约、模型与 migration
  - DataAgent 新能力路由基础

### 5. 会话收尾与协作约定
- 更新 `AGENTS.md` 的“存储约定”，明确以后如何更新 `daily-sessions` 和 `daily-report`
- 停止本地 `postgresql@15`
- 清空 `config/app.yaml` 中的 `cookie` 与 `api.auth.api_keys`
- 将 `docs/TaskList.md` 标题修正为 `TaskList`

## 验证
- 仓库内已无 `New-TaskList.md` 引用。
- `docs/TaskList.md` 已可作为唯一执行入口使用。
- `docs/Project.md / Plan.md / 需求.md / TaskList.md` 的角色边界已明确。
- `postgresql@15` 已成功停止。
- `config/app.yaml` 中敏感值已清理。

## 发现的问题 / 风险
- 当前系统仍未形成稳定市场快照资产，无法直接进入新盘前主链路。
- `DataAgent`、`TraderAgent`、`ManagerAgent` 仍停留在旧最小闭环能力。
- `kaipan` 为私有接口，后续实现必须优先保证原始数据留存与可回放。
- 本次没有新增完成 `Stage 0` 的编码任务，因此 `TaskList` 勾选状态未新增变化。

## 下一步（按优先级）
1. `NTL-S0-007`
   建立 `kaipan` 的 `raw / normalized / snapshots` 目录规范。
2. `NTL-S0-008`
   实现首批 8 个高价值接口的原始 JSON 保存。
3. `NTL-S0-009`
   输出 `hot_topics / topic_constituents / strong_symbols` 标准化快照。
4. `NTL-S0-010`
   为抓取结果记录元信息。
5. `NTL-S0-014`
   为抓取器补最小离线验证。
6. `NTL-S1-001 ~ NTL-S1-013`
   进入配置、契约、模型与 migration 阶段。

## 产物
- `docs/TaskList.md`
- `docs/Project.md`
- `docs/Plan.md`
- `docs/需求.md`
- `docs/Kaipan-Interface-Mapping.md`
- `AGENTS.md`
- `config/app.yaml`
