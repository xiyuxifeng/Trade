# 2026-04-25 日报

## 今日成果

**Stage 5 正式启动，NTL-S5-001 完成。** 同时完成 TD-003-b（Schema/ORM 转换路径明确），所有 Stage 4 技术债全部处理完毕。

## 实现内容

### 1. NTL-S5-001: Evidence Pack 结构

- 新增 `src/evaluation/evidence_pack.py`（EvidencePack dataclass，frozen=True）
  - 聚合：trade_idea / signal_context / market_data / strategy_version_snapshot
  - `from_trade_idea()` 工厂方法：从盘前输出构建证据包
  - `to_dict()` / `from_dict()` 序列化/反序列化（支持 JSON 持久化）
- 新增 `src/evaluation/__init__.py`（模块入口，导出 EvidencePack）

### 2. TD-003-b: Schema → ORM 转换路径明确

- 新增 `src/models/converters.py`（显式转换层）
  - `schema_to_signal_context_orm()`：SignalContext → JSONB dict
  - `orm_to_schema_signal_context()`：dict → SignalContext dataclass
  - `schema_to_trader_strategy_version_orm()`：StrategyVersion → ORM
  - `orm_to_schema_strategy_version()`：ORM → StrategyVersion
  - `schema_to_evidence_pack_dict()` / `orm_to_schema_evidence_pack()`
- 新增 `src/models/evidence_pack.py`（EvidencePackRecord ORM 模型）
  - `pack_data: JSONB` 列存储完整 EvidencePack 结构
  - 外键列（idea_id/trader_id/trade_date/symbol/strategy_version_id）+ 索引
- **存储分类规范**（写入 `docs/superpowers/specs/2026-04-23-stage2-design.md`）：

| 场景 | 存储方式 |
|------|----------|
| 标量字段（str/float/int/UUID） | ORM 列直写 |
| 复杂嵌套（PriceSpec/PositionSize） | JSONB 列 |
| 列表字段（rules_snapshot, triggered_rules） | JSONB 列 |
| 完整聚合（SignalContext, EvidencePack） | JSONB 列 + ID 引用 |

## 验证

- EvidencePack 构造 / 序列化 / 反序列化 roundtrip 验证通过
- SignalContext 转换 roundtrip 验证通过
- EvidencePackRecord ORM from_pack_dict() 验证通过
- 83 tests PASS

## 发现的问题 / 风险

无新增问题。原有 `test_schemas.py` 同名冲突问题保持（需分目录运行测试）。

## 下一步（按优先级，保留 3-6 条）

1. `NTL-S5-002` — 建立失败归因分类（`src/evaluation/failure_taxonomy.py`）
2. `NTL-S5-003` — 建立盘后复盘 service（`src/evaluation/postmortem_service.py`）
3. `NTL-S5-005` — 扩展 TraderMemory schema
4. `NTL-S5-009` — 让 Manager 生成 Evidence Pack（前置 NTL-S5-001 已完成）
5. `NTL-S5-010` — 升级盘后评分口径（MFE/MAE/规则命中）

## 产物

- `src/evaluation/__init__.py`（新增）
- `src/evaluation/evidence_pack.py`（新增）
- `src/models/converters.py`（新增）
- `src/models/evidence_pack.py`（新增）
- `docs/TaskList.md`（NTL-S5-001 标记完成）
- `docs/superpowers/specs/2026-04-23-stage2-design.md`（Schema/ORM 转换路径文档）
- `docs/review/04-24.md`（TD-003-a/b 全部标记完成）
