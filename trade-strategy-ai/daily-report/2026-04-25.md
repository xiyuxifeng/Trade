# 2026-04-25 日报

## 今日成果

**Stage 5 全面推进：NTL-S5-001~005 连续完成。** TD-003-b（Schema/ORM 转换路径明确）同步完成，所有 Stage 4 技术债全部处理完毕。

## 实现内容

### 1. NTL-S5-001: Evidence Pack 结构

- 新增 `src/evaluation/evidence_pack.py`（EvidencePack dataclass，frozen=True）
  - 聚合：trade_idea / signal_context / market_data / strategy_version_snapshot
  - `from_trade_idea()` 工厂方法：从盘前输出构建证据包
  - `to_dict()` / `from_dict()` 序列化/反序列化（支持 JSON 持久化）
- 新增 `src/evaluation/__init__.py`（模块入口）

### 2. NTL-S5-002: 失败归因分类

- 新增 `src/evaluation/failure_taxonomy.py`
  - FailureRootCause：9个根因标签（rule_precondition_failed / signal_quality_low / entry_timing_poor / exit_timing_poor / position_size_mismatch / market_mismatch / external_event / symbol_selection_suboptimal / data_quality_issue）
  - FailureStage：3个阶段标签（stage:entry / stage:exit / stage:holding）
  - FailureRuleType：4个规则类型标签（rule_type:entry / rule_type:exit / rule_type:filter / rule_type:sizing）
  - FailureAttribution dataclass：root_causes + stage + rule_type
  - parse_failure_categories() 函数
- StrEnum 严格管控，扩展需代码评审
- 设计文档：`docs/superpowers/specs/2026-04-25-failure-taxonomy-design.md`

### 3. NTL-S5-003: 盘后复盘 Service

- 新增 `src/evaluation/postmortem_service.py`
  - ValidationDecision / LLMValidationResult（confirm/correct/reject）
  - PostmortemResult dataclass（mfe/mae/return_pct 占位，NTL-S5-010 实现）
  - LLMValidator protocol（可 mock）
  - PostmortemService.generate()：自动归因 + LLM 校验混合模式
  - source 追踪：auto / llm_confirmed / llm_corrected / llm_rejected
  - auto_original 在 extra 中保留（correct/reject 时）
- 设计文档：`docs/superpowers/specs/2026-04-25-postmortem-service-design.md`

### 4. TD-003-b: Schema → ORM 转换路径明确

- 新增 `src/models/converters.py`（显式转换层）
- 新增 `src/models/evidence_pack.py`（EvidencePackRecord ORM）
- 存储分类规范：标量字段用 ORM 列，复杂嵌套用 JSONB

### 5. NTL-S5-004: Ranking Service

- 新增 `src/evaluation/ranking_service.py`
  - `RankingEntry` dataclass + `_sort_key`（return_pct 降序 + 赔率 mfe-mae 降序，None last）
  - `RankingService`（add_entry / generate_ranking / update_entry）
  - `RankingRepository`（upsert / update_rank / query_by_date / find_latest）
  - nested/flat 双视图输出
  - `is_latest` 版本淘汰机制
- 新增 `src/models/ranking_entry.py`（RankingEntryRecord ORM）
- unique constraint on (trade_date, strategy_version_id, symbol)
- **41 tests PASS**

### 6. NTL-S5-005: TraderMemory Schema 扩展

- `TraderMemoryType` 新增 3 种类型：postmortem / strategy_adjustment / market_regime_note
- `TraderMemoryItem` 新增字段：idea_id / strategy_version_id / ranking_entry_id / postmortem_data / strategy_adjustment_data / market_regime_data
- `TraderMemorySummary` 新增字段：postmortem_notes / strategy_adjustments / market_regime_notes
- `summarize_context()` 更新支持聚合 new types
- **22 tests PASS**

### 7. 其他

- 重命名冲突测试文件（同目录同名问题解决）

## 验证

- EvidencePack roundtrip 验证通过
- failure_taxonomy：10 tests PASS
- postmortem_service：25 tests PASS
- ranking_service：41 tests PASS
- trader_memory_schemas：6 tests PASS
- trader_memory_service（含新类型聚合）：22 tests PASS
- **总计：66 tests PASS**

## 发现的问题 / 风险

无新增问题。

## 下一步（按优先级，保留 3-6 条）

### 优先完成：NTL-S5-006 前置依赖（Provider topic mapping 基础设施）

> 以下 3 个任务是 NTL-S5-006 的前置依赖，**必须优先完成**才能让检索层真正落地。

1. **Provider topic_mapping 表填充规则** — 实现 canonical mapping 逻辑：
   - 输入：provider + raw_topic_id
   - 输出：canonical_name + metadata
   - 逻辑：miss 时调用 provider topic 查询接口，命中后写入 topic_mapping 表
   - 建议：SQLite 或 PostgreSQL 表 `{provider, raw_topic_id, canonical_name, metadata}`

2. **Provider topic 查询接口** — 各 provider 实现 `get_canonical_topic(provider, raw_topic_id)`：
   - 给定 provider + raw_topic_id，返回 canonical_name + metadata
   - 用于 miss 时自动查询填充
   - 属于 provider 层（Stage 2/Stage 4 扩展）

3. **TraderMemory 写入时 tags 填充** — 盘后评估完成后：
   - 程序自动从 `SignalContext.topic_source_ids` 取 raw topic_id
   - 调用 canonical mapping 逻辑转为 canonical name
   - 填充到 `TraderMemoryItem.tags` + `topic_source` + `raw_topic_ids`
   - 属于 ManagerAgent 盘后逻辑（NTL-S5-009/010 范围）

### NTL-S5-006 主任务

4. `NTL-S5-006` — 扩展 TraderMemory 检索能力（schema + migration + filter + 测试）

### Stage 5 后续任务

5. `NTL-S5-007` — 扩展 review task 结构
6. `NTL-S5-009` — 让 Manager 生成 Evidence Pack（前置 NTL-S5-001 ✅）
7. `NTL-S5-010` — 升级盘后评分口径（MFE/MAE/规则命中）

## 产物

- `src/evaluation/__init__.py`（更新）
- `src/evaluation/evidence_pack.py`（新增）
- `src/evaluation/failure_taxonomy.py`（新增）
- `src/evaluation/postmortem_service.py`（新增）
- `src/models/converters.py`（新增）
- `src/models/evidence_pack.py`（新增）
- `docs/superpowers/specs/2026-04-25-failure-taxonomy-design.md`（新增）
- `docs/superpowers/specs/2026-04-25-postmortem-service-design.md`（新增）
- `docs/TaskList.md`（NTL-S5-001/002/003 标记完成）
