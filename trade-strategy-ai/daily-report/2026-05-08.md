# 2026-05-08 日报

## 今日成果

- 完成 Web 管理后台总体计划与专项任务清单。
- 完成根目录 `data` / `logs` 清理，并把默认相对路径统一锚定到 `trade-strategy-ai` 项目根目录。
- 完成 API 启动钩子与默认路径的收口，避免启动行为继续依赖当前工作目录。
- 将 Web 方案、API 入口和路径策略同步到用户手册与 API 文档。

## 实现内容

- 新增 `docs/web-plan.md`：
  - 明确 Web 管理后台目标、技术栈、架构、页面模块、安全策略、生产部署和验收标准。
- 新增 `docs/Web-TaskList.md`：
  - 按 Stage 拆分 Web 开发任务，覆盖 CLI 服务化、生产级 Job、UI API、前端工程、操作向导、数据查看、权限安全、测试验收和生产部署。
- 新增 `src/common/paths.py`：
  - 提供统一项目根目录和相对路径解析入口。
  - 让 `config/...`、`data/...`、`logs/...` 默认按 `trade-strategy-ai` 项目根目录解析。
- 修正多个默认路径入口：
  - `src/common/config.py`
  - `src/common/logger.py`
  - `src/alerting/logger_.py`
  - `src/market_universe/snapshot_service.py`
  - `src/services/job_service.py`
  - `src/services/dashboard_service.py`
  - `api/routers/backtest_results.py`
  - `api/routers/run.py`
  - `src/alerting/rules/snapshot_rules.py`
- 修正 API 启动钩子：
  - `src/api/app.py` 的 `lifespan()` 改为从项目根目录读取 `config/app.yaml`。
- 同步文档：
  - `docs/UserManual.md`
  - `docs/APIReference.md`
  - 明确 `config/...`、`data/...`、`logs/...` 相对路径默认按 `trade-strategy-ai` 项目根目录解析。

## 验证

- 路径锚定相关回归已通过：
  - `tests/unit/common/test_paths.py`
  - `tests/unit/common/test_config.py`
  - `tests/unit/market_universe/test_snapshot_service.py`
  - `tests/unit/services/test_job_service.py`
  - `tests/unit/services/test_kaipan_dashboard_service.py`
  - `tests/unit/alerting/test_logger.py`
  - `tests/api/routers/test_backtest_results.py`
  - `tests/api/routers/test_snapshots.py`
  - `tests/api/test_api_app_factory.py`
  - `tests/api/test_api_entrypoints.py`
  - 结果：`15 passed`

## 发现的问题 / 风险

- `api/main.py` 与 `src/api/main.py` 仍然并存，虽然当前都导向同一个 app，但后续还需要做一次收尾级 API 入口清理。
- `docs/README.md` 仍处于删除状态，不属于本次变更，需要后续确认。
- 只读扫描仍有必要，确保仓库里没有遗漏的 `Path("data/...")` 或 `Path("logs/...")` 默认入口。

## 下一步（按优先级，保留 3-6 条）

0. `kaipan_scheduler2.py`和`kaipan_scheduler.py` 应该保留哪一个更符合设计和现有代码实现。
1. `WEB-S2-004`：统一任务日志与产物目录，收口 `data/jobs/{job_id}/` 规范。
2. 只读全仓扫描：专门查找是否还有残留的 `Path("data/...")` 或 `Path("logs/...")` 需要继续收敛。
3. Review Stage 2 的实现和文档，确认 Job Center、Worker、白名单和 API 入口描述与代码一致。
4. `WEB-S3-006`：继续收敛两套 API 使用方式，按 canonical entrypoint 清理兼容层并同步文档。

## 产物

- `docs/web-plan.md`
- `docs/Web-TaskList.md`
- `src/common/paths.py`
- `src/api/app.py`
- `docs/UserManual.md`
- `docs/APIReference.md`
