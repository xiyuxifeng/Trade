# 2026-05-09 日报

## 今日成果

- 完成 Stage 3 收尾，`WEB-S3-004`、`WEB-S3-005`、`WEB-S3-007` 都已落地并通过回归。
- 完成 UI BFF 版本化入口统一，当前 Web 侧正式依赖 `/api/ui/v1/*`。
- 完成 Stage 2 / Stage 3 相关文档对齐，避免 `src/api/*`、旧 UI 前缀和已废弃描述继续污染后续开发。
- 补齐并整理 Web UserManual 覆盖矩阵，把“已实现”和“目标设计”拆成两层视图。

## 实现内容

- 新增 Artifact 中心：
  - `src/services/artifact_service.py`
  - `api/routers/ui/artifacts.py`
  - 支持主要产物统一查询、预览、下载。
  - 默认索引覆盖 `data/jobs`、`data/processed`、`data/backups`、`data/market_universe/snapshots`、`data/kaipan` 等常见产物目录。
  - 明确排除 `config/backups`，避免配置备份中的敏感内容被当成普通产物暴露。
- 新增 Market UI API：
  - `api/routers/ui/market.py`
  - `MarketService` 增加 `list_symbols()` 和 `get_ohlcv()`。
  - 支持按 symbol 和日期区间返回 K 线数据。
- 完成 UI versioning 收口：
  - `GET /api/ui/v1/system/status`
  - `GET/POST /api/ui/v1/jobs*`
  - `GET/POST /api/ui/v1/workflows*`
  - `GET /api/ui/v1/artifacts*`
  - `GET /api/ui/v1/market*`
  - `GET /api/ui/system/status` 作为兼容别名保留。
- 修复 service / router 的耦合问题：
  - `WorkflowService` 不再反向依赖 API router 的依赖注入函数。
  - `JobService` 增加了可复用的 service factory。
- 补齐和更新文档：
  - `docs/APIReference.md`
  - `docs/Web-TaskList.md`
  - `docs/web-plan.md`
  - `docs/Web-UserManual-Coverage.md`
- 重新整理覆盖矩阵：
  - 增加“当前已实现”区块，明确 Stage 3 已上线入口。
  - 将目标设计留在下方，避免把规划项误认为已完成。

## 验证

- 通过回归：
  - `pytest /Users/wanghui/Documents/Claude/trade-strategy-ai/tests/api /Users/wanghui/Documents/Claude/trade-strategy-ai/tests/unit/services`
- 结果：
  - `72 passed`

## 发现的问题 / 风险

- `ArtifactService` 仍然是扫描式发现产物，当前规模可接受，但后续如果产物数量继续增长，可能需要索引缓存或增量刷新。
- 覆盖矩阵里仍保留大量 Stage 4 以后规划项，这些项是目标设计，不是当前可用实现，后续接前端时要继续按两层视图理解。

## 下一步（按优先级，保留 3-6 条）

1. `WEB-S4-001`：初始化前端工程。
2. `WEB-S4-002`：接入 Tailwind CSS 和 shadcn/ui。
3. `WEB-S4-003`：实现前端路由和 API client。
4. `WEB-S4-004`：实现基础布局和完整导航骨架。
5. `WEB-S4-005`：让总览页接入系统状态、最近任务和最近产物。
6. `WEB-S4-006`：让任务中心、工作流、产物中心和市场数据页具备可用的首批功能。

## 产物

- `src/services/artifact_service.py`
- `src/services/market_service.py`
- `src/services/workflow_service.py`
- `api/app.py`
- `api/routers/ui/artifacts.py`
- `api/routers/ui/market.py`
- `api/routers/ui/system.py`
- `api/routers/ui/workflows.py`
- `docs/APIReference.md`
- `docs/Web-TaskList.md`
- `docs/Web-UserManual-Coverage.md`
- `docs/web-plan.md`

## 补充成果

- 完成 Stage 4 前端 design / baseline 的一致性 review。
- 将原先只存在于实施计划中的 `WEB-S4-005`、`WEB-S4-006`、`WEB-S4-007` 正式补入 `docs/Web-TaskList.md` 的 Stage 4 任务清单。
- 将 Stage 4 推荐完成顺序同步写入入口文档，避免后续接手时只看到计划、看不到可执行任务。
- 当前文档链路已稳定对齐，下一步直接从 `WEB-S4-001` 初始化前端工程开始。
