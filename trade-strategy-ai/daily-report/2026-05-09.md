# 2026-05-09 日报

## 今日成果
- 完成 `WEB-S6-008`，把数据分析、数据导入、运维与监控三组工作台全部落地并通过验收。
- 新增 `signals`、`persona`、`market-state`、`imports`、`kaipan`、`data-health` 的前后端链路、页面入口、路由和测试。
- 补充修复 `React Router` 测试环境 future flag warning，并把相关路由/测试封装统一到 `RouterProvider future={{ v7_startTransition: true }}`。
- 收紧 `signals`、`market-state`、`data-health` 三个页面的测试 selector，避免摘要文本与 JSON 预览重复命中。
- 继续为 `jobs`、`strategy-studio` 的详情面板和 JSON 面板补稳定锚点，减少未来纯文本断言的重复命中风险。
- 继续为 `backtests` 的详情摘要、回测报告和规则验真面板补稳定锚点，避免后续 tab 切换后纯文本断言失效。
- 完成本次任务状态同步，`WEB-S6-008` 已在 `Web-TaskList` 中标记为完成。
- 补齐并标记完成 `WEB-S6-009` 安全预览设计与实现收口，同时将 `WEB-S6-001` 的追踪计划子项同步完成。
- 回看 Stage 6 的实现质量并修正两个收尾问题：回测报告安全预览改为共享组件复用，产物中心补上 `tar.gz` 筛选项。
- 最终复核 Stage 6 交付物，确认回测中心已补齐 `预览 / 原文` 切换与下载原文入口，整体体验和安全预览策略一致。
- 继续推进 Web Stage 7，完成 `WEB-S7-001` 的 API Key 基线收口并通过验证。

## 实现内容
- 新增后端 UI BFF 路由：
  - `api/routers/ui/signals.py`
  - `api/routers/ui/persona.py`
  - `api/routers/ui/imports.py`
  - `api/routers/ui/kaipan.py`
  - `api/routers/ui/data_health.py`
- 新增前端工作台：
  - `web/src/features/signals/`
  - `web/src/features/persona/`
  - `web/src/features/market-state/`
  - `web/src/features/imports/`
  - `web/src/features/kaipan/`
  - `web/src/features/data-health/`
- 补齐 API 封装、类型定义、页面入口、路由和导航。
- 为 `RouterProvider` 统一补上 `v7_startTransition` future flag，去掉测试环境 warning。
- 修正页面测试中的不稳定断言，并为重复文案补充 `data-testid` 锚点，避免同一文案在多个节点重复命中。
- 为 `jobs`、`strategy-studio` 的参数、日志、版本 JSON 和规则 JSON 补充锚点。
- 为 `backtests` 的详情摘要、回测报告原文、规则验真原文和详情 JSON 补充锚点。
- 补入 `python-multipart` 依赖，支持 multipart 上传链路。
- 新增共享安全预览层 `web/src/components/artifacts/artifact-preview.tsx`，`reports`、`artifacts`、`backtests` 统一复用安全预览策略。
- 后端 `ArtifactService` 增加下载路径允许目录校验，下载接口拒绝目录外路径。
- `Jobs` 页面产物引用和 `Artifacts` 页面 `jobId` 串联链路保持可追踪。
- `WEB-S7-001` 完成后端鉴权收口：`api/dependencies.py` 统一改为严格 API Key 基线，`enabled=true` 时必须命中配置的 key，空 key 列表不再匿名放行。
- `WEB-S7-001` 完成前端鉴权收口：`web/src/lib/api/http.ts` 提供统一 header helper，`imports`、`alerts`、`backtests`、`artifacts`、`reports` 统一复用。
- `WEB-S7-001` 补齐后端 unit 与 UI 路由回归测试，以及前端 API client 测试。

## 验证
- 后端路由测试：`11 passed`
- 前端相关测试：`11 files, 15 tests passed`
- `pnpm typecheck` 通过
- `pnpm lint` 通过
- 本次补丁补充验证：`pnpm test -- src/pages/signals/index.test.tsx src/pages/market-state/index.test.tsx src/pages/data-health/index.test.tsx` 通过，且 stderr 不再输出 React Router future flag warning。
- 本次补丁补充验证：`pnpm test -- src/pages/jobs/index.test.tsx src/pages/strategy-studio/index.test.tsx` 通过。
- 本次补丁补充验证：`pnpm test -- src/pages/backtests/index.test.tsx` 通过。
- 本次补丁类型检查：`pnpm typecheck` 通过。
- Stage 6 相关定向验证已通过，包含 Jobs / Artifacts / Reports / Backtests / 预览组件测试与后端下载校验。
- 最终复核命令通过：`pnpm test -- src/pages/backtests/index.test.tsx src/features/reports/report-center.test.tsx src/pages/artifacts/index.test.tsx src/components/artifacts/artifact-preview.test.tsx`、`pnpm typecheck`、`pnpm exec eslint . --ext .ts,.tsx --no-cache`。
- `WEB-S7-001` 补充验证：`pytest tests/unit/test_dependencies.py tests/api/routers/ui/test_ui_auth_baseline.py -v`，`8 passed`。
- `WEB-S7-001` 补充验证：`pnpm test -- src/lib/api/http.test.ts src/lib/api/imports.test.ts src/lib/api/alerts.test.ts src/lib/api/backtests.test.ts src/lib/api/artifacts.test.ts`，`5 files, 9 tests passed`。
- `WEB-S7-001` 补充验证：`pnpm typecheck`、`pnpm lint` 通过。

## 发现的问题 / 风险
- `React Router` warning 和重复 selector 问题已收口。
- 当前 Stage 6 的 Web 交付物已完成复核，没有新增阻塞项。
- 本机默认 Node 版本较旧，前端测试需要显式切到 Node 18。

## 下一步（按优先级，保留 3-6 条）
1. 继续 Web Stage 7，优先推进 `WEB-S7-002` 配置脱敏与敏感字段保护。
2. 若需要继续策略工作台扩展，再推进 `WEB-S10-003`。
3. 如需回到主链路，再处理 `NTL-S10-007`。
4. 后续新增页面时继续保持 UI BFF、页面、测试同步落地。

## 产物
- `api/routers/ui/signals.py`
- `api/routers/ui/persona.py`
- `api/routers/ui/imports.py`
- `api/routers/ui/kaipan.py`
- `api/routers/ui/data_health.py`
- `web/src/features/signals/`
- `web/src/features/persona/`
- `web/src/features/market-state/`
- `web/src/features/imports/`
- `web/src/features/kaipan/`
- `web/src/features/data-health/`
- `web/src/lib/api/imports.ts`
- `web/src/features/kaipan/kaipan-center.tsx`
