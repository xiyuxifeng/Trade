# 2026-05-15 日报

## 今日成果
- 完成 `NW-V1-S2-002` 的方案收窄，确认 Step Timeline 仍以单一 canonical contract 为准。
- 明确 Step Timeline 的实现路径：先基于现有 Job audit events 做结构化归一化，再逐步收口到后续执行层。
- 确认 `NW-V1-S2-002` 与 `NW-V1-S2-003` 可分步推进，默认顺序仍是先前者、后者后置。
- 完成 `NW-V1-S2-002` 的代码落地与验收，Job Detail API 现已返回结构化 Step Timeline。
- 完成 `NW-V1-S2-003` 的代码落地与验收，Workflow 运行入口现已委托 `WorkflowRunner`，支持顺序执行、失败停止和 step 结果汇总。
- 补充 `NW-V2-S2-004` 下的后续 `Workflow Run 持久化 MVP` 子任务边界，明确表结构、字段和验收标准。
- 完成 `NW-V1-S3-002` 与 `NW-V1-S3-003` 的代码落地与验收，`article_pipeline` 已收口为专用 `PipelineApplicationService` 的 canonical 入口，legacy `pipeline` 仅保留兼容层语义。

## 实现内容
- 梳理了 `NW-V1-S2-002` 的边界：
  - 不在 UI 中伪造 step 状态
  - 不把 timeline 只写成日志文本
  - 不要求所有内部 pipeline step 一次性迁移
- 明确了 Step Timeline 的稳定字段边界：
  - `step_id`
  - `step_name`
  - `title`
  - `status`
  - `started_at`
  - `finished_at`
  - `duration_ms`
  - `error`
  - `artifact_refs`
  - `order`
- 新增 `src/models/step_timeline.py` 与 `src/services/step_timeline_service.py`，把 Job audit events 归一化为结构化 timeline contract。
- `JobService` 新增 `get_job_timeline`，`/api/ui/v1/jobs/{job_id}/timeline` 改为返回结构化 Step Timeline。
- 新增 `src/services/workflow_runner.py`，以薄编排器方式串起 Workflow 的 step 执行与 Job 复用。
- 调整 `src/services/workflow_service.py`，将 Workflow 运行委托给 `WorkflowRunner`，保持服务层只负责定义、校验和入口调度。
- 在 `docs/New-Web-Linked-TaskLists/New-Web-TaskList.md` 中补充 `NW-V2-S2-004` 的 `Workflow Run 持久化 MVP` 后续子任务。
- 新增 `src/services/pipeline_application_service.py`，把 article pipeline 作为独立业务入口接到现有 `WorkflowRunner` / `JobRunner` / `PipelineService` 分层上。
- 调整 `api/routers/ui/pipelines.py`，让 `/api/ui/v1/pipelines` 不再依赖 `WorkflowService` 的 legacy `pipeline` workflow。
- 在 `docs/New-Web-Linked-TaskLists/New-Web-TaskList.md` 中把 `NW-V1-S3-002` / `NW-V1-S3-003` 标记完成，并明确 `pipeline` 仅作为兼容层。

## 验证
- 已执行并通过：
  - `pytest tests/unit/services/test_step_timeline_service.py -v`
  - `pytest tests/api/routers/test_jobs_api.py -v`
  - `pytest tests/unit/services/test_job_service.py -v`
  - `pytest tests/unit/models/test_models.py -v`
  - `pytest tests/api/test_ui_openapi_contract.py -v`
- 已执行并通过：
  - `pytest tests/unit/services/test_workflow_runner.py -v`
  - `pytest tests/unit/services/test_workflow_service.py -v`
  - `pytest tests/api/routers/test_workflows.py -v`
  - `pytest tests/api/routers/test_pipelines.py -v`
  - `pytest tests/api/test_ui_openapi_contract.py -v`
  - `pytest tests/pipelines/test_article_pipeline_spec.py -v`
- 已执行并通过：
  - `pytest tests/unit/services/test_pipeline_application_service.py -v`
  - `pytest tests/api/routers/test_pipelines.py -v`
  - `pytest tests/unit/services/test_workflow_service.py -v`
  - `pytest tests/api/routers/test_workflows.py -v`
  - `pytest tests/api/test_ui_openapi_contract.py -v`
  - `pytest tests/pipelines/test_article_pipeline_spec.py -v`

## 发现的问题 / 风险
- Timeline contract 目前只覆盖 Job 级过程展示，不等于完整 workflow trace。
- 后续若要做数据库级 workflow 历史，需要引入独立事实源，不能继续依赖 audit events 拼装。
- `article_pipeline` 已收口为 canonical Web/API 入口，但 `pipeline` 作为 legacy workflow 还在通用 workflow catalog 中存在，后续若正式退役需再拆一轮兼容边界。

## 下一步
1. 准备 `NW-V2-S2-004` 下的 `Workflow Run 持久化 MVP` 后续任务。
2. 保持 UI 继续消费当前 canonical Job Timeline contract。
3. 后续扩展 Workflow 时优先复用现有 `WorkflowRunner` 分层。
4. 下一阶段优先看 `NW-V1-S4-001` 的 V1 E2E 回归。

## 产物
- `trade-strategy-ai/daily-sessions/2026-05-15.md`
- `trade-strategy-ai/daily-report/2026-05-15.md`

## 风险与注意事项
- 不要把 Step Timeline 变成临时兼容层。
- 不要让前端继续推断后端未暴露的事实。
- 不要把尚未验收的任务写成已完成。
