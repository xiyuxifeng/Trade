# 2026-05-17 日报

## 今日成果
- 完成了 `UI-V2-007 Artifact Center`，把 `/artifacts` 从工具型页面升级为正式 Web 工作台入口。
- 保持产物检索在 UI BFF / ArtifactService 契约内，不直接暴露服务器绝对路径，也不把 artifact 简化成只能下载的文件。
- 额外修复了详情错误态和下载错误态，确保 `404 / 403 / network` 失败对用户可见。

## 实现内容
- 扩展了 Artifact 查询 contract，支持 `job_type` 与 `date` 筛选。
- 将 `/artifacts` 页面升级为浅色正式工作台风格，支持 kind / job_type / date / 关键字 / job_id 过滤、预览、下载和来源 Job 跳转。
- 给 `ArtifactCard`、`ArtifactPanel` 和 `ArtifactPreview` 做了正式工作台收口，补齐来源 Job 和浅色预览体验。
- 补充测试，覆盖 API contract、服务筛选、页面交互和组件跳转。
- 修正 Artifact Center 的错误态呈现，避免详情失败和下载失败被静默吞掉。

## 验证
- `python -m pytest tests/unit/services/test_artifact_service.py tests/api/routers/test_artifacts.py tests/api/test_ui_openapi_contract.py -q`
- `6 passed`
- `cd trade-strategy-ai/web && pnpm vitest run src/pages/artifacts/index.test.tsx src/components/artifacts/artifact-panel.test.tsx src/features/strategy-workspace/strategy-workspace-artifacts.test.tsx`
- `6 passed`
- `git diff --check` 通过
- `pnpm typecheck` 仍被仓库现有 `ignoreDeprecations` 配置和历史类型错误阻塞，无法作为本次交付的最终验收依据。
- 回查后确认，`UI-V2-007` 的任务目标和验收标准已闭合。

## 发现的问题 / 风险
- 浏览器实机验收未完成，原因是本机 Node 版本过低，`pnpm dev` 无法启动。
- 仓库中仍有若干既有类型错误，和本次 Artifact Center 改动无关，但会影响全量 typecheck 的整体结论。

## 下一步
1. 推进 `UI-V2-008`。
2. 继续保持 V2 的正式 Web 工作台定位，不扩张 CLI surface。

## 产物
- `trade-strategy-ai/daily-sessions/2026-05-17.md`
- `trade-strategy-ai/docs/New-Web-Linked-TaskLists/New-Web-UI-TaskList.md`
