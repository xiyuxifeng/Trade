# 2026-05-18 日报

## 今日成果
- 完成 `Market Regime` 正式定义文档 `New-Web-Market-Regime-Definition.md`，补齐字段定义、规则设计、数据分层和执行顺序。
- 落地 `NW-V3-SX-001 P0 Market Regime Definition` 的代码闭环，包括 schema、repository、service、API、Web viewer 和测试。
- 落地 `UI-V3-010 P0 Market Regime Viewer`，继续保持浅色正式工作台风格。
- 补齐 backtest 对 `market_regime_version` 的读取链，回测上下文可以按 `snapshot_id + regime_version` 读取 regime。
- 将 `NW-V3-SX-001` 与 `UI-V3-010` 同步标记为完成。
- 预置常用指数到 `stock_info`，OHLCV 抓取会顺带落库指数日线，回测页新增 benchmark 下拉框并显示名称 + 代码。
- 修复 benchmark options 的 `limit` 截断逻辑，并在回测加载阶段对缺失 benchmark OHLCV 做显式失败。
- 完成 Kaipan 10.5 接口扩展：`MarketStockZDNum`、`ZhangTingExpression`、`DailyLimitIndex`、`WeightPerformance`、`GetFengKList` 已接入 provider / normalizer / service / snapshot / DB 链路。
- 在现有 `/kaipan` 页面增加了 10.5 ingestion 提示块，保持浅色正式工作台风格，不新增独立页面。

## 补充
- 移除了 `persona` 配置中的 benchmark 默认值，回测、MarketState、Snapshot 和对应的市场工作台入口都改为显式选择 `benchmark_symbol`。
- 同步收紧了 job schema 与服务签名，避免再从配置层隐式继承基准指数。
- 相关 Python 与 Web 测试已重新验证通过。
- 为 `10.5` 补充了落表与计算分层设计文档，并在 regime 定义文档中挂接了链接。
- 已将 `NW-V2-S2-007 Kaipan 10.5 ingestion expansion` 标记为完成，并同步到 daily session / TaskList。
- review 阶段修正了 Kaipan 10.5 请求参数错位：`GetFengKList` 按文档补齐 `Day / Time / Index / Order / st`，`DailyLimitIndex` 今日请求移除多余 `Day`，并同步更新了相关测试与 schema 注释。
- review 阶段继续收口 `GetFengKListBest`：`strong_fengkou_best` 作为快照 section / KaipanService 成功标识 / UI 提示的统一语义，同时修复了 `KaipanService._fetchers_for_slot()` 的 17-30 分支缩进错误。
- 继续把 `New-Web-Market-Regime-Definition.md` 的 `10.2` 状态表更新到最新实现：`breadth_up_ratio` / `breadth_down_ratio` / `benchmark_ohlcv_window` / `ret_5d` / `ret_20d` / `ma20_gap` / `ma60_gap` / `vol_spike` / `turnover_ratio` / `theme_concentration` 已进入计算层落地状态，`GetFengKListBest` 统一为 canonical 强势风口入口。

## 验证
- `pytest tests/unit/backtest/test_snapshot_loader.py tests/unit/backtest/test_engine_regime_context.py tests/unit/services/test_backtest_service.py tests/unit/services/test_market_regime_service.py -v`
- 结果：`16 passed`
- `./node_modules/.bin/vitest run src/features/market-browser/market-snapshot-browser-regime.test.tsx src/pages/market/index.test.tsx`
- 结果：`2 passed`
- `pytest tests/unit/backtest/test_snapshot_loader.py tests/unit/backtest/test_engine_regime_context.py tests/unit/services/test_backtest_service.py tests/unit/services/test_market_regime_service.py -q`
- 结果：`16 passed`
- `rtk python -m pytest tests/unit/backtest/test_schemas.py tests/unit/backtest/test_engine_regime_context.py tests/unit/services/test_backtest_service.py tests/unit/services/test_job_runner.py -vv`
- 结果：`31 passed`
- `./node_modules/.bin/vitest run src/lib/api/backtests.test.ts src/lib/api/market.test.ts src/pages/backtest/index.test.tsx src/pages/backtests/index.test.tsx`
- 结果：`8 passed`
- `rtk python -m pytest tests/api/routers/test_market_ui.py tests/unit/backtest/test_snapshot_loader.py -vv`
- 结果：`16 passed`
- `rtk python -m pytest tests/unit/services/test_backtest_service.py -vv`
- 结果：`3 passed`
- `python -m pytest tests/providers/test_kaipan_pipeline.py tests/unit/providers/test_kaipan_provider.py tests/unit/providers/test_kaipan_normalizer.py tests/unit/services/test_kaipan_dashboard_service.py tests/unit/services/test_market_snapshot_builders.py tests/unit/services/test_market_snapshot_service.py tests/unit/services/test_market_data_storage_service.py -q`
- 结果：`48 passed`
- `pnpm --dir web test -- src/pages/kaipan/index.test.tsx src/lib/api/kaipan.test.ts`
- 结果：`4 passed`
- `python -m pytest tests/unit/providers/test_kaipan_provider.py tests/unit/services/test_market_snapshot_builders.py tests/unit/services/test_market_snapshot_service.py tests/unit/services/test_kaipan_dashboard_service.py tests/unit/services/test_market_regime_feature_service.py tests/unit/services/test_market_regime_service.py -q`
- 结果：`30 passed`

## 结论
- 当前 regime 契约已进入可继续推进状态。
- 下一阶段按文档顺序先补 P0 数据，再进入 `NW-V3-SX-002 Regime-aware Backtest`。
