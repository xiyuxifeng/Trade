# 2026-05-31 日报

## 今日成果
- 完成了 Web 兜底 `default` Profile 的显式提示与导入引导，避免用户误把 fallback 当正式配置。
- 系统状态页、Dashboard 总览页、Profile 导入页的文案口径已经统一。
- Web 用户手册与部署文档同步加入了 `default` 仅用于兜底启动的说明。

## 实现内容
- 新增共享提示组件 `ProfileBootstrapWarning`，仅在 `default` 且没有 snapshot 时展示。
- 系统状态页和 Dashboard 总览页加入“去导入正式配置”的 CTA。
- Profile 导入页增加首次部署引导，明确推荐先导入 `config/app.template.yaml`。
- 补齐对应前端测试。

## 验证
- 新增/相关前端测试通过。
- `pnpm typecheck` 通过。
- `git diff --check` 通过。

## 风险 / 说明
- `default` 仍然保留为系统自愈兜底 Profile，目的是保证 Web 可启动。
- 现在已经通过 UI 和文档明确标注其非正式性，减少误用风险。

## 下一步
1. 如有需要，继续检查其他 Profile 入口是否也要补同样的 fallback 提示。
2. 若用户希望，可进一步把 `default` 兜底状态在更多业务页做统一横幅提示。

