# 2026-04-11 daily session

## 一句话目标
完成 P1-032/033 API接口 + 修复增量爬取 + 备份恢复功能

## 关键决策
- 增量爬取策略：改为"只要 URL 已被抓取过就直接跳过"，不依赖 `last_seen_article_url` 作为停止条件
- 备份恢复：数据库表 + `data/processed/` 目录完整备份，支持跨设备迁移
- 超时配置：API 操作超时通过配置文件 `timeout_seconds` 参数可调整

## 今日完成
| 任务 | 类型 |
|------|------|
| P1-032/033 API手动触发和报告查询接口 | 代码 |
| Code Review Critical 1,2 + Important 5,6,7,8 修复 | 修复 |
| 增量爬取逻辑修复 | 修复 |
| 备份/恢复功能实现 | 代码 |
| trade_logs 表结构修复 | 修复 |

### API 接口实现 (P1-032/033)
- `api/routers/run.py`: 手动触发端点 `/run/backtest`, `/run/extract`, `/run/analyze`
- `api/routers/reports.py`: 报告查询端点 `/reports`（分页 + 日期过滤）
- 线程安全：双检锁模式防止 FastAPI 启动时重复初始化
- 可配置超时：`timeout_seconds` 从配置文件读取

### Code Review 修复
- Critical 1: FastAPI 启动时 singleton 重复初始化问题
- Critical 2: CORS 配置改为具体域名（非 wildcard）
- Important 5,6,7,8: 超时参数改为可配置（CLI 或 config 可调）

### 增量爬取逻辑修复
- 问题：已抓取的文章被重复抓取
- 原因：`last_seen_article_url` 作为停止条件导致问题
- 修复：改为 `if item["source_url"] in seen_urls: continue`（先检查再抓取）
- 同时更新 `state.json` 和 `crawl_state` 表

### 备份恢复功能
- `src/backup/service.py`: 实现 `backup_project_state()` 和 `restore_project_state()`
- `cli/main.py`: 新增 `backup-data` 和 `restore-data` 命令
- 支持：数据库全量表 + `data/processed/` 目录
- 跨设备迁移：通过 `migrate-crawl-state` 命令同步增量状态

### 数据库表结构修复
- `market_data` 表：手动添加缺失列（source, market, timeframe, adj_factor, is_adjusted, indicators, raw_payload）
- `trade_logs` 表：手动添加缺失列（source, market, position_side, order_type, currency, strategy_tag, rationale）
- 添加 migration 文件 `2026-04-11_0002`

## 验证结果
- 备份命令执行成功：`data/backups/20260411-080156`
- Git 已推送：commit 7bcb2e7

## 发现的问题 / 风险
- Alembic 迁移存在问题，部分表缺失列需要手动执行 SQL 添加
- `trade_logs` 表结构与 ORM 模型不一致，backup 时报错

## 下一步
1. 测试 restore-data 命令验证恢复功能
2. 在另一个设备上测试跨设备迁移流程
3. 继续其他 TaskList 任务

## 产物
- `api/routers/run.py` - 手动触发端点
- `api/routers/reports.py` - 报告查询端点
- `src/backup/service.py` - 备份恢复服务
- `cli/main.py` - backup/restore/migrate-crawl-state 命令
- `docs/crawl.md` - 更新增量爬取文档

## 风险与注意事项
- 备份/恢复功能已验证 backup 成功，restore 未测试
- 数据库表结构问题已修复，但 Alembic 迁移链可能需要整理
