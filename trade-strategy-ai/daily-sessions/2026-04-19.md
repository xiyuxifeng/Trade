# 2026-04-19 daily session

## 一句话目标
收敛项目主线与执行入口，明确后续从“文章 LLM 提取已完成”推进到“市场快照 + 新链路底座”的下一步任务。

## 关键决策
- 不再使用 `New-TaskList.md` 作为主清单，统一改为 `docs/TaskList.md`。
- `Project.md` 保持“项目结构说明”定位，不再承担目标总览或路线图职责。
- 主线正式收敛为：`数据快照 -> provider -> 市场候选池 -> per-trader 策略版本 -> 盘前决策 -> 盘后评估/归因 -> 回测验真 -> 自主优化`。
- 当前已完成的“文章 LLM 提取”视为知识输入基础，下一步不继续深挖抽取，而是先补市场快照资产和新链路基础结构。

## 今日完成
- 重构并细化主任务清单，将 `docs/New-TaskList.md` 改造成可脱离上下文执行的交付型清单。
- 将主清单重命名为 `docs/TaskList.md`，并同步修正文档中的引用。
- 将 `AGENTS.md` 中 `daily-sessions / daily-report` 的更新约定补充到“存储约定”部分，固定收尾写法。
- 整理文档入口，明确当前有效入口为：
  - `docs/Project.md`
  - `docs/Plan.md`
  - `docs/需求.md`
  - `docs/TaskList.md`
  - `docs/Kaipan-Interface-Mapping.md`
- 明确 Agent 边界：
  - 长期保留：`ManagerAgent`、`DataAgent`、`TraderAgent`、`StrategyAgent`、`RiskAgent`
  - 降级为 module/service：`KnowledgeAgent`、`BehaviorAgent`、`BacktestAgent`
  - 冻结：`AlignmentAgent`
- 明确“文章 LLM 提取之后”的下一步主线：
  - 先做 `Stage 0` 的 `kaipan` 数据资产化
  - 再做 `Stage 1` 的配置、契约、模型与 migration
  - 再进入 `Stage 1.5` 和 `Stage 2`
- 会话收尾已执行：
  - `brew services stop postgresql@15`
  - 清理 `config/app.yaml` 中的 `cookie` 与 `api_keys`
  - 同步今日 `daily-session` 与 `daily-report`

## 验证结果
- `docs/TaskList.md` 已成为唯一主清单，仓库内已无 `New-TaskList.md` 引用。
- `docs/Project.md` 已恢复为结构说明文档，并与当前目录状态对齐。
- `docs/TaskList.md` 已补齐：
  - 无上下文接手规则
  - 任务状态与完成规则
  - 最终可交付标准
  - Stage 0 到 Stage 7 的细粒度子任务
- `postgresql@15` 已成功停止。
- `config/app.yaml` 中 `crawl.auth.tgb.cn.cookie` 已清空为 `null`。
- `config/app.yaml` 中 `api.auth.api_keys` 已清空为 `[]`。

## 发现的问题 / 风险
- 当前项目虽然完成了文章 LLM 提取，但仍缺市场快照资产，无法直接进入盘前新主链路。
- `DataAgent` 仍然只支持 `last_price`，短期内不能直接消费新快照字段。
- `TaskList` 已经足够可执行，但真正开始编码时仍应优先锁定首批 `kaipan` 接口名单和快照字段。
- 本次会话没有新增完成 `Stage 0` 编码任务，`TaskList` 勾选状态未新增变化。

## 下一步（明日执行清单）
1. 执行 `NTL-S0-007`
   定义 `kaipan` 的 `raw / normalized / snapshots` 目录规范与命名规则。
2. 执行 `NTL-S0-008`
   确定首批 8 个高价值接口，并实现原始 JSON 保存。
3. 执行 `NTL-S0-009`
   为 `hot_topics / topic_constituents / strong_symbols` 输出标准化快照 JSON。
4. 执行 `NTL-S0-010`
   为每次抓取补齐请求参数、抓取时间、接口版本、鉴权来源等元信息。
5. 如果前三项进展顺利，再执行 `NTL-S0-014`
   为 `kaipan` 抓取器补最小离线验证或样例断言。

## 产物
- `docs/TaskList.md`
- `docs/Project.md`
- `docs/Plan.md`
- `docs/需求.md`
- `docs/Kaipan-Interface-Mapping.md`
- `src/providers/kaipan_provider.py`
- `src/providers/__init__.py`

## 风险与注意事项
- 明天开始编码时，不要直接进入 `Stage 1` 或 `Stage 2`，先把 `Stage 0` 的数据资产层做出来。
- 不要继续扩展旧 `watchlist + last_price` 主路径。
- `kaipan` 是私有接口，优先保留原始 JSON 和元信息，避免只存清洗后的字段。
- 以后每次收尾更新：
  - `daily-sessions` 要明确写出下一次从哪个 `TaskList` 任务继续
  - `daily-report` 只写真正完成的交付，不把“推进中”写成“已完成”
