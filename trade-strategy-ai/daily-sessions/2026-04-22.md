# 2026-04-22 daily session

## 一句话目标
完成 `NTL-S0-011` 的最终验证，并把历史/今日 URL 返回差异归一化到解析层。

## 关键决策
- 批量验证继续沿用 `scripts/kaipan_batch_fetch.py`，不提前接 CLI
- 响应差异归一化放在 `KaipanNormalizer`，避免上层按 URL 分支处理
- 只处理已确认的差异，不做泛化猜测

## 今日完成
- 完成最近 30 个交易日、13 个接口的批量抓取与样本验证
- 完成 4 类差异字段归一化：
  - `RealRankingInfo` 的 `MinDay`
  - `GetFengKListBest` 的 `Tip` / `Tips`
  - `GetInterviewsByDateStock` 的 `Count`
  - `GetZhangTingTianTi` 的 `Date` / `date`
- 更新 `docs/kaipan.md` 说明这些差异和统一策略
- 补充 normalizer 单测并验证通过

## 验证结果
- 批量抓取结果：`455` 成功，`0` 失败
- Provider 测试：`22 passed`
- 运行产物已落盘：
  - `data/processed/kaipan/ntl-s0-011/20260422-231110/run.log`
  - `data/processed/kaipan/ntl-s0-011/20260422-231110/records.jsonl`
  - `data/processed/kaipan/ntl-s0-011/20260422-231110/summary.json`

## 发现的问题 / 风险
- 目前确认的差异只有 4 类，后续新增接口仍需继续做样本验证
- 文档更新后要保持和 normalizer 规则同步，避免说明与实现漂移

## 下一步（3-5 条，只保留当前会话要继续做的事情）
1. 从 `NTL-S1-001` 开始推进 Stage 1
2. 如有新增 Kaipan 接口或新样本差异，再补充 normalizer 规则
3. 保持批量抓取脚本作为回归验证工具

## 产物
- `scripts/kaipan_batch_fetch.py`
- `src/providers/kaipan_normalizer.py`
- `docs/kaipan.md`
- `tests/providers/test_kaipan_pipeline.py`

## 风险与注意事项
- 只有已验证过的字段差异才进入归一化规则
- 后续新增差异要同步更新文档、测试和归一化逻辑
- 会话收尾时已将 `config/app.yaml` 中的 `kaipan.token` / `kaipan.user_id` 清空
- `brew services stop postgresql@15` 返回结果为服务未启动
