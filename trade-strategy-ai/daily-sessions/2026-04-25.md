# 2026-04-25 daily session

## 一句话目标

完成 NTL-S5-001（Evidence Pack 结构）和 TD-003-b（Schema/ORM 转换路径明确）。

## 关键决策

- **EvidencePack 聚合内容**：trade_idea + signal_context + market_data + strategy_version_snapshot，统一供盘后评估和 ranking 使用
- **Schema/ORM 分离原则**：dataclass 约束内存结构，ORM 模型负责持久化，converters.py 提供显式转换层
- **JSONB 存储策略**：复杂嵌套对象（PriceSpec/PositionSize/SignalContext/EvidencePack）存入 JSONB 列，标量字段用 ORM 直列
- **失败归因分类**：多维度标签体系（根因必选 + 交易阶段可选 + 规则类型可选），StrEnum 严格管控
- **LLM 校验定位**：LLM 作为安全层（confirm/correct/reject），不是主驱动；auto_original 保留用于分析
- **复盘 Service 架构**：自动归因为主 + LLM 校验可选，通过 LLMValidator protocol 解耦

## 今日完成

### NTL-S5-001: Evidence Pack 结构
- 新增 `src/evaluation/evidence_pack.py`（EvidencePack dataclass）
  - 属性：pack_id / idea_id / trade_idea / signal_context / market_data / strategy_version_id / strategy_version_snapshot
  - 方法：from_trade_idea() / to_dict() / from_dict()
- 新增 `src/evaluation/__init__.py`（模块入口）

### TD-003-b: Schema → ORM 转换路径明确
- 新增 `src/models/converters.py`（显式转换层）
- 新增 `src/models/evidence_pack.py`（EvidencePackRecord ORM）

### NTL-S5-002: 失败归因分类
- 新增 `src/evaluation/failure_taxonomy.py`
  - FailureRootCause（9个根因标签）
  - FailureStage（3个阶段标签）
  - FailureRuleType（4个规则类型标签）
  - FailureAttribution dataclass
  - parse_failure_categories() 函数
- 多维度标签体系设计文档：`docs/superpowers/specs/2026-04-25-failure-taxonomy-design.md`

### NTL-S5-003: 盘后复盘 Service
- 新增 `src/evaluation/postmortem_service.py`
  - ValidationDecision / LLMValidationResult
  - PostmortemResult dataclass（mfe/mae/return_pct 占位）
  - LLMValidator protocol（可 mock）
  - PostmortemService.generate() 方法
  - 自动归因 + LLM 校验混合模式
  - source 追踪（auto/llm_confirmed/llm_corrected/llm_rejected）
  - auto_original 在 extra 中保留
- 盘后复盘 service 设计文档：`docs/superpowers/specs/2026-04-25-postmortem-service-design.md`

### 其他
- 重命名冲突测试文件：
  - `tests/unit/market_universe/test_schemas.py` → `test_market_universe_schemas.py`
  - `tests/unit/strategy_library/test_schemas.py` → `test_strategy_library_schemas.py`

## 验证结果

- NTL-S5-001：EvidencePack roundtrip 验证通过
- NTL-S5-002：10 tests PASS（failure_taxonomy）
- NTL-S5-003：25 tests PASS（postmortem_service）
- 总计：25 evaluation tests PASS

## 下一步（3-5 条）

1. `NTL-S5-007` — 扩展 review task 结构（依赖 Stage 4 完成）
2. `NTL-S5-009` — 让 Manager 生成 Evidence Pack（前置 NTL-S5-001 ✅）
3. `NTL-S5-010` — 升级盘后评分口径（MFE/MAE/规则命中）
4. `NTL-S5-012` — 差评案例写回记忆（依赖 NTL-S5-010）

## 当前停点

- NTL-S5-006 Topic-Memory 完整集成已完成，包含编码格式修复和 docstring 补全

## 产物

- `src/evaluation/__init__.py`（更新）
- `src/evaluation/evidence_pack.py`（新增）
- `src/evaluation/failure_taxonomy.py`（新增）
- `src/evaluation/postmortem_service.py`（新增）
- `src/models/converters.py`（新增）
- `src/models/evidence_pack.py`（新增）
- `docs/superpowers/specs/2026-04-25-failure-taxonomy-design.md`（新增）
- `docs/superpowers/specs/2026-04-25-postmortem-service-design.md`（新增）
- `docs/TaskList.md`（NTL-S5-002/003 标记完成）

## 风险与注意事项

- `test_schemas.py` 同名冲突已解决（重命名）
- LLM 笔记生成（enable_llm_notes）当前未实现，占位
- MFE/MAE/return_pct 在 NTL-S5-010 实现，当前为 None 占位

---

## 下午补充（2026-04-25 下午）

### NTL-S5-006 Review 发现的问题及修复

**问题 1：raw_topic_ids 覆盖 bug**
- 位置：`manager_agent/agent.py` `_build_topic_tags`
- 问题：循环中 `raw_ids["kaipan"] = tid` 覆盖，多 topic 时只保留最后一个
- 修复：schema 改为 `dict[str, list[str]]`，代码用 `setdefault + append`

**问题 2：topic_id 跨数据源无法匹配**
- 位置：`trader_agent/agent.py` `generate_trade_ideas`
- 问题：hot_topics.topic_id 和 topic_constituents.topic_id 来自不同数据源，ID 空间不同（实测重叠率 0%）
- 修复：改用 "topic_name|kind" 编码格式，直接从 topic_constituents 生成 canonical tag，不依赖 hot_topics 查表
- 决策依据：hot_topics.score/increase_pct 在项目后续 Task 中未使用，丢弃不影响功能

**问题 3：缺少 docstrings**
- 修复：补充 ManagerAgent 关键方法 docstrings（`__init__`, `_trader_profiles_path`, `_resolve_path`, `_guess_instrument_focus`, `run_pre_market`, `run_after_close`）

**问题 4：_build_topic_tags 重复调用（性能问题）**
- 决策：保持现状，函数本身很轻量，影响极小

### 相关文档更新

- `docs/superpowers/specs/2026-04-25-topic-memory-integration-design.md` — 更新数据流和设计决策
- `docs/superpowers/plans/2026-04-25-topic-memory-integration.md` — 更新实现步骤
- `docs/TaskList.md` — 更新 NTL-S5-006 完成情况

### commits

```
89b0f50 refactor(NTL-S5-006): use 'topic_name|kind' encoded format for source_topic_ids
cb8942a fix(NTL-S5-006): preserve all topic_ids in raw_topic_ids as list
0d56759 fix(NTL-S5-006): use dict-based hot_topics parsing in _build_topic_tags
652f2f6 docs: add docstrings to ManagerAgent methods
266e6a4 docs: update NTL-S5-006 completion with encoded format approach
```

---

## 晚间补充（2026-04-25 晚间）

### Code Review 发现的问题及修复

**问题 1：测试数据格式错误**
- `test_trader_memory_schemas.py` — `raw_topic_ids` 类型应为 `dict[str, list[str]]`，测试传入 `dict[str, str]`
- `test_extract_article_metadata.py` — 使用 `schema_version` 字段（不存在），应为 `version`

**问题 2：RankingService 异步封装反模式**
- 位置：`src/evaluation/ranking_service.py`
- 问题：已有 event loop 时创建新 loop
- 修复：改用 `asyncio.run()`

**问题 3：_plan_data_requests 重复代码**
- agent.py 和 premarket_service.py 各有一份
- 分析：agent.py 中的是死代码，未被调用
- 修复：删除 agent.py 中的，保留 premarket_service.py 版本并增强递归处理

**问题 4：_build_topic_tags 位置不当**
- 问题：通用解析函数放在 ManagerAgent 中，耦合不合理
- 修复：移到 `src/market_universe/topic_utils.py`，导出到 `market_universe/__init__.py`

### NTL-S5-007 完成：Review Task 结构扩展

- `ReviewTaskDetails.failure_category: str` → `failure_attribution: FailureAttribution | None`
- 与 `PostmortemResult.failure_attribution` 类型对齐
- 测试更新：`test_contracts.py`

### commits

```
[修复] test_trader_memory_schemas.py raw_topic_ids 格式
[修复] test_extract_article_metadata.py schema_version → version
[修复] ranking_service.py 异步封装改用 asyncio.run()
[删除] agent.py 中死代码 _plan_data_requests
[增强] premarket_service.py _plan_data_requests 递归处理
[移动] _build_topic_tags → market_universe/topic_utils.py
[完成] NTL-S5-007 review_task 结构扩展
```

### 当前停点

- NTL-S5-007 ✅ 完成
- NTL-S5-008 未开始（依赖 S5-003 ✅, S5-007 ✅）

### 下一步（3-5 条）

1. `NTL-S5-008` — 把 postmortem 接入任务系统
2. `NTL-S5-009` — 让 Manager 生成 Evidence Pack
3. `NTL-S5-010` — 升级盘后评分口径

---

## 深夜补充（2026-04-25 深夜）

### NTL-S5-010 完成：盘后评分口径升级

#### 设计确认

通过 brainstorming 澄清了 3 个关键问题：
- MFE/MAE 计算窗口：持仓期间（A，从 entry_date 起）
- exit 价格：当日收盘价（A）
- 多空方向：默认只支持做多（B）
- rules_hit：从 SignalContext.rules_snapshot 提取（C+heuristic）
- postmortem 是持续跟踪机制，非一次性事件

#### 实现内容

**Task 1: metrics_calculator.py**
- `_normalize_bar()` — 兼容大小写 key
- `_find_bar_index()` — 按日期查找 bar
- `_extract_rules_hit()` — 从 rules_snapshot 提取 rule_id 列表
- `compute_mfe_mae_return()` — 核心计算（MFE/MAE + exit 判定 + return_pct）
- 14 个单元测试覆盖：止盈触发/止损触发/仍持仓/空数据/日期缺失等

**Task 2: 集成到 PostmortemService**
- `generate()` — 调用 `compute_mfe_mae_return()` 填充 mfe/mae/return_pct
- `_auto_attribution()` — 增强归因逻辑：
  - 亏损 + rules_hit 非空 → `RULE_PRECONDITION_FAILED`
  - 亏损 + rules_hit 为空 → `ENTRY_TIMING_POOR`
  - 缺 bars 数据 → `data_quality_issue`
- extra 新增：`rules_hit`, `exit_triggered`, `exit_date`, `is_final`
- 修复 3 个现有测试 + 新增 2 个归因逻辑测试

**Task 3: 端到端测试**
- 6 个测试覆盖：止盈触发/止损触发/仍持仓/rules_hit/亏损归因

#### 设计文档

- `docs/superpowers/specs/2026-04-25-postmortem-metrics-design.md`
- `docs/superpowers/plans/2026-04-25-postmortem-metrics-plan.md`

#### 验证

- metrics_calculator: 14 tests PASS
- postmortem_service: 17 tests PASS（包含新增测试）
- evaluation 总计: 63 tests PASS
- 总测试: 109 PASS

#### 当前停点

- NTL-S5-010 ✅ 完成
- NTL-S5-008 ✅ 完成
- NTL-S5-009 ✅ 完成

#### 下一步（3-5 条）

1. `NTL-S5-011` — 在盘后生成 ranking（依赖 NTL-S5-010 ✅）
2. `NTL-S5-012` — 差评触发 LLM 归因并写回记忆（依赖 NTL-S5-010 ✅）
3. `NTL-S5-013` — 替换当前仅基于 current_price 的简化评估逻辑

---

## 极晚间补充（2026-04-25 极晚）

### NTL-S5-011 完成：ranking 接入盘后链路

- `handle_postmortem_analysis` 在完成复盘后调用 `RankingService.add_entry`
- ranking_task 在 postmortem_analysis 之后执行

### NTL-S5-012 完成：差评触发 LLM 归因并写回记忆

#### 实现内容

**核心改动：**

1. **TraderMemoryStore.update() + extra 字段**
   - `TraderMemoryItem.extra: dict` 新增（存储 auto_original 等扩展数据）
   - `TraderMemoryStore.update()` 新增：load → modify → save 模式，基于 archive() 实现

2. **PostmortemService.llm_attribution()**
   - 调用 `LLMClient.complete_json(*, system_prompt, user_prompt)`
   - 从 `prompts/llm_attribution.md` 加载模板，变量替换用 string.replace() 避免 {} 与 JSON 格式冲突
   - LLM 未配置时降级为 auto

3. **prompts/llm_attribution.md**
   - 完整 LLM 归因 prompt 模板
   - 变量：{symbol}/{side}/{entry}/{target}/{stop_loss}/{bars}/{auto_reason}/{auto_confidence}

4. **handle_postmortem_analysis 原地更新**
   - 用 `list_filtered` 查找已有的 `failure_case` 条目
   - 存在则 `update()` 原地更新，不新增条目
   - 不存在才 `append()`

5. **run_after_close 传递 auto_attribution**
   - 从 `signal_context.trigger_rules` + `signal_context.confidence` 提取 auto_attribution
   - 传入 `postmortem_task details`

6. **PostgreSQL constraint name 修复**
   - `"uq_trader_strategy_versions_trader_id_strategy_date_version_name"`（68 chars）→ `"uq_tsv_trader_dt_ver"`（19 chars）

#### commits

```
b0aff74 feat(NTL-S5-012): add TraderMemoryStore.update() and extra field
94c58b7 feat(NTL-S5-012): add PostmortemService.llm_attribution()
a8911e2 feat(NTL-S5-012): update failure_case in-place in handle_postmortem_analysis
08ed164 feat(NTL-S5-012): pass auto_attribution to postmortem_task in run_after_close
c37eac8 fix: shorten constraint name to fit PostgreSQL 63 char limit
cc463ed refactor(NTL-S5-012): extract LLM attribution system prompt to prompts folder
a063e57 refactor(NTL-S5-012): move full attribution prompt to prompts/llm_attribution.md
```

#### 验证

- 105 tests PASS
- LLMClient.complete_json signature 问题已修复（`*` keyword-only 参数）
- KeyError '\n  "reason"' 问题已修复（改用 string.replace()）
- pre-existing test failure 已确认（test_manager_creates_structured_review_task_and_review_note，UniqueViolationError，非本次引入）

#### 当前停点

- NTL-S5-011 ✅ 完成
- NTL-S5-012 ✅ 完成
- NTL-S5-013 ✅ 完成（2026-04-26）

#### 下一步（按优先级）

1. `NTL-S5-014` — 验证记忆写回可被下次消费
2. Stage 6 回测基础设施

#### 产物

- `src/evaluation/metrics_calculator.py`（新增）
- `src/evaluation/postmortem_service.py`（修改）
- `tests/unit/evaluation/test_metrics_calculator.py`（新增）
- `tests/unit/evaluation/test_postmortem_with_metrics.py`（新增）
- `docs/superpowers/specs/2026-04-25-postmortem-metrics-design.md`（新增）
- `docs/superpowers/plans/2026-04-25-postmortem-metrics-plan.md`（新增）

---

## 04-26 补充（NTL-S5-013 完成）

### NTL-S5-013 完成：替换简化评估逻辑

#### 实现内容

**核心改动：**

1. **IdeaEvaluation schema 扩展**
   - `status` 扩展为 `Literal["ok", "partial", "fallback", "not_evaluated"]`
   - `current_price` 标注废弃（语义变为 exit_price）

2. **run_after_close 评估循环重构**
   - 先生成 EvidencePack 获取 bars 数据
   - 使用 `compute_mfe_mae_return()` 计算 mfe/mae/return_pct
   - 状态判断：
     - 完整 bars（≥2）→ `status="ok"`
     - bars < 2 → `status="partial"`
     - 无 bars + 有 current_price → `status="fallback"`（降级使用 last_price）
     - 无 entry_price → `status="not_evaluated"`

3. **完全替换旧公式**
   - 废弃 `return_pct = (current_price - entry_price) / entry_price`
   - 统一走 `compute_mfe_mae_return()` 口径

#### commits

```
2f5b982 refactor(NTL-S5-013): extend IdeaEvaluation.status with ok/partial/fallback
27662b4 refactor(NTL-S5-013): replace simplified return_pct with compute_mfe_mae_return
```

#### 验证

- evaluation 模块: 70 tests PASS
- 核心模块（evaluation/schemas/trader_memory/market_universe）: 143 tests PASS

#### 修复 Pre-existing Test Failures

**问题**：`test_manager_writes_memory_and_reuses_it` 和 `test_manager_creates_structured_review_task_and_review_note` 失败，UniqueViolationError on ranking_entries

**根因**：
1. `strategy_version_id=''`（空字符串）导致多个 Phase 0 entry 冲突
2. `upsert()` 的 UPDATE+INSERT 分离操作无法正确处理 unique constraint（constraint 不含 `is_latest`）

**修复**：
1. `ranking_service.py`: `strategy_version_id or ""` → `"__none__"`
2. `ranking_repository.py`: 改用 DELETE+INSERT 原子操作
3. Phase 0 测试断言：`status == "ok"` → `status == "fallback"`（无 bars 数据的正确行为）

#### commits

```
2f5b982 refactor(NTL-S5-013): extend IdeaEvaluation.status with ok/partial/fallback
27662b4 refactor(NTL-S5-013): replace simplified return_pct with compute_mfe_mae_return
77b73e0 fix: ranking_repository upsert DELETE-then-INSERT and update Phase 0 test assertions
```

#### 设计文档

- `docs/superpowers/specs/2026-04-26-S5-013-postmortem-logic-upgrade-design.md`
- `docs/superpowers/plans/2026-04-26-S5-013-postmortem-logic-upgrade-plan.md`
