# 2026-05-07 daily session

## 一句话目标

按 `docs/review/05-06-codex.md` 的 `待修复` 顺序，修复全部 6 个问题，补全日志，完成全项目 Stage Review。

## 关键决策

- 修复顺序严格按 review 文档的依赖关系排列：先修数据可信度（回测/置信度），再修规则闭环（分层提取/模型一致性），再修审核/风控/A股约束
- `_calc_t1_return` 因 `_load_ohlcv_from_db` 不加载前向数据总是返回 None → 新增 `_preload_forward_bars()` 预加载，而非修改共享 loader
- `avg_loss > 0` 条件错误 → 改为 `avg_loss != 0` + `abs()`，设计文档同步修正
- 规则池回测 scheduler 不传 loader → 新增 `_derive_indicators_from_bars()` fallback
- 自动审核在 `_finalize_extraction_artifacts()` 中集成，审核原因写入 `extraction_layer._auto_review` 保证可追溯
- CLI `rule-pool` 命令组新增 `show`/`review-batch`/`--force`/`--status`/`--rule-type`/`--skip-no-mapped`
- 一字板与停牌区分 → 新增 `classify_bar_status()` + `_is_bar_limit_locked()`

## 今日完成

### 修复 #1: Stage 11 真实规则回测与置信度闭环
- `src/backtest/engine.py`: 重写 `_backtest_single_rule()` 实现真实回测；新增 `_evaluate_mapped_condition()`、`_preload_forward_bars()`、`_derive_indicators_from_bars()`、`_calc_sharpe()`、`_calc_max_drawdown()`
- `src/rule_backtest/confidence.py`: 权重对齐设计文档 (0.40/0.20/0.20/0.20)，修复 `avg_loss > 0` 条件
- `src/rule_pool/repository.py`: `update_backtest_result()` 调用 `compute_confidence_adjustment()` 替代简化公式

### 修复 #2: Stage 11 分层提取真正独立化
- `src/agents/data_agent/skills/extract_article_metadata.py`: 重写 `_finalize_extraction_artifacts()` 实现五路独立分流；新增 `_extract_trade_samples_from_content()`、`_persist_trade_samples()`、`_find_sentence_bounds()`

### 修复 #3: 数据模型一致性修正
- `src/rule_pool/models.py`: `TradeSample.article_id` 和 `ArticleClassification.article_id` 改为 UUID + ForeignKey
- 新增 migration: `2026_05_07_0001_fix_article_id_type_to_uuid.py`

### 修复 #4: 自动审核流程补齐
- `src/rule_pool/repository.py`: 新增 `auto_review_rule()`、`update_review()` 增加 `force` 参数
- `cli/main.py`: 新增 `rule-pool show`、`review-batch` 命令；`list` 支持过滤；`review` 支持 `--force` 和 `pending`
- 自动审核在 `_finalize_extraction_artifacts()` 各分支中调用

### 修复 #5: 交易记录进入风控/决策主闭环
- `src/risk/account_service.py`: 新建，`build_account_snapshot()` 从 TradeLog 构建真实 AccountSnapshot
- `src/agents/manager_agent/agent.py`: `evaluate_signal()` 中硬编码模拟账户替换为 `_get_account_snapshot()`

### 修复 #6: A 股实际约束与验证增强
- `src/evaluation/metrics_calculator.py`: 新增 `_is_bar_limit_locked()`、`classify_bar_status()`；`compute_mfe_mae_return` 返回值新增 `limit_locked_dates`
- `src/backtest/scoring.py`、`src/evaluation/postmortem_service.py`、`src/agents/manager_agent/agent.py`: 调用方更新

### 日志补全
- 6 个关键模块从 0 日志补全为 14 处关键日志点：`extract_article_metadata.py`(5)、`repository.py`(3)、`confidence.py`(1)、`classifier.py`(1)、`attribution.py`(1)、`account_service.py`(3)

### 全项目 Stage Review
- 写入 `docs/review/05-07-ds.md`（273+ 行），覆盖 6 个 review 维度，含 16 项修复顺序表
- 3 个后台 Agent 并行审查 40+ 源文件，确认 11 个新缺陷

### 文档更新
- `docs/UserManual.md`: 新增 §7.1 文章→规则完整链路、A 股约束表、风控快照说明；修正安装路径、补全 `extract-articles` 参数
- `docs/db-struct.md`: 新增 rule_pool/trade_sample/article_classification 三张表
- `docs/superpowers/specs/2026-04-30-article-to-rule-pipeline-design.md`: 修正 `avg_loss > 0` 条件
- `docs/superpowers/plans/2026-04-30-article-to-rule-pipeline-plan.md`: TODO 标记为已实现
- `docs/TaskList.md`: 更新 Stage 11 完成标准和下一步建议

## 验证结果

- 核心测试 69 ~ 143 passed（不同阶段不同测试集）
- 全链路导入无循环依赖
- ForeignKey 约束正确指向 `blog_articles.id`
- 一字板识别 6 项 smoke test 通过
- 自动审核逻辑 9 分支验证通过

## 发现的问题 / 风险

- Stage 4 裸 `except Exception` 静默吞没快照/版本加载失败 → 已纳入 05-07-ds.md P0
- `PipelineHealthChecker` 未接入 runner → 已纳入 P1
- `RulePoolPredictionService` 预测结果未被 ManagerAgent 消费 → 已纳入 P0
- `extract_article_metadata.py` 代码膨胀（1686 行）→ 已纳入 P2
- `TraderProfile` 从文件加载非实时 → 已纳入 P2

## 下一步（3-5 条）

1. 按 `docs/review/05-07-ds.md` 修复顺序 Phase 1 开始：Stage 4 异常处理加固
2. Phase 2：规则池回测 CLI + mapped_condition 覆盖率 + 规则池预测集成
3. Phase 3：代码质量（去重、瘦身、防御性加固）
4. Phase 4-5：市场约束增强 + 测试与文档收敛
5. 提交代码到 GitHub

## 产物

- `docs/review/05-07-ds.md`
- `docs/UserManual.md` (更新)
- `docs/db-struct.md` (更新)
- `docs/TaskList.md` (更新)
- `docs/review/05-06-codex.md` (标记完成)
- `src/backtest/engine.py` (重写)
- `src/rule_backtest/confidence.py` (修正)
- `src/rule_pool/repository.py` (增强)
- `src/rule_pool/models.py` (类型修正)
- `src/agents/data_agent/skills/extract_article_metadata.py` (增强)
- `src/risk/account_service.py` (新建)
- `src/evaluation/metrics_calculator.py` (增强)
- `cli/main.py` (CLI 扩展)
- `src/db/migrations/versions/2026_05_07_0001_fix_article_id_type_to_uuid.py` (新建)

## 风险与注意事项

- DB 迁移 `2026_05_07_0001_fix_article_id_type_to_uuid.py` 需要在生产环境执行
- 规则池回测依赖 OHLCV 数据，无数据时返回空结果
- scheduler 创建 BacktestEngine 不传 loader，依赖 fallback 指标派生（仅支持基础 OHLCV+MA）
