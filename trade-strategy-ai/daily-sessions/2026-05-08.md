# 2026-05-08 daily session

## 一句话目标

把 Web 方案、API 入口、默认路径和根目录遗留目录统一收口到 `trade-strategy-ai` 项目内。

## 关键决策

- Web 方案继续按标准管理后台推进，不做 TUI 或 CLI 套壳。
- 后端统一使用 FastAPI app factory，前端仍按 React + TypeScript + Vite 推进。
- CLI 与 Web 共用同一套 service，避免行为分叉。
- 默认 `config` / `data` / `logs` 相对路径统一锚定到 `trade-strategy-ai` 项目根目录，不再依赖当前工作目录。
- API 入口暂时保留 `api/main.py` 与 `src/api/main.py` 两个导出路径，但应用定义只保留一份。

## 今日完成

- 删除 workspace 根目录下的冗余目录：
  - `/Users/wanghui/Documents/Vibe/Trade/data`
  - `/Users/wanghui/Documents/Vibe/Trade/logs`
- 新增统一路径工具 `src/common/paths.py`：
  - 提供 `project_root()` 与 `resolve_project_path()`。
  - 让相对路径默认锚定到 `trade-strategy-ai`。
- 收敛多个默认路径入口：
  - `src/common/config.py`
  - `src/common/logger.py`
  - `src/alerting/logger_.py`
  - `src/market_universe/snapshot_service.py`
  - `src/services/job_service.py`
  - `src/services/dashboard_service.py`
  - `api/routers/backtest_results.py`
  - `api/routers/run.py`
  - `src/alerting/rules/snapshot_rules.py`
- 修正 API 启动钩子：
  - `src/api/app.py` 的 `lifespan()` 也改成从项目根目录读取 `config/app.yaml`。
- 补充文档说明：
  - `docs/UserManual.md`
  - `docs/APIReference.md`
  - 明确 `config/...`、`data/...`、`logs/...` 都按 `trade-strategy-ai` 项目根目录解析。

## 验证结果

- 通过的回归测试：
  - `tests/unit/common/test_paths.py`
  - `tests/unit/common/test_config.py`
  - `tests/unit/market_universe/test_snapshot_service.py`
  - `tests/unit/services/test_job_service.py`
  - `tests/unit/services/test_kaipan_dashboard_service.py`
  - `tests/unit/alerting/test_logger.py`
  - `tests/api/routers/test_backtest_results.py`
  - `tests/api/routers/test_snapshots.py`
  - 结果：`13 passed`
- 额外补跑：
  - `tests/api/test_api_app_factory.py`
  - `tests/api/test_api_entrypoints.py`
  - 结果：`2 passed`

## 发现的问题 / 风险

- `api/main.py` 与 `src/api/main.py` 仍然并存，虽然现在共享同一个 app，但后续还要做一次收尾级清理。
- `docs/README.md` 仍处于删除状态，这不是本轮操作引入的，需要后续确认。
- Root `data/logs` 已清理，但后续新增默认路径时必须继续遵守项目根目录锚定规则，避免再次在 workspace 根目录建目录。

## 下一步（3-5 条，只保留当前会话要继续做的事情）

1. `WEB-S2-004`：统一任务日志与产物目录，收口 `data/jobs/{job_id}/` 规范。
2. 只读全仓扫描：专门查找是否还有残留的 `Path("data/...")` 或 `Path("logs/...")` 需要继续收敛。
3. Review Stage 2 的实现和文档，确认 Job Center、Worker、白名单和 API 入口描述与代码一致。
4. `WEB-S3-006`：继续收敛两套 API 使用方式，按 canonical entrypoint 清理兼容层并同步文档。

## 产物

- `docs/web-plan.md`
- `docs/Web-TaskList.md`
- `src/common/paths.py`
- `src/api/app.py`
- `docs/UserManual.md`
- `docs/APIReference.md`

## 风险与注意事项

- 不要把 Web 做成远程 shell。
- 不要跳过 UserManual 覆盖矩阵，否则容易遗漏独立 CLI 入口。
- Job Center 必须按生产级实现，不能只用 FastAPI background task。
- 相对路径现在默认锚定到 `trade-strategy-ai` 项目根目录，新增路径时必须继续沿用这一规则。
