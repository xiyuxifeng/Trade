# 2026-05-09 daily session

## 一句话目标

完成 Stage 3 的收尾、风险修复和文档对齐，并把当前可用的 UI BFF 范围说清楚。

## 关键决策

- UI API 统一收敛到 `/api/ui/v1/*`，旧的 `/api/ui/system/status` 仅作兼容别名保留。
- Artifact 中心只索引可安全展示的产物，不把 `config/backups` 这类敏感配置备份纳入默认产物中心。
- `WorkflowService` 保持纯业务层，不再反向依赖 API router 的注入函数。
- 覆盖矩阵必须拆成“当前已实现”和“目标设计”两层，避免把规划项误认为已上线。

## 今日完成

- 新增并接通：
  - `ArtifactService`
  - `api/routers/ui/artifacts.py`
  - `api/routers/ui/market.py`
- 迁移并统一：
  - `api/routers/ui/system.py`
  - `api/routers/ui/jobs.py`
  - `api/routers/ui/workflows.py`
  - `api/app.py`
- 扩展服务层：
  - `MarketService.list_symbols()`
  - `MarketService.get_ohlcv()`
  - `JobService.get_job_service()` factory
  - `WorkflowService.make_workflow_service()`
- 修复与收敛：
  - 去掉 `WorkflowService -> api/routers/ui/jobs.py` 的反向耦合
  - 默认产物索引排除 `config/backups`
  - Stage 3 路由版本化收口到 `/api/ui/v1/*`
- 文档对齐：
  - `docs/APIReference.md`
  - `docs/Web-TaskList.md`
  - `docs/web-plan.md`
  - `docs/Web-UserManual-Coverage.md`

## 验证结果

- 后端回归：`72 passed`
- 重点覆盖：
  - `tests/api/routers/test_system_status.py`
  - `tests/api/routers/test_jobs_api.py`
  - `tests/api/routers/test_workflows.py`
  - `tests/api/routers/test_artifacts.py`
  - `tests/api/routers/test_market_ui.py`
  - `tests/unit/services/test_artifact_service.py`
  - `tests/unit/services/test_snapshot_market_service.py`
  - `tests/unit/services/test_workflow_service.py`

## 发现的问题 / 风险

- `ArtifactService` 目前是文件系统扫描式索引，数据量再增大时需要考虑缓存或增量索引。
- 覆盖矩阵中大量 Stage 4/5/6/7 项仍是设计目标，不代表已实现。
- 目前前端尚未落地，`Web-UserManual-Coverage.md` 里“已实现”只应理解为 Stage 3 UI BFF。

## 下一步（3-5 条，只保留当前会话要继续做的事情）

1. `WEB-S4-001`：初始化前端工程。
2. `WEB-S4-002`：接入 Tailwind CSS 和 shadcn/ui。
3. `WEB-S4-003`：实现前端路由和 API client。
4. `WEB-S4-004`：搭建 Dashboard shell 和完整导航骨架。
5. `WEB-S4-005`：让总览页接入系统状态、最近任务和最近产物。

## 产物

- `src/services/artifact_service.py`
- `src/services/market_service.py`
- `src/services/workflow_service.py`
- `api/app.py`
- `api/routers/ui/artifacts.py`
- `api/routers/ui/market.py`
- `api/routers/ui/system.py`
- `api/routers/ui/workflows.py`
- `docs/APIReference.md`
- `docs/Web-TaskList.md`
- `docs/Web-UserManual-Coverage.md`
- `docs/web-plan.md`

## 风险与注意事项

- 不要让 `config/backups` 再进入 Artifact 中心默认索引。
- 不要把 `Web-UserManual-Coverage.md` 的目标设计当成当前已实现范围。
- 新增 UI 路由时必须继续走 `/api/ui/v1/*`，不要再引入新的非版本化入口。

## 补充记录

- 已完成 Stage 4 设计文档与实施计划的 review，确认原先的 `WEB-S4-005` ~ `WEB-S4-007` 只是写入了计划文档，还没有落到 `Web-TaskList.md` 的 Stage 4 任务清单里。
- 已将 `WEB-S4-005`、`WEB-S4-006`、`WEB-S4-007` 补入 `docs/Web-TaskList.md`，并把推荐完成顺序同步到 Stage 4 入口。
- 当前稳定停在 `WEB-S4-001` 前的文档准备阶段，下一次应从 `WEB-S4-001` 初始化前端工程开始。
- 继续执行前需要注意：Stage 4 的首批可用范围已经明确为总览、任务中心、工作流、产物中心和市场数据，后续页面先作为占位路由保留。
