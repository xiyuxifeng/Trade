# 2026-05-09 daily session

## 一句话目标

把 Stage 4 前端基线完整跑通、修复关键交互问题，并把当前停点收束到 Stage 5 的起点。

## 关键决策

- UI API 继续统一收敛到 `/api/ui/v1/*`，旧的兼容入口只保留最小范围。
- Stage 4 的任务中心、工作流、产物中心和市场页先以“可用”为目标，不强行提前引入 K 线图、报表图表等 Stage 6 能力。
- 工作流默认日期必须使用本地日期，不接受 UTC 切日导致的默认值偏差。
- Job 详情里的取消动作只对 `pending / running` 生效，终态任务不应暴露无效操作。
- 表格组件需要纳入共享 UI 原子层，避免各页面重复写原生表格结构。

## 今日完成

- 完成并提交：
  - `WEB-S4-001` 初始化前端工程
  - `WEB-S4-002` Tailwind CSS 和 shadcn/ui 基线
  - `WEB-S4-003` 前端路由和 API client
  - `WEB-S4-004` Dashboard shell 和完整导航骨架
  - `WEB-S4-005` 总览页接入系统状态、最近任务、最近产物
  - `WEB-S4-006` 任务中心、工作流、产物中心和市场页首批功能
  - `WEB-S4-007` 启动说明、验证命令和入口文档对齐
- 新增前端工程与页面：
  - `web/`
  - `web/src/pages/jobs/index.tsx`
  - `web/src/pages/workflows/index.tsx`
  - `web/src/pages/artifacts/index.tsx`
  - `web/src/pages/market/index.tsx`
  - `web/src/components/ui/table.tsx`
  - `web/src/lib/date.ts`
  - `web/README.md`
- 修复 Stage 4 里的实际问题：
  - 工作流和市场页的默认日期从 UTC 切日改为本地日期
  - Job 取消按钮只允许 `pending / running`
  - Jobs / Market 表格统一改用共享 `Table` 组件
- 文档对齐：
  - `docs/Web-TaskList.md`
  - `web/README.md`

## 验证结果

- 前端验证通过：
  - `corepack pnpm build`
  - `corepack pnpm typecheck`
  - `corepack pnpm lint`
- 前端工程可通过 Vite 启动并访问当前 Stage 4 页面。

## 发现的问题 / 风险

- `web/src/pages/workflows/index.tsx` 仍然是基于固定默认参数的首版实现，后续 Stage 5 需要替换成真正的向导式参数表单。
- `web/src/pages/market/index.tsx` 目前是表格版 OHLCV 展示，`WEB-S6-002` 才是最终的 K 线图增强。
- `ArtifactService` 仍然是扫描式索引，后续数据量增长时可能需要缓存或增量刷新。
- Stage 4 的页面已经能用，但很多交互仍是首版实现，后续需要继续按 Stage 5 / Stage 6 逐步补强。

## 下一步（3-5 条，只保留当前会话要继续做的事情）

1. `WEB-S5-001`：定义 UserManual Workflow 数据结构。
2. `WEB-S5-002`：实现操作向导页面。
3. `WEB-S5-003`：实现参数表单与高风险确认。
4. `WEB-S5-004`：让操作向导接入 Job Center。

## 产物

- `web/`
- `web/README.md`
- `web/src/components/ui/table.tsx`
- `web/src/lib/date.ts`
- `web/src/pages/jobs/index.tsx`
- `web/src/pages/workflows/index.tsx`
- `web/src/pages/artifacts/index.tsx`
- `web/src/pages/market/index.tsx`
- `docs/Web-TaskList.md`

## 风险与注意事项

- 不要把 Stage 6 的 K 线图目标误认为 Stage 4 已完成。
- 不要让工作流日期继续回到 UTC 默认值。
- Job 终态不要再暴露无效取消动作。
- 后续新增页面尽量继续复用共享 UI 原子组件，不要回到原生表格和分散样式。
