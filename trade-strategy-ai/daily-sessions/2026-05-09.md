# 2026-05-09 daily session

## 一句话目标

把 `WEB-S6-001` 的任务中心增强做成可追踪、可交接、可验收的状态，并把今天的实现、验证和交接点收束清楚。

## 关键决策

- UI API 继续统一收敛到 `/api/ui/v1/*`，旧的兼容入口只保留最小范围。
- Stage 6 的任务中心增强先落在现有 Jobs 页面，不另起新页面体系。
- `WEB-S6-001` 的细粒度追踪放到 `docs/superpowers/plans/2026-05-09-web-s6-001-job-center-enhancement.md`，Stage 6 主清单只保留阶段级状态。
- `WEB-S8-006` 的 E2E 追踪放到 `docs/superpowers/plans/2026-05-09-web-s8-006-e2e-acceptance-plan.md`，Stage 8 主清单继续保留总入口。
- Jobs 页的重跑入口只复用当前 `job_type` 和 `params`，不引入新的调度模型。
- 产物关联先通过 `jobId` 跳转到 Artifacts Center，避免在 Jobs 页里手工拼复杂预览。

## 今日完成

- 新增 `WEB-S6-001` 追踪计划：
  - `docs/superpowers/plans/2026-05-09-web-s6-001-job-center-enhancement.md`
- 新增 `WEB-S8-006` E2E 追踪计划：
  - `docs/superpowers/plans/2026-05-09-web-s8-006-e2e-acceptance-plan.md`
- 把 Stage 6 的任务追踪写入主清单：
  - `WEB-S6-001` 标记为进行中
  - `WEB-S6-010` 作为实施追踪与回归验收清单已完成并同步到主清单
- 启动 Jobs 页面增强：
  - `web/src/pages/jobs/index.tsx` 增加 rerun 确认弹窗
  - `web/src/pages/jobs/index.tsx` 增加产物引用卡片和跳转 Artifacts Center 的入口
  - `web/src/pages/artifacts/index.tsx` 增加 `jobId` 过滤
  - `web/src/lib/api/jobs.ts` 增加 `createJob`
  - `web/src/types/jobs.ts` 补齐 Job artifact 类型
- 给 `web/` 补上测试框架：
  - `vitest`
  - `@testing-library/react`
  - `@testing-library/user-event`
  - `@testing-library/jest-dom`
  - `jsdom`
- 新增第一批前端测试：
  - `web/src/pages/jobs/index.test.tsx`
  - `web/src/pages/artifacts/index.test.tsx`
  - `web/src/test/test-utils.tsx`
  - `web/src/test/setup.ts`
- 前端验证通过：
  - `corepack pnpm typecheck`
  - `corepack pnpm lint`
  - `corepack pnpm build`
  - `corepack pnpm test`

## 验证结果

- 前端验证通过：
  - `corepack pnpm typecheck`
  - `corepack pnpm lint`
  - `corepack pnpm build`
  - `corepack pnpm test`
- `WEB-S6-001` 相关 Jobs / Artifacts 页面改动已通过本地构建和类型检查。

## 发现的问题 / 风险

- `WEB-S6-001` 已完成，当前 Jobs / Artifacts 联动闭环已经落地，后续如果继续细化可再开新追踪点。
- `web/src/pages/market/index.tsx` 目前仍是表格版 OHLCV 展示，`WEB-S6-002` 才是最终的 K 线图增强。
- `ArtifactService` 仍然是扫描式索引，后续数据量增长时可能需要缓存或增量刷新。

## 下一步（3-5 条，只保留当前会话要继续做的事情）

1. `WEB-S6-002`：市场数据页升级为 K 线图 + OHLCV 表格。
2. `WEB-S8-006`：按计划推进 Jobs / Workflows / Artifacts / Settings / Auth 的 E2E 主链路。
3. 如果继续收口文档，补充 `docs/UserManual.md` 与前端路由说明的对应关系。
4. 关注 `ArtifactService` 的扫描式索引成本，必要时再做缓存或增量刷新。

## 产物

- `docs/superpowers/plans/2026-05-09-web-s6-001-job-center-enhancement.md`
- `docs/superpowers/plans/2026-05-09-web-s8-006-e2e-acceptance-plan.md`
- `web/src/lib/api/jobs.ts`
- `web/src/pages/artifacts/index.tsx`
- `web/src/pages/jobs/index.tsx`
- `web/src/types/jobs.ts`
- `web/src/pages/jobs/index.test.tsx`
- `web/src/pages/artifacts/index.test.tsx`
- `web/src/test/test-utils.tsx`
- `web/src/test/setup.ts`
- `docs/Web-TaskList.md`

## 风险与注意事项

- 不要把 Stage 6 的 K 线图目标误认为 Stage 5 已完成。
- 继续保持工作流向导的参数语义和后端 schema 一致，避免再次出现静默默认值问题。
- 后续新增页面尽量继续复用共享 UI 原子组件，不要回到原生表格和分散样式。
- 新增路由或 query param 联动时，文档要同步更新，避免代码和说明分叉。
