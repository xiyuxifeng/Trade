# 2026-05-15 daily session

## 一句话目标
继续收口 V1 交付链，完成 `NW-V1-S2-003` 的可执行编排实现，并把 `NW-V2-S2-004` 的后续数据库持久化边界提前写清。

## 关键决策
- `NW-V1-S2-002` 继续采用单一 canonical 的 Step Timeline contract，不新增第二套正式入口。
- Step Timeline 的优先实现方式是基于现有 Job audit events 做结构化归一化，先满足 Job Detail 的用户可用性，再把执行层事件逐步收口到 `NW-V1-S2-003`。
- `NW-V1-S2-002` 和 `NW-V1-S2-003` 可以一起做，但默认顺序仍然是先 `NW-V1-S2-002`，再 `NW-V1-S2-003`。
- `NW-V1-S2-003` 采用薄 `WorkflowRunner` 方案：`WorkflowService` 继续负责定义与校验，执行交给 `WorkflowRunner`，每个 step 复用 `JobRunner`。
- `NW-V2-S2-004` 仍然是 Market Data DB Storage 主任务，但其下可以挂一个后续子任务，专门收口 Workflow Run 持久化 MVP，避免重复任务编号。

## 今日完成
- 明确了 `NW-V1-S2-002` 的用户价值：它是用户可直接看到的 Job 执行过程，而不是内部日志视图。
- 完成 `NW-V1-S2-002` 的实现方案讨论，收窄为：
  - 新增结构化 timeline model/service
  - 从 Job audit events 归一化输出
  - 保持 Job Detail / Step Timeline 组件只消费 contract
- 落地 `src/models/step_timeline.py` 与 `src/services/step_timeline_service.py`，把 Job audit events 归一化为结构化 Step Timeline。
- 调整 `JobService` 与 `/api/ui/v1/jobs/{job_id}/timeline`，让 Job Detail API 返回结构化 timeline contract。
- 补齐 unit / API 测试并通过回归，确认成功、失败、取消和运行中场景都能返回 timeline。
- 落地 `src/services/workflow_runner.py`，实现 Workflow 的顺序编排、失败停止、step 结果汇总和 artifact 绑定。
- 调整 `src/services/workflow_service.py`，把运行入口改为委托 `WorkflowRunner`，同时保留参数校验与高风险确认。
- 更新 `NW-V2-S2-004` 的 TaskList 说明，补入后续 `Workflow Run 持久化 MVP` 子任务边界、表结构、字段和验收标准。
- 补齐 `tests/unit/services/test_workflow_runner.py`、`tests/unit/services/test_workflow_service.py`、`tests/api/routers/test_workflows.py`、`tests/api/routers/test_pipelines.py`、`tests/api/test_ui_openapi_contract.py`、`tests/pipelines/test_article_pipeline_spec.py` 并通过回归。
- 新增 `src/services/pipeline_application_service.py`，把 `article_pipeline` 收敛为 Web/API 的 canonical 入口，并通过 `WorkflowRunner` 复用现有 crawl / pipeline-run / pipeline-step 能力。
- 调整 `api/routers/ui/pipelines.py`，让 `/api/ui/v1/pipelines` 不再依赖 `WorkflowService` 的 legacy `pipeline` workflow。
- 更新 `docs/New-Web-Linked-TaskLists/New-Web-TaskList.md`，将 `NW-V1-S3-002` / `NW-V1-S3-003` 标记为完成，并把 `pipeline` 说明为兼容层而不是正式新入口。
- 补齐 `tests/unit/services/test_pipeline_application_service.py`，并回归 `tests/api/routers/test_pipelines.py`，确认 article_pipeline 只暴露 canonical 入口。
- 收口 `UI-V1-004`，补齐 Job List 页面实现与回归测试，并将 UI TaskList 更新为已验收完成。

## 验证结果
- 已执行并通过：
  - `pytest tests/unit/services/test_step_timeline_service.py -v`
  - `pytest tests/api/routers/test_jobs_api.py -v`
  - `pytest tests/unit/services/test_job_service.py -v`
  - `pytest tests/unit/models/test_models.py -v`
  - `pytest tests/api/test_ui_openapi_contract.py -v`
- 已执行并通过：
  - `pytest tests/unit/services/test_workflow_runner.py -v`
  - `pytest tests/unit/services/test_workflow_service.py -v`
  - `pytest tests/api/routers/test_workflows.py -v`
  - `pytest tests/api/routers/test_pipelines.py -v`
  - `pytest tests/api/test_ui_openapi_contract.py -v`
  - `pytest tests/pipelines/test_article_pipeline_spec.py -v`
- 已执行并通过：
  - `pytest tests/unit/services/test_pipeline_application_service.py -v`
  - `pytest tests/api/routers/test_pipelines.py -v`
  - `pytest tests/unit/services/test_workflow_service.py -v`
  - `pytest tests/api/routers/test_workflows.py -v`
  - `pytest tests/api/test_ui_openapi_contract.py -v`
  - `pytest tests/pipelines/test_article_pipeline_spec.py -v`
- `NW-V1-S2-002`、`NW-V1-S2-003`、`NW-V1-S3-002`、`NW-V1-S3-003` 已满足当前 TaskList 的验收边界。

## 发现的问题 / 风险
- Step Timeline 目前仍是基于 Job audit events 的归一化视图，适合 V1 交付，但不是完整的 workflow execution trace。
- 后续 workflow 持久化若要上数据库，需要另起事实源，避免继续依赖 job audit events 拼装历史。

## 下一步（3-5 条，只保留当前会话要继续做的事情）
1. 准备 `NW-V2-S2-004` 下的 `Workflow Run 持久化 MVP` 后续子任务设计。
2. 保持 Job Detail 页面继续消费当前 canonical timeline contract。
3. 如果后续要继续扩展 Workflow，优先复用当前 `WorkflowRunner` 和 `JobRunner` 分层。

## 产物
- `trade-strategy-ai/daily-sessions/2026-05-15.md`
- `trade-strategy-ai/daily-report/2026-05-15.md`

## 风险与注意事项
- 不要把 Step Timeline 做成纯日志文本。
- 不要提前把 `NW-V1-S2-003` 的执行模型塞进 `NW-V1-S2-002`。
- 不要在未确认前更新 TaskList 状态为完成。

## Current Context
- 当前主线是 V1 交付链收口与 V2 持久化边界预埋。
- `UI-V1-004`、`UI-V1-005`、`UI-V1-006`、`NW-V1-S2-001`、`NW-V1-S2-002`、`NW-V1-S2-003`、`NW-V1-S3-002`、`NW-V1-S3-003` 已完成。
- 现在主要停在 `NW-V1-S4-001` 的回归验收与 `NW-V2-S2-004` 的后续持久化子任务边界上。

## Resume Point
- 从 `NW-V1-S4-001` 的 V1 E2E 回归或 `NW-V2-S2-004` 的 Workflow Run 持久化 MVP 后续子任务继续，当前 `article_pipeline` 已切到 canonical 应用服务。
- `UI-V1-004` 已收口完成，可直接进入 `NW-V1-S4-001` 或继续处理 `NW-V2-S2-004`。
