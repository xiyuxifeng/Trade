# 2026-05-18 daily session

## 一句话目标
收口 `NW-V3-SX-001` / `UI-V3-010`，并补上 benchmark OHLCV 选项链路，让回测页可直接按指数名称+代码选择基准。

## Current Context
- `ACTIVE_TASK_LIST`: `docs/New-Web-Linked-TaskLists/New-Web-TaskList.md`
- `Current Stage`: `NW-V3-SX-001` 已完成并同步 TaskList；`UI-V3-010` 已完成并同步 TaskList
- `Current Focus`: benchmark 指数预置、OHLCV 抓取与回测选择链路已补齐，下一步仍按 `New-Web-Market-Regime-Definition.md` 先补 P0 数据，再做 `NW-V3-SX-002`

## 关键决策
- 先做后续任务，不等待所有增强数据一次补完。
- 仅把 `benchmark_ohlcv_window`、`ret_5d`、`ret_20d`、`ma20_gap`、`ma60_gap` 作为下一阶段前置的 P0 数据。
- 保持 Web-first，不回到 CLI / Demo 方向。

## 今日完成
- 完成 `Market Regime` 正式定义文档，补齐字段定义、规则设计、数据清单和实现顺序。
- 落地 `Market Regime` schema、repository、service、API、Web viewer 与测试。
- 将 `UI-V3-010` 标记为完成，并把 `NW-V3-SX-001` 的回测读取链补到可复现闭环。
- 把 `NW-V3-SX-001`、`UI-V3-010` 的 TaskList 状态同步完成。
- 预置常用指数到 `stock_info`，让 OHLCV 抓取顺便拉取指数日线。
- 给回测页加了 benchmark 下拉框，显示指数名称 + 代码，并把 `benchmark_symbol` 贯穿到回测请求与结果展示。
- 修复 benchmark options 的 `limit` 截断问题，并在回测加载时对缺失 benchmark OHLCV 做显式失败。

## 本次补充
- 移除了 `persona` 配置里的 benchmark 默认项，回测 / MarketState / Snapshot 入口都改成显式选择 `benchmark_symbol`。
- 同步收紧了 `market-state-build` / `snapshot-build` 的 job schema、Market Workspace、Snapshots Page 和相关服务签名，避免再从配置里隐式回退基准指数。
- 后端与前端的针对性测试已通过，当前没有新增的运行断点。
- 为 `New-Web-Market-Regime-Definition.md` 的 `10.5` 补了一份落表与计算分层设计文档，并在正文中挂了链接。

## 验证
- `pytest tests/unit/backtest/test_snapshot_loader.py tests/unit/backtest/test_engine_regime_context.py tests/unit/services/test_backtest_service.py tests/unit/services/test_market_regime_service.py -v`
- 结果：`16 passed`
- `./node_modules/.bin/vitest run src/features/market-browser/market-snapshot-browser-regime.test.tsx src/pages/market/index.test.tsx`
- 结果：`2 passed`
- `rtk python -m pytest tests/unit/backtest/test_schemas.py tests/unit/backtest/test_engine_regime_context.py tests/unit/services/test_backtest_service.py tests/unit/services/test_job_runner.py -vv`
- 结果：`31 passed`
- `./node_modules/.bin/vitest run src/lib/api/backtests.test.ts src/lib/api/market.test.ts src/pages/backtest/index.test.tsx src/pages/backtests/index.test.tsx`
- 结果：`8 passed`
- `rtk python -m pytest tests/api/routers/test_market_ui.py tests/unit/backtest/test_snapshot_loader.py -vv`
- 结果：`16 passed`
- `rtk python -m pytest tests/unit/services/test_backtest_service.py -vv`
- 结果：`3 passed`

## Resume Point
- `Current Task`: `NW-V3-SX-002`
- `Blocked By`: `P0 数据补齐`（`benchmark_ohlcv_window`、`ret_5d`、`ret_20d`、`ma20_gap`、`ma60_gap`）
- `Files`: `docs/New-Web-Market-Regime-Definition.md`, `src/market_data/stock_info_service.py`, `src/market_data/ohlcv_service.py`, `web/src/features/backtest/backtest-center.tsx`, `web/src/features/backtests/backtests-center.tsx`
- `Pending Validation`: 无
- `Next Action`: 补 P0 数据后启动 `NW-V3-SX-002 Regime-aware Backtest`

## 下一步
- 先补 P0 数据，再做 `NW-V3-SX-002`
- 保持 regime contract 版本化
- 不把缺失增强字段写死到 UI / Backtest 主流程
