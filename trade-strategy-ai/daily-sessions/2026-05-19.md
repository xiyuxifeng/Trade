# 2026-05-19 daily session

## 一句话目标
完成 `NW-V3-SX-002` / `UI-V3-011`，把 regime-aware backtest 的 full-market 版本、统一结果 schema、报告页和文档状态收口到可交付状态。

## Current Context
- `ACTIVE_TASK_LIST`: `docs/New-Web-Linked-TaskLists/New-Web-TaskList.md`
- `Current Stage`: Regime-aware Backtest 收口
- `Current Focus`: 已将 regime / feature 默认版本统一切到 v3，回测结果扩展为 canonical schema，新增 per-regime / per-rule-per-regime 统计，并把 full-market 特征写入 `market-regime-features-v3`

## 关键决策
- 保留 benchmark 口径，不替换现有主链路。
- 额外新增 `gap_down_rate_full_market` / `extreme_drop_count_full_market`，作为 full-market 风险增强输入。
- `UI-V3-011` 直接消费统一后的回测结果 schema，保持浅色正式工作台风格。
- 进一步修正 `/backtest_results` 的结果发现逻辑：新增兼容 `data/jobs/<job_id>/result.json` 和 `backtest_report.md`，避免 regime-aware backtest 只写 job 目录却无法在报表页检索。

## 今日完成
- 将 `Market Regime` 与 `Market Regime Feature` 默认版本统一收口为 v3，避免最终交付时再切版本。
- 扩展 `BacktestResult` / `RuleBacktestResult`，加入 regime 版本、来源 feature 版本和 regime 分桶统计。
- 回测引擎支持按 market regime 汇总 overall / per-regime / per-rule-per-regime 结果。
- `BacktestService` 新增 rule-pool backtest 统一入口，并在执行前补齐 v3 feature / regime 产物。
- `market_regime_features-v3` 落库 full-market 风险特征。
- `Regime Backtest Report` 页面上线，包含结果列表、版本展示、分 regime 表、规则分桶和 markdown / JSON 切换。
- `NW-V3-SX-002` 与 `UI-V3-011` 已标记为完成。

## 验证
- `python -m pytest tests/unit/services/test_job_registry.py tests/unit/services/test_job_runner.py tests/unit/services/test_market_regime_feature_service.py tests/unit/services/test_market_regime_service.py tests/unit/services/test_backtest_service.py tests/api/routers/test_backtest_results.py -q`
- 结果：`32 passed`
- `pnpm --dir web test -- src/pages/backtest/RegimeBacktestReportPage.test.tsx src/app/navigation.test.ts src/app/route-registry.test.ts`
- 结果：`5 passed`
- `python -m pytest tests/api/routers/test_backtest_results.py tests/unit/services/test_job_runner.py tests/unit/services/test_backtest_service.py -q`
- 结果：`20 passed`

## Resume Point
- `Current Task`: `NW-V3-SX-003 P0 Rule Applicability Profile`
- `Blocked By`: 无
- `Files`: `src/backtest/engine.py`, `src/backtest/reporting.py`, `src/backtest/schemas.py`, `src/services/backtest_service.py`, `src/services/market_regime_feature_service.py`, `src/services/market_regime_service.py`, `web/src/features/backtest/regime-backtest-report.tsx`
- `Pending Validation`: 无
- `Next Action`: 进入 `NW-V3-SX-003` 的实现准备，复用当前 regime-aware backtest 结果作为事实源
