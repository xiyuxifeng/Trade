# 2026-05-22 daily session

## 一句话目标
实现 review 文档 `docs/review/strategies_new.md` 的 T4 策略辅助页面与版本构建需求，完成 Profile-only 提交、辅助页面拆分与相关验收闭环。

## Current Context
- `ACTIVE_TASK_LIST`: 无，当前任务来自 `docs/review/strategies_new.md`
- `Current Stage`: review 需求实现与验证收尾
- `Current Focus`: 已完成 strategy-build Web 契约收口、策略辅助页面拆分、benchmark_symbol 可选与 Profile 级回填修复，后端单测已通过

## 关键决策
- `strategy-build` 保持兼容 `config_path` 的 CLI 入口，但 Web 提交改为 `profile_id` 为主，不再在页面层暴露旧路径参数。
- 策略辅助页面拆为 `/strategies/versions`、`/strategies/candidates`、`/strategies/history`、`/strategies/regime-selection`，页面通过真实 API 读取数据。
- `benchmark_symbol` 在前端改为可空，留空时由后端按 Profile 默认值回填；后端已修正为优先读取 Profile，而不是只用全局配置默认值。
- 旧 `/workflows` 的盘前 / 盘后正式入口已通过路由重定向退役到策略页。

## 今日完成
- 收口 `strategy-build` 的 Web 契约：
  - `job_registry` 允许 `profile_id` 作为 Web 正式入口
  - `job_runner` 传递 `profile_id`
  - `strategy_service` 兼容 `profile_id` / `config_path`
- 新增并接入策略辅助页面：
  - `/strategies/versions`
  - `/strategies/candidates`
  - `/strategies/history`
  - `/strategies/regime-selection`
- 更新策略工作台与盘前页：
  - 去掉 Web 侧 `config_path` 暴露
  - `benchmark_symbol` 改为可空并支持后端回填
  - 补齐返回策略首页入口
- 修正 `benchmark_symbol` 的后端回填优先级：
  - Profile 级默认值优先
  - Profile 缺失时返回明确错误
  - `job_runner` 不再把 `snapshot-build` 的 `profile_id` 强制补成 `default`
- 更新路由与 route registry，补充相关测试。
- 后端单测通过，验证 `profile_id` / `benchmark_symbol` 相关契约。

## 验证
- `python -m pytest tests/unit/services/test_job_registry.py tests/unit/services/test_job_runner.py tests/unit/services/test_strategy_service.py -q`
- 结果：`29 passed`
- `python -m pytest tests/unit/services/test_snapshot_market_service.py tests/unit/services/test_job_runner.py -q`
- 结果：`38 passed`
- `PATH=/Users/wanghui/.nvm/versions/node/v18.20.8/bin:$PATH /Users/wanghui/.nvm/versions/node/v18.20.8/bin/pnpm typecheck`
- 结果：通过
- `PATH=/Users/wanghui/.nvm/versions/node/v18.20.8/bin:$PATH /Users/wanghui/.nvm/versions/node/v18.20.8/bin/pnpm test`
- 结果：`65` 个测试文件、`147` 个测试全部通过

## Resume Point
- `Current Task`: `docs/review/strategies_new.md` 的 T4
- `Blocked By`: 无
- `Files`: `src/services/job_registry.py`, `src/services/job_runner.py`, `src/services/strategy_service.py`, `web/src/features/strategy-workspace/*`, `web/src/pages/strategies/*`, `web/src/app/router.tsx`, `web/src/app/route-registry.ts`
- `Pending Validation`: 无
- `Next Action`: 无
