# 2026-05-24 daily session

## 一句话目标
推进 `docs/New-Web-Data-Storage-Migration-Plan.md` 的迁移 TaskList，继续推进后续迁移项。

## Current Context
- `ACTIVE_TASK_LIST`: `docs/New-Web-Data-Storage-Migration-Plan.md`
- `Current Stage`: 3 年回测摘要与索引层收口完成
- `Current Focus`: 进入端到端链路校验，确认没有新的文件主查询分支
- `Current Status`: `DS-MIG-004` 已完成并标记为 `[x]`

## 关键决策
- 以 3 年回测为硬约束推进存储收口。
- 任务清单先只更新状态，不扩大范围。

## 今日完成
- 确认迁移文档中的 TaskList 归属。
- 将 `DS-MIG-001` 完成并标记为 `[x]`。
- 将 `DS-MIG-002` 完成并标记为 `[x]`。
- 将 `DS-MIG-003` 完成并标记为 `[x]`，补齐 `backtest_result_runs`、`strategy_regime_selections`、`regime_rule_selections` 摘要层与索引层。
- 将 `DS-MIG-004` 完成并标记为 `[x]`，确认 3 年回测端到端链路已遵循存储边界。
- 在迁移文档中补充文件 canonical 与 artifact 边界规则，明确哪些路径是长期输入，哪些路径只保留为 artifact。
- 将 `src/strategy/signal_version.py` 明确为旧兼容模块，不再作为新的业务事实层入口。
- 将 pipeline 的 `crawl/validate/process/export` 产物目录纳入 artifact 边界，避免遗漏实际运行中间件文件。
- 将 `signals` 的 UI 查询链路收口为 DB 优先：
  - 新增 `SignalRepository`
  - `SignalService` 优先查询 `signals` 表
  - `/api/ui/v1/signals` 改为注入 DB session factory
- 补充了信号、回测结果、规则适用性与 trader option 的测试，验证 DB-only 查询链路。
- 将回测结果与规则适用性链路收口为 DB-only，并补齐分页：
  - `/backtest_results` 已收口为 DB-only，列表、详情和报告下载都不再扫旧回测目录
  - `RuleApplicabilityService` 已收口为 DB-only，唯一输入来自 `jobs.result`
  - `TraderOptionService` 的 `backtest` 来源已改为读取 `jobs` 表，不再扫文件
- `/api/ui/v1/signals` 与 `SignalService.list_signals` 已收口为 DB-only，CLI 也改为读取 DB
- `ManagerAgent` 生成的信号已写入 `signals` 表，`EvaluationContextService` 改为从 DB 读取 signal context
- `backtest_result_runs` 已落库并接入 `JobService.complete_job`，`backtest_results` 列表优先读取该摘要表
- `strategy_regime_selections` / `regime_rule_selections` 已落库并接入 `RegimeRuleSelectionService`
- 修复了摘要层静默失败问题：
  - `JobService.complete_job` 在 `backtest_result_runs` 写入失败时不再静默成功，失败会直接冒泡并触发回滚
  - `RegimeRuleSelectionService.build_regime_rule_selection` 在摘要持久化失败时返回 `partial` 并附带 warning，不再返回 `ok`

## 验证结果
- `python -m pytest tests/unit/db/repositories/test_signal_repository.py tests/unit/evaluation/test_evaluation_context_service.py tests/unit/services/test_trader_option_service.py tests/api/routers/test_backtest_results.py tests/unit/services/test_persona_signal_service.py tests/unit/services/test_rule_applicability_service.py tests/api/routers/ui/test_signals.py -q`，`17 passed`
- `python -m pytest tests/unit/services/test_job_service.py tests/unit/services/test_regime_rule_selection_service.py -q`，`19 passed`
- `python -m pytest tests/unit/services/test_workflow_runner.py tests/unit/services/test_job_audit_query_service.py -q`，`5 passed`
- `python -m pytest tests/unit/services/test_persona_signal_service.py tests/unit/services/test_trader_option_service.py tests/api/routers/test_backtest_results.py tests/unit/services/test_rule_applicability_service.py tests/api/routers/ui/test_signals.py -q`，`14 passed`
- `git diff --check` 通过

## 发现的问题 / 风险
- `Signal` ORM 仍使用 PostgreSQL 专用类型，SQLite 不能直接建表，因此相关测试采用 mock session 而不是本地 SQLite 直连。
- 当前仍需按 Task 顺序推进后续迁移项，避免提前扩展到未确认的表或文件边界。

## Resume Point
Current Task: 无
Blocked By: 无
Files: `docs/New-Web-Data-Storage-Migration-Plan.md`, `src/services/job_service.py`, `src/services/regime_rule_selection_service.py`, `api/routers/backtest_results.py`, `src/db/migrations/versions/2026_05_24_0001_add_backtest_and_regime_selection_summary_tables.py`, `tests/unit/db/repositories/test_backtest_result_run_repository.py`, `tests/unit/db/repositories/test_strategy_regime_selection_repository.py`, `tests/unit/services/test_job_service.py`, `tests/unit/services/test_regime_rule_selection_service.py`, `tests/api/routers/test_backtest_results.py`
Commands: `sed`, `rg`, `git diff --check`, `python -m pytest tests/unit/db/repositories/test_backtest_result_run_repository.py tests/unit/db/repositories/test_strategy_regime_selection_repository.py tests/unit/services/test_job_service.py tests/unit/services/test_regime_rule_selection_service.py tests/api/routers/test_backtest_results.py -q`
Pending Validation: 无
Next Action: 若后续继续迁移，再从新的 TaskList 任务开始

## 下一步（3-5 条，优先引用 ACTIVE_TASK_LIST 中的任务编号）
1. 如需继续迁移，先确认新的 TaskList 任务编号。
2. 否则仅做最终收尾与归档。

## 产物
- `docs/New-Web-Data-Storage-Migration-Plan.md`

## 风险与注意事项
- 不要在未完成验收前把任务标成 `[x]`。
