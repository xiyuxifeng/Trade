# 2026-06-08 daily session

## 一句话目标
把 GD-R4 的 Web 交付口径继续收口到最终版本，兼容路由只保留历史访问，不再以正式路径叙事暴露给用户。

## Current Context
ACTIVE_TASK_LIST: `docs/New-Web-Linked-TaskLists/Goal-Driven-Rebuild/TaskList.md`
Current Stage: GD-R5 最终验收完成
Current Focus: 最终交付口径已收口，剩余既有 typecheck 阻塞已清理完毕
Current Status: 相关前端测试已通过；`dashboard-status-summary.test.tsx`、`ProfileDetailPage.test.tsx`、`market-workspace-shell.test.tsx` 通过；`typecheck` 已通过

## 关键决策
- `/strategies` 根入口只保留跳转，不作为正式入口。
- `/strategies/versions`、`/strategies/candidates`、`/strategies/history`、`/strategies/regime-selection` 继续保留历史访问，但不进入主导航。
- 全局壳对兼容页面只展示收口后的兼容口径，不再把原始路径当作正式 Route 叙事。

## 今日完成
- 将 `routeRegistry` 的兼容路由分层为 `compat`，并让全局状态条对兼容路由显示 `兼容访问 / 历史链接`
- `dashboard-status-summary.tsx` 的 `>` 文本解析错误已修复
- `market-workspace-shell.tsx` 的 toast 参数类型已收口
- `ProfileDetailPage.tsx` 的 `Badge` 导入已补齐
- 对应测试与 `typecheck` 已通过

## 验证结果
- `pnpm vitest run src/app/route-registry.test.ts src/app/navigation.test.ts src/features/strategy-workspace/strategy-workspace-shell.test.tsx src/pages/strategies/index.test.tsx src/pages/strategies/RegimeRuleSelectionPage.test.tsx`

## 发现的问题 / 风险
- 兼容路由仍保留在 `routeRegistry` 中用于历史链接命中，这是预期行为。
- 兼容路由仍保留用于历史链接命中，后续如有需要再决定是否进一步迁移为明确重定向页。

## Resume Point
Current Task: 最终交付收口完成
Blocked By: 无
Files:
- `docs/New-Web-Linked-TaskLists/Goal-Driven-Rebuild/TaskList.md`
- `web/src/app/route-registry.ts`
- `web/src/components/layout/status-strip.tsx`
- `web/src/layouts/dashboard-layout.tsx`
- `web/src/features/strategy-workspace/strategy-workspace-home.tsx`
- `web/src/features/strategy-workspace/strategy-workspace-shell.tsx`
- `web/src/features/strategy-regime-selection/strategy-regime-selection.tsx`
- `web/src/pages/strategies/index.test.tsx`
- `web/src/app/route-registry.test.ts`
Commands:
- `pnpm vitest run src/app/route-registry.test.ts src/app/navigation.test.ts src/features/strategy-workspace/strategy-workspace-shell.test.tsx src/pages/strategies/index.test.tsx src/pages/strategies/RegimeRuleSelectionPage.test.tsx`
Pending Validation: 无
Next Action: 如需继续，进入交付摘要或后续维护

## 下一步（3-5 条，优先引用 ACTIVE_TASK_LIST 中的任务编号）
- 这次补完了 `dashboard-status-summary.tsx`、`market-workspace-shell.tsx`、`ProfileDetailPage.tsx`
- 相关测试和 `typecheck` 都已通过
- 当前可进入交付摘要或后续维护

## 产物
- 兼容路由分层与状态条收口
- TaskList 兼容层口径补充

## 风险与注意事项
- 兼容路由是为历史链接保留，不应再次进入主导航或首页主叙事
- 继续修改时不要把兼容层重新包装成正式入口
