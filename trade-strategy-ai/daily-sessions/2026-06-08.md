# 2026-06-08 daily session

## 一句话目标
把 GD-R5 的 Web 交付口径继续收口到最终版本，候选能力拆成独立子页面，兼容路由只保留历史访问，不再以正式路径叙事暴露给用户。

## Current Context
ACTIVE_TASK_LIST: `docs/New-Web-Linked-TaskLists/Goal-Driven-Rebuild/TaskList.md`
Current Stage: GD-R5 最终交付收口完成
Current Focus: 将候选版本能力从回测页内折叠区收口为独立子页面，并同步验证路由、测试和文案
Current Status: 相关前端测试已通过；`src/pages/backtest/CandidatesPage.test.tsx`、`src/pages/backtest/index.test.tsx`、`src/app/route-registry.test.ts` 通过；`typecheck` 已通过

## 关键决策
- `/strategies` 根入口只保留跳转，不作为正式入口。
- `/strategies/versions`、`/strategies/candidates`、`/strategies/history`、`/strategies/regime-selection` 已删除，不再保留历史访问。
- `/articles/jobs` 和 `/articles/maintenance` 已从正式路由中移除，不再作为对外入口。
- `candidates` 的能力保留在 `backtest` 体系内的独立子页面 `/backtest/candidates`，回测页只保留轻入口。
- 候选区块的错误恢复语义已切回 `backtest` / `artifacts`，不再回流旧 `strategy` 文案。
- 文章页中的 `ArticleMaintenancePage` 死代码已移除。

## 今日完成
- `articles` 首页已收口为四个正式入口，去掉了 `最近任务` 和 `高级维护`
- `routeRegistry` 中的 `/articles/jobs`、`/articles/maintenance` 已移除
- `策略` 相关旧兼容页已从代码和路由中删除
- 回测页保留了候选版本轻入口，并新增了独立的候选版本子页面
- 候选子页面的错误态页面语义已收口
- `ArticleMaintenancePage` 已从源码中删除
- `Entry-Subentry-Matrix.md` 已同步更新
- `backtest/CandidatesPage.test.tsx`、`backtest/index.test.tsx`、`articles/index.test.tsx`、`route-registry.test.ts` 与 `typecheck` 已通过

## 验证结果
- `pnpm vitest run src/pages/backtest/index.test.tsx`
- `pnpm vitest run src/pages/articles/index.test.tsx`
- `pnpm typecheck`

## 发现的问题 / 风险
- `/strategies/*` 中的旧兼容页已删除，只有 `pre-market` 和 `after-close` 作为正式入口保留。
- `ArticleMaintenancePage` 已删除，文章兼容入口已全部收口。
- 候选能力不再单独暴露为旧工作台入口，但通过 `/backtest/candidates` 作为正式子页面保留。
- 当前代码里没有残留的 `ArticleMaintenancePage` 符号。

## Resume Point
Current Task: 候选版本能力独立化并完成最终验证
Blocked By: 无
Files:
- `docs/New-Web-Linked-TaskLists/Goal-Driven-Rebuild/TaskList.md`
- `web/src/features/backtest/backtest-center.tsx`
- `web/src/features/backtest/backtest-candidates-page.tsx`
- `web/src/features/strategy-workspace/strategy-workspace-candidate.tsx`
- `web/src/pages/articles/ArticlePipelinePage.tsx`
- `web/src/pages/backtest/CandidatesPage.tsx`
- `web/src/pages/backtest/CandidatesPage.test.tsx`
- `web/src/pages/backtest/index.test.tsx`
- `web/src/pages/articles/index.test.tsx`
Commands:
- `pnpm vitest run src/pages/backtest/CandidatesPage.test.tsx src/pages/backtest/index.test.tsx src/app/route-registry.test.ts`
- `pnpm eslint src/features/backtest/backtest-candidates-page.tsx src/features/backtest/backtest-center.tsx src/pages/backtest/index.test.tsx src/pages/backtest/CandidatesPage.test.tsx`
- `pnpm typecheck`
Pending Validation: 无
Next Action: 如需对外交付，可提交这轮候选子页面拆分修复并给出摘要

## 下一步（3-5 条，优先引用 ACTIVE_TASK_LIST 中的任务编号）
- 这次把 `candidates` 的实际能力收口成回测体系内的独立子页面
- 这次顺手清掉了候选区块的旧 strategy 语义和文章维护页死代码
- 相关测试和 `typecheck` 都已通过
- 当前已经满足最终交付口径

## 产物
- 兼容路由分层与状态条收口
- TaskList 兼容层口径补充

## 风险与注意事项
- `/strategies/versions`、`/strategies/candidates`、`/strategies/history`、`/strategies/regime-selection` 不应再作为正式入口回流
- 继续修改时不要把兼容层重新包装成正式入口
