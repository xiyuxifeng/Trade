# Stage 6 Code Review

> 日期：2026-04-27
> 范围：NTL-S6-001 ~ NTL-S6-013 全部实现
> 审查文件：src/backtest/、src/evaluation/metrics_calculator.py、cli/backtest.py
> 更新：2026-04-27 修复后状态

---

## 一、总体评价

**结论：设计目标基本达成，代码整体质量良好，88 项单元测试全部通过。**

主要优点：
- 架构正确，新建 `src/backtest/` 而非复用旧 `backtest_agent`
- A 股核心规则实现完整（T+1、100 股整数倍、ST 规则切换、新股无涨跌幅）
- 评分口径与 Stage 5 统一（`compute_mfe_mae_return` 直接复用）
- 模块边界清晰，依赖关系正确

---

## 二、问题修复状态

### 🔴 R1: `validate_rule_hits` 遍历 `version.rules_snapshot` 时未处理 None

**位置：** `engine.py:206`

**状态：✅ 已修复**

```python
# 修复前
for rule_dict in version.rules_snapshot:  # 可能为 None

# 修复后
for rule_dict in (version.rules_snapshot or []):
```

---

### 🔴 R2: 回测报告中百分比字段显示为小数

**位置：** `reporting.py:53`（per-record `return_pct`）、`reporting.py:98-99`（JSON `win_rate/avg_return_pct`）

**状态：✅ 已修复**

- `render_backtest_markdown`: per-record `return_pct` 由 `{rec.return_pct}` 改为 `f"{rec.return_pct:.2%}"`
- `render_backtest_json`: `win_rate` 和 `avg_return_pct` 字段改为百分比字符串 `f"{v:.2%}"`
- 跳过记录（`return_pct=None`）时输出 `None` 字符串，正确区分空值与零值

---

### 🟡 M1: `replay_candidates` 的 `market_context` 参数未使用

**位置：** `execution.py:22-26`

**状态：✅ 已改进**

在 docstring 中明确说明：该参数为未来扩展预留，当前实现仅做格式转换，不依赖快照中的市场数据。

---

### 🟡 M2: `_calc_t1_return` 在 bars 不连续时无诊断信息

**位置：** `engine.py:348-350`

**状态：✅ 已改进**

在 `RuleValidationResult.notes` 中增加 `t1_return_incomplete: {trade_date} {symbol}` 标注，当 T+1 收益无法计算时给出明确原因。

---

### 🟡 M3: 规则命中只按交易日计数，缺少标的粒度信息

**位置：** `engine.py:337,356`

**状态：✅ 已改进**

增加 `hit_symbols_per_day` 列表（`{trade_date}:{symbol}` 格式），在 `RuleValidationResult.notes` 中以 `hit_symbols: {date}:{symbol},...` 形式记录每次命中的具体标的，提供细粒度诊断信息。

---

### 🟡 M4: `SnapshotLoader.load_version_for_date` 异常时静默返回 None

**位置：** `snapshot_loader.py:176-177`

**状态：✅ 已改进**

```python
# 修复前
except Exception:
    return None

# 修复后
except Exception:
    warnings.warn(
        f"SnapshotLoader.load_version_for_date failed for trader={trader_id}, date={trade_date}: "
        "strategy_repo raised an exception. Returning None.",
        UserWarning,
    )
    return None
```

---

### 🟢 L1: `iter_trade_dates` 懒加载 akshare（设计决策，无需修复）

**说明：** 首次调用才触发 `TradeCalendar.load_from_akshare()` 是合理的设计，避免无意义的网络开销。生产环境可在启动时主动调用 `TradeCalendar.load_from_akshare()` 预热。

---

### 🟢 L2: `BacktestRequest.frozen=True` 与 `RuleValidationResult` 使用普通 dataclass（设计决策，无需修复）

**说明：** `RuleValidationResult` 需要在构造后填充字段（`trader_id` / `strategy_version_id` 由调用方传入），不能使用 `frozen=True`，是正确的设计选择。

---

### 🟢 L3: 缺少节假日跨越集成测试（已补充）

**状态：✅ 已补充**

新增 `TestIterTradeDatesWithHolidays` 测试类（4 项测试），覆盖：
- 清明假期（4/4-4/6）跨越场景
- 五一假期（5/1-5/5）跨越场景
- 引擎层面清明假期跳过验证
- 引擎层面五一假期跳过验证

---

## 三、与之前 Stage 的串联验证

| 串联点 | 实现 | 状态 |
|--------|------|------|
| `StrategyVersion.rules_snapshot` 持久化 | `repository.py:117` 写入、`service.py:137` 发布复制 | ✅ 正常 |
| `rules_snapshot` 类型为 `list[dict]` | `strategy_library/schemas.py:58` | ✅ 正常 |
| `compute_mfe_mae_return` 7返回值 | `metrics_calculator.py:275` | ✅ 正常 |
| `score_backtest_trade` 正确解包 | `backtest/scoring.py:46-63` | ✅ 正常 |
| `TradeConstraint` 扩展字段（market/trade_date/listing_date） | `metrics_calculator.py:18-49` | ✅ 正常 |
| `SnapshotLoader` 与 `strategy_repo` 接口 | `strategy_library/repository.py` | ✅ 正常（含 warning） |

---

## 四、A 股市场规则合规性检查

| 规则 | 设计要求 | 实现位置 | 状态 |
|------|----------|----------|------|
| 主板 10% 涨跌幅 | 参数化 | `metrics_calculator.py:122-123` | ✅ |
| 创业板/科创板 20% | 参数化 | `metrics_calculator.py:124` | ✅ |
| ST 股票按市场+日期切换 | 2026-07-06 沪市 ST 调为 10% | `metrics_calculator.py:114-120` | ✅ |
| 新股前 5 日无涨跌幅 | `listing_date` 自动计算 | `metrics_calculator.py:161-173` | ✅ |
| T+1 约束 | 当日买入不能卖出 | `metrics_calculator.py:368-375` | ✅ |
| 100 股整数倍 | 买入校验 | `engine.py:543-546` | ✅ |

---

## 五、测试覆盖分析

| 测试文件 | 测试数 | 覆盖内容 | 状态 |
|----------|--------|----------|------|
| test_engine.py | 19 | 引擎编排、交易日迭代、规则验真、节假日跨越 | ✅ |
| test_execution.py | 6 | 重放、gap检测 | ✅ |
| test_schemas.py | 11 | 请求校验、字段定义 | ✅ |
| test_scoring.py | 21 | scoring适配、ST规则切换 | ✅ |
| test_reporting.py | 9 | 报告渲染（含百分比格式化） | ✅ |
| test_rule_registry.py | 8 | 规则分类 | ✅ |
| test_snapshot_loader.py | 10 | 快照加载、兜底标记、异常warning | ✅ |
| test_reproducibility.py | 1 | 复现校验 | ✅ |
| **合计** | **88** | - | **✅ 全部通过** |

---

## 六、结论

Stage 6 所有审查问题（R1、R2、M1-M4、L3）均已修复或改进，88 项单元测试全部通过。

剩余 L1（懒加载 akshare）和 L2（frozen dataclass 设计）是合理的设计决策，无需修复。

---

## Appendix: 关键文件清单

| 文件 | 职责 | 行数 |
|------|------|------|
| `src/backtest/schemas.py` | 请求/结果数据结构 | ~227 |
| `src/backtest/engine.py` | 引擎编排+规则验真 | ~650 |
| `src/backtest/scoring.py` | 评分适配层 | ~64 |
| `src/backtest/execution.py` | 策略重放 | ~65 |
| `src/backtest/snapshot_loader.py` | 快照加载器 | ~180 |
| `src/backtest/rule_registry.py` | 规则白名单 | ~113 |
| `src/backtest/reporting.py` | 报告渲染 | ~160 |
| `src/evaluation/metrics_calculator.py` | 核心评分+交易约束 | ~405 |
| `cli/backtest.py` | CLI入口 | ~266 |
