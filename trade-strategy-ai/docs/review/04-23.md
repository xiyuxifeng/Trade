# Code Review 报告 - 2026-04-23

本文档基于 `docs/TaskList.md` 对 `trade-strategy-ai` 项目当前已完成的任务进行代码审查。

## 1. 整体评估

项目已高质量完成 **Stage 0** 到 **Stage 3** 的所有任务，整体进度符合规划。核心主线 `数据快照 -> provider -> 市场候选池 -> per-trader 策略版本` 已基本贯通，为后续的盘前决策、盘后评估和回测功能奠定了坚实的基础。

## 2. 任务完成度与代码实现确认

| Stage | 状态 | 核心实现确认 |
| :--- | :--- | :--- |
| **Stage 0: 统一主线与数据底座** | ✅ 已完成 | 文档入口已收敛，`kaipan` 数据抓取、标准化、快照流程已在 `src/providers/kaipan_scheduler.py` 中实现，数据来源可追溯。 |
| **Stage 1: 配置、契约、模型与迁移** | ✅ 已完成 | 新的配置、数据库模型和迁移脚本已全部实现并包含测试。基础设施为后续开发做好了准备。 |
| **Stage 1.5: Agent 边界收敛** | ✅ 已完成 | `src/agents/` 目录下的各 Agent 职责边界已通过文档字符串和注释明确，旧的开发路径已被冻结。 |
| **Stage 2: Provider 抽象与市场候选池** | ✅ 已完成 | 实现了可扩展的 Provider 架构和 `DataAgent` 能力路由。`src/market_universe/` 模块可每日生成市场候选池快照。 |
| **Stage 3: 按 trader 版本化策略库** | ✅ 已完成 | `src/strategy_library/` 模块功能完整，可按 Trader 生成、管理和查询独立的策略版本。`TraderProfile` 已增强。 |

## 3. 潜在问题与优化建议

| 优先级 | 问题/建议 | 影响范围 | 解决方案 |
| :--- | :--- | :--- | :--- |
| **高** | **硬编码敏感信息** | `src/providers/kaipan_scheduler.py` | 将 `user_id` 和 `token` 等移至环境变量或安全的配置文件中，通过 `KaipanConfig` 加载。 |
| **中** | **指标计算能力缺失** | `DataAgent` 数据能力 | 尽快完成 `NTL-S2-017` 任务，实现指标拉取 skill，为策略提供更丰富的决策维度。 |
| **低** | **Schema 定义分散** | `src/providers/` | 建议创建 `src/providers/kaipan_schema/` 目录，将数据标准化所用的 YAML schema 文件进行显式管理。 |
| **低** | **核心业务契约中心化** | `src/schemas/` | 考虑将跨模块复用的核心数据契约（如 `TradeIdea`）统一到 `src/schemas/contracts.py`，便于维护。 |

## 4. 项目完成度评估

项目目前完成了核心架构和关键业务模块的搭建，**总体完成度约为 60%**。下一阶段的重点是打通完整的交易决策与学习闭环。

- **已完成**:
    - 稳定的数据输入与快照机制。
    - 可扩展的 Provider 和 DataAgent 架构。
    - 支持多 Trader、版本化的策略生成体系。
- **待完成**:
    - 基于新架构的盘前决策主链路。
    - 盘后评估、归因与学习闭环。
    - 离线回测与规则验真能力。
    - 自主优化与可观测性。

## 5. 文档缺失情况

1.  **`Project.md`**: 对 Agent 状态的描述可以链接到 `TaskList.md` 中对应的 `NTL-S15-XXX` 任务，以增强可追溯性。

## 6. 文档缺失处理意见

| 项目 | 处理意见 | 优先级 |
| :--- | :--- | :--- |
| **硬编码敏感信息**（高） | 需先建立 `kaipan_secrets.json` 或环境变量加载机制，再迁移 `user_id`/`token`。属于 Stage 4+ 安全加固项，当前阶段可接受 | 中（等安全基础设施就绪） |
| **指标计算能力缺失**（中） | ✅ **2026-04-23 本次会话已修复**：补充了 `DataAgent.handle` 中 `fetch_indicators` 路由分支，NTL-S2-017 路由已贯通 | 已完成 |
| **Schema 定义分散**（低） | Stage 4+ 稳定后统一治理，当前不处理 | 低 |
| **核心业务契约中心化**（低） | Stage 4+ 稳定后评估是否值得迁移，当前不处理 | 低 |
| **Project.md Agent 状态链接** | 建议在 Stage 4 完成 `ManagerAgent`/`TraderAgent` 重构后，一并补充可追溯链接 | 低 |

**结论**：无阻塞性问题。建议按 Section 6 立即推进 Stage 4。
