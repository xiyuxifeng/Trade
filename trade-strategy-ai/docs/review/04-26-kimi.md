# Stage 6 任务完成情况 Review

> Reviewer: Kimi  
> 日期: 2026-04-26  
> 范围: `NTL-S6-001` ~ `NTL-S6-013`  
> 代码基线: `src/backtest/`、`cli/backtest.py`、`src/evaluation/metrics_calculator.py` 及相关测试

---

## 1. 总体结论

Stage 6 的 **13 项任务在 TaskList 中已全部标记为 `[x]`**，代码骨架和单元测试均已落地。核心链路（schema → execution → scoring → engine → reporting → CLI → rule_registry → reproducibility）已打通，**与 Stage 5 的 scoring 口径实现了逻辑单源复用**，ST 规则日期切换、旧 backtest_agent 冻结等关键设计点也已实现。

但当前代码仍处于**"骨架完成、细节待填"**的状态：部分核心功能存在实现简化、类型不一致、A股交易制度覆盖不全等问题，距离设计文档中的"完整回测"和"规则验真"目标仍有差距。

---

## 2. 代码是否符合设计目标（按任务逐项 Review）

| 任务 | 设计目标 | 实现状态 | 评估 |
|------|----------|----------|------|
| `NTL-S6-001` schema | 统一输入输出契约，Pydantic BaseModel | 使用了 `dataclass(frozen=True)`，非 Pydantic | ⚠️ 结构正确但选型偏离设计 |
| `NTL-S6-002` execution | 策略版本重放，检测 rules_snapshot 缺口 | `replay_candidates` + `classify_rules_snapshot_gap` 已实现 | ✅ 符合 |
| `NTL-S6-003` scoring | 复用 Stage 5 口径，不复制公式 | 正确导入 `compute_mfe_mae_return`，ST 规则切换已落地 | ✅ 符合 |
| `NTL-S6-004` engine | 编排 loader/replayer/scoring，区间回测 | 引擎骨架完成，但**每日只处理首个 candidate** | ⚠️ 功能不完整 |
| `NTL-S6-005` reporting | Markdown/JSON 报告 | 三种渲染函数均已实现 | ✅ 符合 |
| `NTL-S6-006` snapshot_loader | 只读快照，禁止实时 provider，兼容兜底 | `SnapshotLoader` 已实现，`compatibility_fallback` 有标记 | ✅ 符合（待生产环境验证） |
| `NTL-S6-007` scoring 口径统一 | 线上线下同输入同结果 | `test_scoring_alignment.py` 已验证 | ✅ 符合 |
| `NTL-S6-008` CLI | `backtest run/report/validate-rules/reproducibility-check` | 四个子命令均已注册 | ⚠️ `run` 未注入实际 loader，只能跑空 |
| `NTL-S6-009` rule_registry | 规则可程序化分类 | `classify_rule` 已实现 | ⚠️ 实现过于简化（见第3节） |
| `NTL-S6-010` 规则命中验证 | hit/miss 统计 | `validate_rule_hits` 已实现 | ⚠️ 实际只做了字段存在性检查 |
| `NTL-S6-011` 规则覆盖率报告 | 覆盖率、命中率、后验收益 | 报告模板有字段，**后验收益未计算** | ❌ 数据层缺失 |
| `NTL-S6-012` 冻结旧 agent | 旧目录标记为历史 | `src/agents/backtest_agent/__init__.py` + `Project.md` 已更新 | ✅ 符合 |
| `NTL-S6-013` 复现验证 | 同输入同结果 | `fingerprint_result` 已实现 | ✅ 符合 |

---

## 3. 代码缺陷与设计缺陷

### 3.1 严重缺陷（影响功能正确性）

#### D1: `BacktestEngine` 每日仅处理首个 candidate（功能缺失）

```python
# src/backtest/engine.py:249
candidates = replay_candidates(strategy_version, market_context)
# ...
candidate = candidates[0]   # 只取第一个！
```

**问题**: 一个 `StrategyVersion` 通常包含多条 `recommendations`（多标的建议），但引擎每天只生成**一条** `BacktestTradeRecord`，其余 candidate 被静默丢弃。这与设计文档中"完整回测"的目标严重不符。

**建议**: `_process_single_day` 应遍历所有 candidates，为每条 recommendation 生成独立的 `BacktestTradeRecord`，并汇总到当日结果中。

---

#### D2: `RuleValidationResult` 为 frozen dataclass，但代码试图修改其字段

```python
# src/backtest/engine.py:83-84
validation_result.trader_id = trader_id
validation_result.strategy_version_id = version_id
```

**问题**: `RuleValidationResult` 定义为 `@dataclass(frozen=True)`，上述赋值会在运行期抛出 `FrozenInstanceError`。当前单元测试未覆盖 `validate_rules_for_trader` 的完整路径，因此该 bug 未暴露。

**建议**: 取消 `frozen=True`，或在构造 `RuleValidationResult` 时一次性传入所有字段。

---

#### D3: `BacktestTradeRecord.entry_date/exit_date` 类型与赋值不匹配

```python
# src/backtest/schemas.py:70-71
entry_date: date | None = None
exit_date: date | None = None

# src/backtest/engine.py:294-295
entry_date=trade_date.isoformat(),   # str!
exit_date=exit_date_str,             # str!
```

**问题**: schema 声明为 `date | None`，但 engine 赋值为 `str`。这会导致序列化/反序列化混乱，也与 `BacktestRequest` 中使用 `date` 类型不一致。

**建议**: 统一将 `entry_date`/`exit_date` 改为 `str`（与 scoring 返回值保持一致），或在 engine 层转换为 `date` 对象后再赋值。

---

### 3.2 中等缺陷（影响健壮性或准确性）

#### D4: `validate_rule_hits` 仅做字段存在性检查，未做真正的规则条件判断

```python
# src/backtest/engine.py:361-364
for field in rule_meta.required_fields:
    if field in symbol_indicators:
        found = True
        break
```

**问题**: 规则 "rsi < 30" 被标记为命中，只要 `indicators` 中有 `rsi` 字段即可，**完全不检查数值是否小于 30**。这导致 hit/miss 统计失去意义。

**建议**: 对 `fully_programmable` 规则，至少实现简单的数值比较（如 `rsi < 30`、`macd > 0`）。对于无法解析的复杂条件，应降级为 `partially_programmable`。

---

#### D5: `cli/backtest.py` 的 `run_backtest` 未注入实际依赖

```python
# cli/backtest.py:57
engine = BacktestEngine()
```

**问题**: `BacktestEngine()` 未传入 `loader` 和 `strategy_loader`，运行后所有记录都会是 `skipped` 状态。CLI 虽能跑通，但无实际业务价值。

**建议**: 在 CLI 中根据配置初始化 `SnapshotLoader`（绑定 `snapshot_service` 和 `strategy_repo`），再注入引擎。

---

#### D6: `is_trade_date` 未考虑 A 股法定节假日

```python
# src/backtest/engine.py:91-93
def is_trade_date(d: date) -> bool:
    return d.weekday() < 5
```

**问题**: 仅跳过周末，未跳过春节、国庆、元旦等 A 股法定节假日。这会导致回测在休市日产生无效记录。

**建议**: 接入 `akshare` 交易日历或本地交易日历缓存，替换简单的 `weekday < 5` 判断。

---

#### D7: `classify_rule` 的正则匹配过于宽泛，易误判

```python
# src/backtest/rule_registry.py:34
"ma": re.compile(r"(?<![a-zA-Z])ma\d*(?![a-zA-Z0-9])|(?<![a-zA-Z])ema\d*(?![a-zA-Z0-9])", re.IGNORECASE)
```

**问题**: `"market"` 中的 `"ma"` 会被匹配为均线指标；`"max_volume"` 也可能被误匹配。这会导致纯描述性规则被错误分类为 `fully_programmable`。

**建议**: 收紧正则边界，或在匹配后增加白名单/黑名单校验。

---

#### D8: `RuleValidationResult.posterior_return_*` 永远为 `None`

```python
# src/backtest/engine.py:validate_rule_hits()
# 完全没有计算 posterior_return_mean / posterior_return_median 的逻辑
```

**问题**: `NTL-S6-011` 要求输出"命中后验收益分布"，但 `validate_rule_hits` 和 `validate_rules_for_trader` 均未计算该值。`reporting.py` 的模板虽有显示逻辑，但数据永远为空。

**建议**: 在 `validate_rule_hits` 中，对 hit 的样本计算后续 N 日收益（如 T+1/T+3/T+5），并填充 posterior 字段。

---

### 3.3 轻微缺陷（代码质量/风格）

#### D9: `SnapshotLoader.load_market_context` 中变量重复定义

```python
# src/backtest/snapshot_loader.py:64,78,95
bars_by_symbol: dict[str, list[dict]] = {}    # 第64行
bars_by_symbol: dict[str, list[dict]] = {}    # 第78行（在 try 块内重复定义）
```

**问题**: 同一函数内同名变量多次注解赋值，虽不影响运行，但降低可读性。

**建议**: 统一初始化，避免重复声明。

---

#### D10: `BacktestEngine.run_sync` 在已有 event loop 中调用会失败

```python
def run_sync(self, request: BacktestRequest) -> BacktestResult:
    return asyncio.run(self.run(request))
```

**问题**: 若已在异步环境（如 Jupyter、async CLI）中调用 `run_sync`，`asyncio.run` 会抛出 `RuntimeError`。

**建议**: 提供纯同步接口，或使用 `asyncio.get_event_loop().run_until_complete()` 做兼容处理。

---

## 4. 与之前 Stage 的串联情况

### 4.1 串联良好的部分

| 上游 Stage | 串联点 | 状态 |
|------------|--------|------|
| Stage 3 (`strategy_library`) | `execution.py` 直接使用 `StrategyVersion`、`StrategyRecommendation` | ✅ 正常 |
| Stage 5 (`evaluation`) | `scoring.py` 导入 `compute_mfe_mae_return`，`TradeConstraint` 已扩展 | ✅ 正常 |
| Stage 2/4 (快照) | `SnapshotLoader` 接口兼容 `snapshot_service.load()` 和 `strategy_repo.get_released_by_trader_and_date()` | ✅ 正常 |
| CLI 主入口 | `cli/main.py` 已注册 `backtest_app` | ✅ 正常 |
| 旧 agent 冻结 | `src/agents/backtest_agent/__init__.py` 已标注历史状态 | ✅ 正常 |

### 4.2 串联问题

#### C1: `MarketContextSnapshot` 未定义为正式类型

设计文档中定义了 `MarketContextSnapshot(TypedDict)`，但代码中 `SnapshotLoader.load_market_context` 返回的是普通 `dict[str, Any]`。这导致：
- 类型检查器（如 mypy）无法约束字段访问
- 下游模块（`engine`、`execution`）对 context 的字段访问无类型保障

**建议**: 将 `MarketContextSnapshot` 实现为 `TypedDict` 或 `dataclass`，替换所有 `dict[str, Any]` 用法。

---

#### C2: `BacktestRequest` 选型为 dataclass 而非 Pydantic

设计文档明确要求使用 Pydantic BaseModel（用于请求校验、JSON 序列化、字段约束），但实现使用了标准库 `dataclass`。这导致：
- 缺少自动校验（如 `date_from <= date_to`）
- CLI 层需要手动解析和校验参数

**建议**: 若后续需要接入 FastAPI 或更复杂的 CLI 校验，应迁移至 Pydantic v2。

---

#### C3: `StrategyVersion.rules_snapshot` 的 `list[dict]` 类型过于松散

`rules_snapshot` 在 Stage 3 中定义为 `list[dict]`，Stage 6 的 `rule_registry.py` 需要从中提取 `rule_id`、`condition`、`text` 等字段。由于 dict 结构无约束，字段缺失时 `classify_rule` 只能使用 `.get()` 兜底。

**建议**: 在 Stage 3 或 Stage 6 中定义 `RuleSnapshot` 的正式 schema（至少包含 `rule_id` 和 `condition`），并做版本兼容性处理。

---

## 5. A 股市场实际情况符合度

### 5.1 已正确实现的规则

| 规则项 | 实现状态 | 代码位置 |
|--------|----------|----------|
| 主板涨跌幅 10% | ✅ | `metrics_calculator.py:_get_limit_pct` |
| 科创板/创业板 20% | ✅ | `metrics_calculator.py:_get_limit_pct` |
| ST 股票 5% / 10% 日期切换 | ✅ | `metrics_calculator.py:_get_limit_pct` + `_resolve_constraint` |
| T+1（买入当日不能卖出） | ✅ | `metrics_calculator.py:compute_mfe_mae_return`（entry_date 跳过 exit 检查） |
| 涨跌停价格约束 | ✅ | `metrics_calculator.py:effective_high / effective_low` |
| 停牌识别 | ✅ | `metrics_calculator.py:_is_bar_halted` |
| 北交所 30% | ✅（预留） | `metrics_calculator.py:_get_limit_pct` |

### 5.2 未实现或实现不全的规则

| 规则项 | 设计要求 | 当前状态 | 风险 |
|--------|----------|----------|------|
| **最小交易单位 100 股** | 买入按 100 股整数倍 | ❌ 完全未实现 | 回测模拟交易量失真 |
| **零股处理** | 残股可一次性卖出 | ❌ 完全未实现 | 持仓清算逻辑缺失 |
| **新股前 5 日无涨跌幅** | 科创板/创业板/主板新股上市前 5 日 | ❌ 完全未实现 | 新股回测收益率偏差大 |
| **法定节假日** | 跳过春节、国庆等休市日 | ❌ 仅跳过周末 | 回测覆盖非交易日 |

### 5.3 ST 规则切换验证

测试用例已覆盖以下边界：
- `2026-07-05` + 沪市 ST → limit = 5% ✅
- `2026-07-06` + 沪市 ST → limit = 10% ✅
- `2026-07-06` + 深市 ST → limit = 5% ✅

该实现符合上交所 2026-04-24 发布的修订规则。

---

## 6. 需要改进的地方（按优先级排序）

### P0（阻塞正常使用）

1. **修复 `BacktestEngine` 每日只处理首个 candidate 的问题**（D1）
   - `_process_single_day` 应返回 `list[BacktestTradeRecord]`，或 engine 层遍历 candidates
2. **修复 `RuleValidationResult` frozen 实例被修改的问题**（D2）
   - 取消 `frozen=True` 或调整构造方式
3. **修复 `entry_date/exit_date` 类型不匹配**（D3）
   - 统一为 `str` 或 `date`

### P1（影响回测质量）

4. **实现真正的规则条件判断**（D4）
   - 对 `fully_programmable` 规则解析简单数值表达式（如 `rsi < 30`）
5. **接入 A 股交易日历，跳过法定节假日**（D6）
   - 可用 `akshare.tool_trade_date_hist_sina()` 或本地缓存
6. **为 CLI `run_backtest` 注入实际依赖**（D5）
   - 初始化 `SnapshotLoader` 并绑定服务
7. **计算规则后验收益**（D8）
   - 在 `validate_rule_hits` 中对 hit 样本计算 T+1/T+3/T+5 收益

### P2（提升健壮性和可维护性）

8. **定义正式的 `MarketContextSnapshot` 类型**（C1）
9. **收紧 `classify_rule` 的正则匹配**（D7）
10. **实现最小交易单位和零股处理**（A股规则完整性）
11. **实现新股上市前 5 日无涨跌幅限制**
12. **将 `BacktestRequest` 迁移至 Pydantic**（C2，若后续需要 API 接入）
13. **为 `run_sync` 提供 event loop 兼容**（D10）

---

## 7. 测试覆盖情况

| 测试文件 | 测试数 | 覆盖内容 | 不足 |
|----------|--------|----------|------|
| `test_schemas.py` | 13 | Request/Record/Summary/Result/RuleValidationResult | 未覆盖非法日期区间校验 |
| `test_execution.py` | 6 | replay_candidates / classify_rules_snapshot_gap | 未覆盖 rules_snapshot 为空时的降级路径 |
| `test_scoring.py` | 12 | score_backtest_trade / ST 规则切换 | 未覆盖 T+1 约束对 scoring 的影响 |
| `test_engine.py` | 10 | Engine 编排 / iter_trade_dates / validate_rule_hits | **未覆盖多 candidate 场景**；未覆盖 `validate_rules_for_trader` 完整路径 |
| `test_reporting.py` | 8 | Markdown/JSON/规则报告渲染 | 未覆盖空结果边界 |
| `test_snapshot_loader.py` | 9 | SnapshotLoader / compatibility_fallback | 未覆盖 EvidencePack 兜底路径 |
| `test_rule_registry.py` | 11 | classify_rule / 指标模式匹配 | 未覆盖误判场景（如 "market" 误匹配 "ma"） |
| `test_reproducibility.py` | 1 | fingerprint_result | 未覆盖浮点 rounding 差异场景 |
| `test_scoring_alignment.py` | 3 | 线上线下 scoring 一致性 | 覆盖充分 |

**总体评价**: 单元测试覆盖骨架完整，但缺少**集成测试**（如"完整的多日多标的回测链路"）和**边界测试**（如 frozen dataclass 修改、法定节假日跨越等）。

---

## 8. 总结

Stage 6 的**架构骨架和模块划分是合理的**，与 Stage 2/3/5 的数据资产衔接路径清晰，ST 规则日期切换等 A 股特色需求也已落地。但代码在以下三方面需要补强：

1. **功能完整性**: 每日多 candidate 处理、规则真值判断、后验收益计算、法定节假日过滤等核心功能尚未实现或过于简化。
2. **类型与健壮性**: `frozen dataclass` 修改、日期类型不匹配、event loop 嵌套、正则误判等问题需要在生产化前修复。
3. **A 股规则覆盖**: 最小交易单位、零股、新股特殊涨跌幅等交易制度约束尚未建模，回测结果与真实交易存在偏差。

**建议下一步**: 优先修复 P0 缺陷（D1~D3），然后补齐 P1 功能（D4~D8），使 Stage 6 从"骨架可用"推进到"可运行真实回测"。
