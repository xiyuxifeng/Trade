# Stage 7 Code Review — 2026-04-30

## 一、总体评价

**Stage 7（S7-001 ~ S7-011）整体完成度：高**

所有 11 个子任务已标记完成，代码已合入 main 分支。架构设计合理，双链路（文件/DB）分离清晰，告警系统可插拔扩展。主要问题集中在细粒度 bug 和前后链路兼容性。

---

## 二、各模块 Review

### S7-001 活跃 Trader 筛选（ActiveTraderFilter）

**设计目标：** 贝叶斯收缩胜率 + 样本置信度综合评分，筛选活跃 trader

**代码符合度：** ✅ 已修复所有问题

**已修复缺陷：**

1. **`rule_validations` 参数已实现** ✅
   - `filter()` 新增 `rule_validations: dict[str, list[RuleValidationResult]] | None` 参数
   - `rule_quality` 字段正确填充（rule_id → hit_rate）
   - `min_rule_hit_rate` 门槛过滤已实现，满足条件的 trader 被标记到 `fail_reasons`

2. **所有 trader valid_trades=0 时发出警告** ✅
   - `filter()` 末尾增加检查：若所有 trader 有效交易数均为 0，输出 `logger.warning` 提示
   - 调用方可以区分"无数据"和"全部未通过"

**与 A 股市场适配性：** ✅ 贝叶斯收缩对样本稀少的 A 股散户交易者有实际意义，0.5 baseline 合理

---

### S7-002 策略调整建议（StrategyAdvisor）

**设计目标：** 基于规则验真结果生成 5 类调整建议

**代码符合度：** ✅ 已修复所有问题

**已修复缺陷：**

1. **键名拼写不一致** ✅ 已修复
   - `candidate_builder.py:29` 已修正为 `"programmable_but_rarely_hit"`，与 S7-002/S7-004 保持一致

2. **suggestion 格式化负数处理** ✅ 已修复
   - 新增 `_format_return_mean()` 辅助函数
   - 负数显示为「亏损 X%」，正数显示为「+X%」
   - 模板 `{return_mean:.2%}` 替换为 `{return_mean}` 占位符

**与 A 股市场适配性：** ✅ 5 类调整规则覆盖了 A 股实际场景（规则命中率低但收益正说明未被充分利用，missed_opportunity 逻辑合理）

---

### S7-003 候选版本构建（文件链路）

**设计目标：** 将 S7-002 的 RuleAdjustment[] 映射为修改后的 rules_snapshot

**代码符合度：** ✅ 符合设计，`_OPERATION_MAP` 逻辑清晰，继承自 S7-002 的拼写 bug 已修复

**无明显缺陷**

---

### S7-003b 候选版本 DB 链路

**设计目标：** `--db` flag 将候选版本写入 PostgreSQL

**代码符合度：** ✅ 双链路完全独立，Repository 映射完整，`_get_version_type()` 旧记录兼容测试已覆盖

**特别说明：** `RuleAdjustment → StrategyAdjustment` 转换函数中 `依据` 字段拼接 `hit_rate` 和 `rule_text`，内容较长但可接受

---

### S7-004 滚动评估窗口（RollingEvaluator）

**设计目标：** 按交易日窗口判断信号稳定性，避免单日噪声

**代码符合度：** ✅ 已修复所有问题

**已修复缺陷：**

1. **`_is_trading_day()` 默认值** ✅ 已修复
   - 未注入 `trading_days` 时 fallback 到 `TradeCalendar.is_trade_date()` 判断是否为交易日

2. **`observation_date` 默认值来源** ✅ 已修复
   - `RuleAdjustment` 新增 `trade_date: date | None` 字段
   - `push_adjustment()` 默认优先级: explicit `observation_date` > `adjustment.trade_date` > `Date.today()`

**与 A 股市场适配性：** ✅ 交易日过滤逻辑（节假日/周末）对 A 股市场正确

---

### S7-005 API 扩展

**设计目标：** 4 个新 router（strategy_versions / snapshots / rankings / backtest_results）

**代码符合度：** ✅ 完全符合设计

**架构亮点：**
- 分页响应结构统一
- 下载端点正确返回 `FileResponse`
- 所有 router 都正确注册到 `api/main.py`

**无明显缺陷**

---

### S7-006 CLI 扩展

**设计目标：** `snapshot build` + `strategy build/list`

**代码符合度：** ✅ 完全符合设计

**无明显缺陷**

---

### S7-007 告警系统

**设计目标：** 8 种告警规则（A-H）+ 4 种渠道 + 聚合 + DB + 结构化日志

**代码符合度：** ✅ 已修复所有问题

**已修复缺陷：**

1. **DingTalk/Feishu/Wecom Webhook 实现** ✅ 已验证
   - 这些模块是格式化器（`ChannelFormatter`），不负责发送
   - 实际发送由 `WebhookNotifier`（`notifiers.py`）使用 stdlib `urllib.request` 完成
   - 无需 `requests` 外部依赖，实现正确

2. **`alert.log` 目录未自动创建** ✅ 已修复
   - `logger_.py:23` 已有 `mkdir(parents=True, exist_ok=True)`

3. **API `/alerts/history` 未返回 total** ✅ 已修复
   - `AlertHistoryRepository.count_history()` 新增
   - `PaginatedAlertHistory.total` 字段已添加

4. **`send_test_alert()` 类型修复** ✅ 已修复
   - `AppConfig` 新增 `alerting: dict[str, Any] | None` 字段
   - `send_test_alert()` 改为传入 `loaded.config.alerting`

**与 A 股市场适配性：** ✅ 8 种告警维度覆盖全面

---

### S7-008 集成测试

**设计目标：** 盘前/盘后/回测三大链路集成测试

**代码符合度：** ✅ 完全符合，新增 17 个测试覆盖三大链路

**亮点：**
- 所有测试正确 mock DB 依赖（`session_scope` + `TraderMemoryStore`）
- 交易日使用真实交易日历（清明节后 2026-04-07 作为交易日）
- E2E 冒烟测试覆盖盘前→盘后串联

---

### S7-009 BacktestResult 版本字段

**代码符合度：** ✅ 完全符合

**无明显缺陷**

---

### S7-010 akshare 懒加载 fallback

**代码符合度：** ✅ 完全符合

**亮点：**
- `TradeCalendar.load_from_file()` 自动检测年份并刷新
- 3 次重试 + 失败告警逻辑完善

---

### S7-011 RuleValidationResult.notes 截断

**代码符合度：** ✅ 完全符合

**无明显缺陷**

---

## 三、链路串联 Review（跨 Stage）

### 完整链路：S7-002 → S7-004 → S7-003/003b

```
StrategyAdvisor.advise()
  ↓ (输出 RuleAdjustment[])
RollingEvaluator.push_adjustment()
  ↓ (判断是否应触发)
  ↓ (S7-004 输出 AdjustmentTrigger)
optimize create-candidate --adjustments xxx.json [--db]
  ↓ (RuleAdjustment[] → rules_snapshot 修改)
  ↓ (文件链路: candidate_builder.build_candidate_version()
  ↓ (DB 链路: StrategyLibraryService.create_candidate_version())
  ↓ (输出候选版本 JSON 或写入 DB)
```

**发现的问题：**

1. **S7-002 → S7-004 传递链断在 `RollingEvaluator`** ✅ 已修复
   - `RuleAdjustment` 新增 `trade_date: date | None` 字段
   - `push_adjustment()` 默认优先级: explicit `observation_date` > `adjustment.trade_date` > `Date.today()`
   - S7-002 生成 `RuleAdjustment` 时可携带 `trade_date`，S7-004 窗口计算不再受 `today()` 硬编码影响

2. **S7-003b DB 链路缺少对 `_get_version_type` 旧记录兼容的测试** ✅ 已完成
   - `tests/unit/strategy_library/test_repository_s7_003b.py` 已覆盖 4 个 `_get_version_type` 边界场景

### OHLCV 数据链路：S7-000（数据抓取）→ ManagerAgent（使用）

```
ohlcv crawl --mode incremental
  ↓ (写入 ohlcv_bars 表)
ManagerAgent.run_pre_market()
  ↓ (_load_market_state_from_db() 从 ohlcv_bars 加载 market_state)
  ↓ (classify_market_state() → MarketState)
```

**符合度：** ✅ 数据流正确

---

## 四、总体风险评估

| 风险类型 | 严重程度 | 说明 |
|----------|----------|------|
| **S7-002/S7-003 拼写不一致** | 🟢 已修复 | `candidate_builder.py:29` 已修正为 `programmable_but_rarely_hit` |
| **S7-004 observation_date 硬编码** | 🟢 已修复 | `push_adjustment()` 新增 `observation_date` 参数 |
| **S7-007 alert.log 目录未创建** | 🟢 已修复 | `logger_.py:23` 已有 `mkdir(parents=True, exist_ok=True)` |
| **S7-007 API history 缺 total** | 🟢 已修复 | 新增 `count_history()`，`PaginatedAlertHistory` 返回 `total` |
| **S7-004 _is_trading_day 默认值** | 🟢 已修复 | 未注入时 fallback 到 `TradeCalendar.is_trade_date()` |
| **S7-002 suggestion 格式化** | 🟢 已修复 | `_format_return_mean()` 负数显示为「亏损 X%」|
| **S7-001 rule_validations 未实现** | 🟢 已修复 | `filter()` 支持 `rule_validations` 参数，`rule_quality` 已填充 |

---

## 五、改进优先级

### P0（立即修复，影响链路正确性）

1. ~~**S7-002/S7-003 拼写统一**~~ ✅ 已修复
   - `candidate_builder.py` 第 29 行已改为 `"programmable_but_rarely_hit"`
   - 测试用例同步更新

2. ~~**S7-004 observation_date 修复**~~ ✅ 已修复
   - `RuleAdjustment` 新增 `trade_date` 字段
   - `push_adjustment()` 默认优先级: explicit > `adjustment.trade_date` > `Date.today()`

### P1（下一迭代修复）

3. ~~**S7-007 alert.log 目录自动创建**~~ 🟢 已修复
   - `logger_.py:23` 已有 `mkdir(parents=True, exist_ok=True)`
4. ~~**S7-007 API history 返回 total**~~ 🟢 已修复
   - `AlertHistoryRepository.count_history()` 新增
   - `PaginatedAlertHistory.total` 字段已添加
5. ~~**S7-004 `_is_trading_day()` 未注入时 fallback 到 `TradeCalendar`**~~ 🟢 已修复
   - `rolling_evaluator.py:91-96` fallback 到 `TradeCalendar.is_trade_date()`

### P2（优化项）

6. ~~**S7-002 suggestion 格式化优化**~~ 🟢 已修复
   - `strategy_advisor.py` 新增 `_format_return_mean()` 辅助函数
   - 负数显示为「亏损 X%」，正数显示为「+X%」
   - 模板 `{return_mean:.2%}` 替换为 `{return_mean}` 占位符
7. ~~**S7-001 `rule_validations` 参数实现规则质量过滤**~~ 🟢 已修复
   - `filter()` 新增 `rule_validations` 参数
   - `rule_quality` 字段正确填充（rule_id → hit_rate）
   - `min_rule_hit_rate` 门槛过滤已实现并测试
8. ~~**S7-003b DB 链路补充 `_get_version_type()` 旧记录兼容测试**~~ 🟢 已有测试
   - `tests/unit/strategy_library/test_repository_s7_003b.py` 已覆盖 `_get_version_type()` 旧记录兼容

---

## 六、结论

Stage 7 整体完成质量较高，架构设计合理，双链路分离清晰。**所有 P0/P1/P2 问题均已修复**，无遗留严重缺陷。

**修复汇总：**
- P0: S7-002/S7-003 拼写统一、observation_date 硬编码修复
- P1: alert.log 目录创建、API history total、`_is_trading_day` fallback
- P2: suggestion 格式化、rule_validations 实现、`_get_version_type` 测试覆盖

**测试状态：129 tests PASS**
