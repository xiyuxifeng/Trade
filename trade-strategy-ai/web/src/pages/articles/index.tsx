import { PlaceholderPage } from '@/components/layout/placeholder-page';

export function ArticlesPage() {
  return (
    <PlaceholderPage
      description="文章处理链路的 V1 验收入口。"
      note="文章页面将在后续阶段接入 article_pipeline 的正式工作流与表单。"
      title="Articles"
    />
  );
}
