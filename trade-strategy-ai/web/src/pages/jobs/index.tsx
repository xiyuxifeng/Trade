import { PlaceholderPage } from '@/components/layout/placeholder-page';

export function JobsPage() {
  return (
    <PlaceholderPage
      description="Job Center entry point for long-running task tracking."
      note="The operational job list and detail drawer will land in WEB-S4-006."
      title="Jobs"
    />
  );
}
