import { PlaceholderPage } from '@/components/layout/placeholder-page';

export function WorkflowsPage() {
  return (
    <PlaceholderPage
      description="Workflow entry point for guided UserManual operations."
      note="The workflow builder and parameter form will be connected in WEB-S5-002."
      title="Workflows"
    />
  );
}
